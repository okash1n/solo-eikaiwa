# Scaffolded 4/3/2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ユーザーフィードバック「1段レベルが高い所から始まってる」への対応。4/3/2 ブロックに準備フェーズ（お題専用チャンク＋アウトライン＋モデル聴取）を追加し、ラウンド長を 2分→1.5分→1分 に較正（メニュー設定駆動で将来 4/3/2 へ戻せる）、トピックヒントに日本語を併記する。

**Architecture:** サーバは `coach.ts` に `generatePrepPack`（既存の runner 注入＋extractJson パターン）、`routes.ts` に `POST /api/coach/prep`（modelTalk と同形の 400/404/200）、`menu.ts` に `FTT_ROUNDS_SEC` 定数と four-three-two ブロックの `params.roundsSec` を追加する。クライアントは `FourThreeTwoScreen` に `"prep"` フェーズを追加し、ラウンド秒数を props（menu params 由来）から読む。

**Tech Stack:** 既存と同一（Bun + TypeScript / React + Vite / `@anthropic-ai/claude-agent-sdk` ^0.3.201）。新規依存なし。

## Global Constraints

- サーバは 127.0.0.1:3111。Claude 呼び出しは Agent SDK の runner 注入パターン（ユニットテストは fake runner のみ、実SDK呼び出し禁止）
- **既存の HTTP 契約・既存テスト（94件）を壊さない。全変更は additive**（例外: `routes.test.ts` の `makeTestDeps` に新必須フィールド `prepPack` を足すこと、`menu.test.ts` に roundsSec ピンを足すことは本計画が明示する追加）
- モデル出力の JSON パースは extractJson + フォールバック形状 — 絶対に throw しない
- **タイマー秒数は menu params（`roundsSec`）が正。クライアントでラウンド秒をハードコードしない**（フォールバック定数は可）
- 当日分の古いメニューキャッシュ（`data/progress/menu-*.json`）には `roundsSec` が無い — クライアントはフォールバック `[120, 90, 60]` で動くこと（キャッシュ削除を要求しない）
- データはリポジトリ内プレーンファイル。コミットは Conventional Commits（日本語可）。`00-` で始まるディレクトリは一切登場させない
- ゲート: `cd app && bun test`・`bun run typecheck`・`cd app/client && bun run build`

## File Structure

```
app/server/coach.ts                     # 変更: PrepPack 型 + PREP_SYSTEM + generatePrepPack 追加
app/server/routes.ts                    # 変更: RouteDeps.prepPack + handlePrep + ルート1行
app/server/index.ts                     # 変更: prepPack の実配線（modelTalk と同形）
app/server/menu.ts                      # 変更: FTT_ROUNDS_SEC 定数 + four-three-two params に roundsSec
app/server/__tests__/coach.test.ts      # 変更: generatePrepPack のテスト追加
app/server/__tests__/routes.test.ts     # 変更: makeTestDeps に prepPack + /api/coach/prep の契約テスト
app/server/__tests__/menu.test.ts       # 変更: roundsSec ピンのテスト追加
content/topics/*.md                     # 変更: 12ファイルのヒント行に日本語併記（先頭行の "4 minutes" も削除）
app/client/src/api.ts                   # 変更: PrepPack 型 + fetchPrepPack + MenuBlock.params.roundsSec
app/client/src/screens/FourThreeTwoScreen.tsx  # 変更: prep フェーズ + roundsSec 駆動
app/client/src/screens/SessionRunner.tsx       # 変更: roundsSec を props で渡す（1行）
```

---

### Task 1: サーバ — 準備パック生成・ラウンド秒数のメニュー化・トピック日本語併記

**Files:**
- Modify: `app/server/coach.ts`（末尾に追加）
- Modify: `app/server/routes.ts`
- Modify: `app/server/index.ts`
- Modify: `app/server/menu.ts`
- Modify: `app/server/__tests__/coach.test.ts`, `app/server/__tests__/routes.test.ts`, `app/server/__tests__/menu.test.ts`
- Modify: `content/topics/*.md`（12ファイル）

**Interfaces:**
- Consumes: `coach.ts` の `extractJson` / `ClaudeRunner` / `defaultRunner` パターン、`routes.ts` の `parseJsonBody`/`json`、`menu.ts` の `MenuBlock.params`
- Produces（Task 2 が依存）:
  - `coach.ts`: `export type PrepPack = { chunks: Array<{ en: string; ja: string }>; outline: string[] }` / `generatePrepPack(args: { topicTitle: string; hints: string[] }, runner?: ClaudeRunner): Promise<PrepPack>`
  - `menu.ts`: `export const FTT_ROUNDS_SEC: readonly number[] = [120, 90, 60]`。four-three-two ブロックの `params` が `{ topic, roundsSec: [120, 90, 60] }` になる
  - HTTP: `POST /api/coach/prep` body `{topicId}` → 400（topicId欠落・不正JSON）/ 404（未知id）/ 200 PrepPack

- [ ] **Step 1: 失敗するテストを書く（coach）**

`app/server/__tests__/coach.test.ts` の import に `generatePrepPack, type PrepPack` を追加し、末尾に追記:

```ts
describe("generatePrepPack", () => {
  const valid: PrepPack = {
    chunks: [
      { en: "The main problem we had was ...", ja: "一番の問題は…でした" },
      { en: "What worked well was ...", ja: "うまくいったのは…です" },
    ],
    outline: ["Opening: what the topic is", "Point 1", "Wrap-up"],
  };

  test("正常系: JSONを構造化して返し、topicとhintsがプロンプトに入る", async () => {
    const { runner, seen } = runnerReturning(JSON.stringify(valid));
    const result = await generatePrepPack({ topicTitle: "Zero trust", hints: ["definition — 定義", "example — 例"] }, runner);
    expect(result).toEqual(valid);
    expect(seen[0].prompt).toContain("Zero trust");
    expect(seen[0].prompt).toContain("definition");
    expect(seen[0].systemPrompt).toContain("STRICT JSON");
  });

  test("```フェンス付きJSONでも取り出す", async () => {
    const { runner } = runnerReturning("```json\n" + JSON.stringify(valid) + "\n```");
    const result = await generatePrepPack({ topicTitle: "t", hints: [] }, runner);
    expect(result.chunks).toHaveLength(2);
  });

  test("パース失敗時は素のテキストをoutlineに包むフォールバック（chunksは空）", async () => {
    const { runner } = runnerReturning("just prose, no json");
    const result = await generatePrepPack({ topicTitle: "t", hints: [] }, runner);
    expect(result.chunks).toEqual([]);
    expect(result.outline.join(" ")).toContain("just prose");
  });
});
```

- [ ] **Step 2: 失敗するテストを書く（routes / menu）**

`app/server/__tests__/routes.test.ts`: `makeTestDeps` のオブジェクトリテラル内、`scenarioPrompt` の直後・`...overrides` より**前**に追加:

```ts
    prepPack: async () => ({
      chunks: [{ en: "The main problem was ...", ja: "一番の問題は…" }],
      outline: ["Opening"],
    }),
```

末尾に契約テストを追記:

```ts
describe("POST /api/coach/prep", () => {
  test("topicId欠落は400", async () => {
    const { handler } = makeHandler();
    const res = await handler(post("/api/coach/prep", {}));
    expect(res.status).toBe(400);
    expect(((await res.json()) as { error: string }).error).toContain("topicId");
  });

  test("未知のtopicIdは404", async () => {
    const { handler } = makeHandler({ prepPack: async () => null });
    const res = await handler(post("/api/coach/prep", { topicId: "nope" }));
    expect(res.status).toBe(404);
  });

  test("正常系はPrepPackを返す", async () => {
    const { handler } = makeHandler();
    const res = await handler(post("/api/coach/prep", { topicId: "zero-trust" }));
    expect(res.status).toBe(200);
    const body = (await res.json()) as { chunks: Array<{ en: string; ja: string }>; outline: string[] };
    expect(body.chunks[0].en).toContain("problem");
    expect(body.outline).toEqual(["Opening"]);
  });

  test("不正JSONボディは400", async () => {
    const { handler } = makeHandler();
    const res = await handler(
      new Request("http://x/api/coach/prep", { method: "POST", body: "{oops", headers: { "content-type": "application/json" } }),
    );
    expect(res.status).toBe(400);
  });
});
```

（`makeHandler`/`post` は既存テストのヘルパー名。実ファイルでの実名を確認し、異なる場合は既存ヘルパーに合わせること。）

`app/server/__tests__/menu.test.ts` 末尾に追記:

```ts
describe("four-three-two の roundsSec", () => {
  test("60分・30分とも roundsSec は [120, 90, 60]（スキャフォールド較正値）", () => {
    const deps = makeTempDeps(); // 既存テストの temp-dir ヘルパー名に合わせる
    const m60 = buildTodayMenu(60, deps.deps);
    const ftt60 = m60.blocks.find((b) => b.kind === "four-three-two")!;
    expect(ftt60.params.roundsSec).toEqual([120, 90, 60]);
    const m30 = buildTodayMenu(30, { ...deps.deps, menuCacheDir: deps.deps.menuCacheDir });
    const ftt30 = m30.blocks.find((b) => b.kind === "four-three-two")!;
    expect(ftt30.params.roundsSec).toEqual([120, 90, 60]);
  });
});
```

（既存 menu.test.ts の fixture/deps ヘルパーをそのまま使う。同日2回目の呼び出しはキャッシュが効くため、30分側の検証で別の temp cache dir が必要なら既存テストの流儀に合わせて分ける。）

- [ ] **Step 3: テストが失敗することを確認**

Run: `cd app && bun test server/__tests__/coach.test.ts server/__tests__/routes.test.ts server/__tests__/menu.test.ts`
Expected: FAIL（`generatePrepPack` が存在しない / `prepPack` フィールド型エラー / roundsSec undefined）

- [ ] **Step 4: coach.ts に PrepPack を実装**

`app/server/coach.ts` 末尾に追加:

```ts
export type PrepPack = { chunks: Array<{ en: string; ja: string }>; outline: string[] };

const PREP_SYSTEM = `You prepare a Japanese IT professional (CEFR A2-B1) for a short English monologue.
You receive a topic and hint angles. Reply with STRICT JSON only — no markdown fences, no commentary — exactly this shape:
{"chunks":[{"en":"<reusable spoken chunk or sentence starter, B1 level>","ja":"<自然な日本語訳>"}],"outline":["<short English bullet>"]}
Rules:
- 6-8 chunks. Each must be something the learner can say aloud as-is and reuse in similar talks
  (e.g. "The main problem we had was ...", "What worked well was ...", "Let me give you an example.").
- Prefer sentence starters and connectors over topic-specific full sentences.
- outline: 3-4 bullets forming a simple talk skeleton (opening → 1-2 points → wrap-up), tied to the given hints.`;

export async function generatePrepPack(
  args: { topicTitle: string; hints: string[] },
  runner: ClaudeRunner = defaultRunner,
): Promise<PrepPack> {
  const prompt = `Topic: ${args.topicTitle}\nHint angles:\n${args.hints.map((h) => `- ${h}`).join("\n")}`;
  const { text } = await runner(prompt, undefined, { systemPrompt: PREP_SYSTEM });
  const parsed = extractJson<PrepPack>(text);
  if (parsed && Array.isArray(parsed.chunks) && Array.isArray(parsed.outline)) return parsed;
  // パース失敗時のフォールバック: チャンクなし・素のテキストをアウトラインとして表示できる形
  return { chunks: [], outline: [text] };
}
```

- [ ] **Step 5: routes.ts / index.ts / menu.ts を実装**

`app/server/routes.ts`:
- import に `PrepPack` を追加: `import type { AeFeedback, Reflection, PrepPack } from "./coach";`
- `RouteDeps` の `scenarioPrompt` の下に追加:

```ts
  /** 未知の topicId は null（ルートは404を返す） */
  prepPack: (topicId: string) => Promise<PrepPack | null>;
```

- `handleModelTalk` の下にハンドラを追加:

```ts
async function handlePrep(req: Request, deps: RouteDeps): Promise<Response> {
  const parsed = await parseJsonBody<{ topicId?: string }>(req);
  if (!parsed.ok) return parsed.response;
  if (!parsed.body.topicId?.trim()) return json({ error: "topicId is required" }, 400);
  const pack = await deps.prepPack(parsed.body.topicId);
  if (!pack) return json({ error: "unknown topicId" }, 404);
  return json(pack);
}
```

- `makeFetchHandler` のルート列、model-talk 行の直後に追加:

```ts
      if (req.method === "POST" && url.pathname === "/api/coach/prep") return await handlePrep(req, deps);
```

`app/server/index.ts`: import に `generatePrepPack` を追加し、`realDeps` の `scenarioPrompt` の下に追加（modelTalk と同形）:

```ts
  prepPack: async (topicId) => {
    const topic = loadContent(TOPICS_DIR).find((t) => t.id === topicId);
    if (!topic) return null;
    return generatePrepPack({ topicTitle: topic.title, hints: topic.hints });
  },
```

`app/server/menu.ts`:
- 型定義群の下に定数を追加:

```ts
/** 4/3/2 ブロックのラウンド秒数。スキャフォールド較正値（流暢性が伸びたら [180,120,90] → [240,180,120] へ戻す） */
export const FTT_ROUNDS_SEC: readonly number[] = [120, 90, 60];
```

- `buildTodayMenu` 内の four-three-two ブロック2箇所（60分版・30分版）の params を変更:

```ts
          { id: "b2", kind: "four-three-two", title: `4/3/2: ${mainTopic.title}`, minutes: 16, params: { topic: mainTopic, roundsSec: [...FTT_ROUNDS_SEC] } },
```

```ts
          { id: "b2", kind: "four-three-two", title: `4/3/2: ${mainTopic.title}`, minutes: 12, params: { topic: mainTopic, roundsSec: [...FTT_ROUNDS_SEC] } },
```

- [ ] **Step 6: テストが通ることを確認**

Run: `cd app && bun test`
Expected: PASS（94 + 新規 8 = 102 前後。既存テストの破壊ゼロ）

Run: `cd app && bun run typecheck`
Expected: exit 0

- [ ] **Step 7: トピックシード12ファイルに日本語を併記**

各ファイルの本文を以下の通りに置き換える（frontmatter は変更しない。先頭行の "Talk for 4 minutes. Hints:" は "Talk about:" に変更 — ラウンド長が可変になったため分数を本文に固定しない）。

`content/topics/abac-okta.md` 本文:

```markdown
Talk about:
- What problem does ABAC solve that role-based access control cannot? — RBACで解決できない、ABACが解く問題は？
- How do device state and user attributes change an access decision? — デバイス状態やユーザー属性はアクセス判定をどう変える？
- A real example from your work: which apps, which policies? — 実務での実例: どのアプリに、どんなポリシー？
- What was hard to explain to stakeholders? — 関係者への説明で難しかった点は？
```

`content/topics/ai-agent-governance.md` 本文:

```markdown
Talk about:
- What AI agents do in your company today — 今、社内でAIエージェントが何をしているか
- What could go wrong: permissions, data access, or actions? — 何が起こりうる？権限・データアクセス・実行動作
- How identity and access control apply to non-human actors — 非人間アクターへのID・アクセス制御の適用
- One rule you would set for every AI agent — 全AIエージェントに課したいルールを1つ
```

`content/topics/ai-data-governance.md` 本文:

```markdown
Talk about:
- Which data is safe to give to AI tools, and which is not — AIツールに渡してよいデータ・だめなデータ
- How you decide: classification, contracts, or gut feeling? — 判断基準は？分類・契約・それとも勘？
- A policy or guideline you wrote or want to write — 書いた（書きたい）ポリシーやガイドライン
- What most companies get wrong about this — 多くの会社が間違えている点
```

`content/topics/blog-workflow.md` 本文:

```markdown
Talk about:
- Your writing pipeline from idea to published post — ネタ出しから公開までの執筆パイプライン
- How you use AI tools in research, drafting, and fact-checking — 調査・下書き・ファクトチェックでのAI活用
- What makes a technical article trustworthy — 信頼できる技術記事の条件
- One article you are proud of and why — 自信作を1本、その理由
```

`content/topics/corporate-it.md` 本文:

```markdown
Talk about:
- How you explain your job to someone outside IT — IT外の人に自分の仕事をどう説明するか
- SaaS management, identity, devices: which matters most and why — SaaS管理・ID・デバイス、どれが最重要？
- One project that changed how your company works — 会社の働き方を変えたプロジェクトを1つ
- What people misunderstand about corporate IT — 情シスについてよくある誤解
```

`content/topics/incident-response.md` 本文:

```markdown
Talk about:
- Walk through an incident timeline: detect, triage, contain, review — 検知→トリアージ→封じ込め→振り返りの時系列
- Who you inform, when, and in what words — 誰に・いつ・どんな言葉で伝えるか
- A real (or realistic) example you can describe safely — 安全に話せる実例（または現実的な例）
- What a good post-incident review looks like — 良いポストモーテムの条件
```

`content/topics/my-career.md` 本文:

```markdown
Talk about:
- The short version of your career story — キャリアの要約版
- A turning point and what you learned — 転機と、そこで学んだこと
- What you want to be doing in three years — 3年後にやっていたいこと
- Advice you would give your younger self — 昔の自分へのアドバイス
```

`content/topics/recent-article.md` 本文:

```markdown
Talk about:
- What the article said, in your own words — 記事の内容を自分の言葉で
- Why it caught your attention — なぜ目に留まったか
- Do you agree? What would you push back on? — 同意する？反論するなら？
- How it connects to your own work — 自分の仕事とのつながり
```

`content/topics/recruiting.md` 本文:

```markdown
Talk about:
- What you look for when you read a candidate profile — 候補者プロフィールのどこを見るか
- How you write a scout message that gets a reply — 返信されるスカウト文の書き方
- A hiring mistake you have seen or made — 見た（やった）採用の失敗
- How interviews should really be run — 面接は本当はどう運営すべきか
```

`content/topics/tech-selection.md` 本文:

```markdown
Talk about:
- Your criteria: cost, security, lock-in, team fit? — 選定基準: コスト・セキュリティ・ロックイン・チーム適合？
- A recent selection you made and the trade-offs — 最近の選定とトレードオフ
- When you say no to a shiny new tool — 流行りのツールを断るとき
- How you get buy-in from the team or management — チームや経営の合意をどう取るか
```

`content/topics/this-week-work.md` 本文:

```markdown
Talk about:
- The main thing you worked on and its goal — 今週のメインの仕事とそのゴール
- One problem you hit and how you handled it — ぶつかった問題と対処
- Who you worked with and how you communicated — 誰とどう連携したか
- What you will do next week — 来週やること
```

`content/topics/zero-trust.md` 本文:

```markdown
Talk about:
- Your one-sentence definition of zero trust — ゼロトラストの一文定義
- What you stopped trusting: network location, devices, or accounts? — 何を信頼するのをやめた？NW位置・デバイス・アカウント
- One concrete control you introduced and what changed — 導入した具体的コントロールと変化
- A common misunderstanding you often correct — よく正す誤解
```

- [ ] **Step 8: シード変更後の全ゲート確認**

Run: `cd app && bun test && bun run typecheck`
Expected: 全 PASS（menu の loadContent はヒント行をそのまま読むためパーサ変更不要。既存テストの fixture は temp dir の自前ファイルなので実シード変更の影響なし）

- [ ] **Step 9: コミット**

```bash
git add app/server/ content/topics/
git commit -m "feat: 準備パック生成とラウンド秒数のメニュー化・トピックヒントに日本語併記を追加"
```

---

### Task 2: クライアント — 準備フェーズと params 駆動ラウンド

**Files:**
- Modify: `app/client/src/api.ts`
- Modify: `app/client/src/screens/FourThreeTwoScreen.tsx`
- Modify: `app/client/src/screens/SessionRunner.tsx`

**Interfaces:**
- Consumes: Task 1 の `POST /api/coach/prep`（400/404/200 PrepPack）、menu params の `roundsSec?: number[]`、既存の `fetchModelTalk`/`ttsFetch`/`playBlob`/`stopPlayback`/`Recorder`/`useCountdown`/`sendSessionEvent`
- Produces: なし（最終タスク）

- [ ] **Step 1: api.ts に PrepPack を追加**

`app/client/src/api.ts` の型定義群（`Reflection` の下）に追加:

```ts
export type PrepPack = { chunks: Array<{ en: string; ja: string }>; outline: string[] };
```

`MenuBlock` の params 型を拡張（既存行を置き換え）:

```ts
export type MenuBlock = { id: string; kind: string; title: string; minutes: number; params: { topic?: ContentItem; scenario?: ContentItem; roundsSec?: number[] } };
```

`fetchReflection` の下に追加:

```ts
export async function fetchPrepPack(topicId: string): Promise<PrepPack> {
  const res = await fetch("/api/coach/prep", {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({ topicId }),
  });
  if (!res.ok) throw new Error(`prep failed: ${await extractErrorMessage(res)}`);
  return res.json();
}
```

- [ ] **Step 2: FourThreeTwoScreen を準備フェーズ＋roundsSec 駆動に改修**

`app/client/src/screens/FourThreeTwoScreen.tsx` を以下の内容に全置換:

```tsx
import { useEffect, useRef, useState } from "react";
import {
  fetchAeFeedback, fetchModelTalk, fetchPrepPack, sendSessionEvent, sttUpload, ttsFetch,
  type AeFeedback, type ContentItem, type PrepPack,
} from "../api";
import { playBlob, Recorder, stopPlayback } from "../audio";
import { formatMmSs, useCountdown } from "../useCountdown";

/** メニュー params に roundsSec が無い場合（当日分の古いキャッシュ等）のフォールバック */
const DEFAULT_ROUNDS_SEC = [120, 90, 60];
const PREP_SECONDS = 180;

const LISTENERS = [
  "Listener: a colleague who doesn't know this topic yet.",
  "New listener: your manager. Tell the same story, faster.",
  "New listener: someone at a conference. Same story, shorter.",
] as const;

/** 90 → "1.5分"、120 → "2分" のような表示 */
function minLabel(seconds: number): string {
  const m = seconds / 60;
  return `${Number.isInteger(m) ? m : m.toFixed(1)}分`;
}

type Phase = { kind: "prep" } | { kind: "round"; index: number } | { kind: "ae" } | { kind: "done" };
type RecState = "idle" | "recording" | "transcribing";
type PrepState = "loading" | "ready" | "error";

/**
 * スキャフォールド付き 4/3/2 流暢性ブロック。
 * 準備フェーズ（チャンク＋アウトライン＋モデル聴取）→ 同じ話を 2分→(AE)→1.5分→1分。
 * ラウンド秒数は menu params (roundsSec) が正で、流暢性の伸びに応じてサーバ側で較正する。
 */
export function FourThreeTwoScreen(props: { topic: ContentItem; sessionId: string; roundsSec?: number[] }) {
  const roundsSec =
    props.roundsSec && props.roundsSec.length === 3 && props.roundsSec.every((s) => s > 0)
      ? props.roundsSec
      : DEFAULT_ROUNDS_SEC;

  const [phase, setPhase] = useState<Phase>({ kind: "prep" });
  const [recState, setRecState] = useState<RecState>("idle");
  const [transcripts, setTranscripts] = useState<string[]>(["", "", ""]);
  // setState は非同期に反映されるため、finishRound が直後に読む用の同期ミラーを持つ
  const transcriptsRef = useRef<string[]>(["", "", ""]);
  const [ae, setAe] = useState<AeFeedback | null>(null);
  const [aeLoading, setAeLoading] = useState(false);
  const [aeSkippedNoRecording, setAeSkippedNoRecording] = useState(false);
  const [errorMsg, setErrorMsg] = useState("");
  // 準備フェーズ
  const [prepState, setPrepState] = useState<PrepState>("loading");
  const [prep, setPrep] = useState<PrepPack | null>(null);
  const [modelPlaying, setModelPlaying] = useState(false);
  const prepFetchedRef = useRef(false); // StrictMode の二重マウントで prep を二重フェッチしない
  const prepTimer = useCountdown(PREP_SECONDS);
  const recorderRef = useRef(new Recorder());
  const timer = useCountdown(roundsSec[0]);
  const roundStartedRef = useRef(false);
  const aliveRef = useRef(true);
  const roundIndexRef = useRef(0);
  const remainingRef = useRef(timer.remaining);

  const roundIndex = phase.kind === "round" ? phase.index : 0;
  useEffect(() => { roundIndexRef.current = roundIndex; }, [roundIndex]);
  useEffect(() => { remainingRef.current = timer.remaining; }, [timer.remaining]);

  useEffect(() => {
    aliveRef.current = true;
    if (!prepFetchedRef.current) {
      prepFetchedRef.current = true;
      loadPrep();
      prepTimer.start();
    }
    return () => {
      aliveRef.current = false;
      recorderRef.current.cancel();
      stopPlayback();
      if (roundStartedRef.current) {
        roundStartedRef.current = false;
        const idx = roundIndexRef.current;
        sendSessionEvent("round_end", props.sessionId, {
          block: "four-three-two",
          round: idx + 1,
          aborted: true,
          transcript: transcriptsRef.current[idx],
          elapsedSec: roundsSec[idx] - remainingRef.current,
        });
      }
    };
  }, []);

  async function loadPrep() {
    setPrepState("loading");
    setErrorMsg("");
    try {
      const pack = await fetchPrepPack(props.topic.id);
      if (!aliveRef.current) return;
      setPrep(pack);
      setPrepState("ready");
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
      setPrepState("error");
    }
  }

  async function playModelTalk() {
    setModelPlaying(true);
    setErrorMsg("");
    try {
      const text = await fetchModelTalk(props.topic.id);
      if (!aliveRef.current) return;
      const blob = await ttsFetch(text);
      if (!aliveRef.current) return;
      await playBlob(blob);
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
    } finally {
      if (aliveRef.current) setModelPlaying(false);
    }
  }

  async function toggleRecording() {
    setErrorMsg("");
    if (recState === "idle") {
      try {
        await recorderRef.current.start();
        setRecState("recording");
        if (!timer.running && !timer.expired) {
          timer.start();
          roundStartedRef.current = true;
          sendSessionEvent("round_start", props.sessionId, { block: "four-three-two", round: roundIndex + 1 });
        }
      } catch (err) {
        setErrorMsg(`マイクにアクセスできません: ${err instanceof Error ? err.message : String(err)}`);
      }
      return;
    }
    if (recState !== "recording") return;
    try {
      setRecState("transcribing");
      const blob = await recorderRef.current.stop();
      if (!aliveRef.current) return;
      const text = await sttUpload(blob);
      if (!aliveRef.current) return;
      transcriptsRef.current[roundIndex] = [transcriptsRef.current[roundIndex], text]
        .filter(Boolean)
        .join(" ");
      setTranscripts([...transcriptsRef.current]);
      setRecState("idle");
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
      setRecState("idle");
    }
  }

  async function finishRound() {
    if (recState === "recording") await toggleRecording();
    if (!aliveRef.current) return;
    timer.pause();
    if (roundStartedRef.current) {
      roundStartedRef.current = false;
      sendSessionEvent("round_end", props.sessionId, {
        block: "four-three-two",
        round: roundIndex + 1,
        transcript: transcriptsRef.current[roundIndex],
        elapsedSec: roundsSec[roundIndex] - timer.remaining,
      });
    }
    if (roundIndex === 0) {
      setPhase({ kind: "ae" });
      const transcript = transcriptsRef.current[0];
      if (!transcript.trim()) {
        setAeSkippedNoRecording(true);
        setAe(null);
      } else {
        setAeSkippedNoRecording(false);
        setAeLoading(true);
        try {
          const feedback = await fetchAeFeedback(transcript, props.topic.title);
          if (!aliveRef.current) return;
          setAe(feedback);
        } catch (err) {
          if (!aliveRef.current) return;
          setErrorMsg(err instanceof Error ? err.message : String(err));
        } finally {
          if (aliveRef.current) setAeLoading(false);
        }
      }
    } else if (roundIndex < roundsSec.length - 1) {
      startRound(roundIndex + 1);
    } else {
      setPhase({ kind: "done" });
    }
  }

  function startRound(index: number) {
    setPhase({ kind: "round", index });
    timer.reset(roundsSec[index]);
    roundStartedRef.current = false;
  }

  if (phase.kind === "prep") {
    return (
      <div>
        <h3>準備 — {props.topic.title}</h3>
        {props.topic.titleJa && <p style={{ color: "#666" }}>{props.topic.titleJa}</p>}
        <p style={{ color: "#666" }}>
          これから同じ話を {roundsSec.map(minLabel).join("→")} で3回話します。まず使えそうな表現と骨組みを確認してください（目安 {minLabel(PREP_SECONDS)}）。
        </p>
        <p style={{ fontVariantNumeric: "tabular-nums" }}>⏱ 準備 {formatMmSs(prepTimer.remaining)}{prepTimer.expired && " — そろそろ始めましょう"}</p>
        <ul>
          {props.topic.hints.map((h, i) => (
            <li key={i}>{h}</li>
          ))}
        </ul>
        {prepState === "loading" && <p>コーチが表現チャンクを用意しています…</p>}
        {prepState === "error" && (
          <p style={{ color: "crimson" }}>
            {errorMsg} <button onClick={loadPrep}>再試行</button>
          </p>
        )}
        {prepState === "ready" && prep && (
          <div>
            {prep.chunks.length > 0 && (
              <div>
                <h4>使える表現</h4>
                <ul>
                  {prep.chunks.map((c, i) => (
                    <li key={i}>
                      <strong>{c.en}</strong> <span style={{ color: "#666" }}>— {c.ja}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}
            {prep.outline.length > 0 && (
              <div>
                <h4>話の骨組み</h4>
                <ol>
                  {prep.outline.map((o, i) => (
                    <li key={i}>{o}</li>
                  ))}
                </ol>
              </div>
            )}
          </div>
        )}
        <p>
          <button onClick={playModelTalk} disabled={modelPlaying} style={{ padding: "0.6rem 1.2rem" }}>
            {modelPlaying ? "🔊 再生中…" : "🎧 モデルトークを聞く（任意）"}
          </button>{" "}
          <button onClick={() => startRound(0)} style={{ padding: "0.6rem 1.2rem" }}>
            Round 1 を始める（{minLabel(roundsSec[0])}）→
          </button>
        </p>
        {prepState !== "error" && errorMsg && <p style={{ color: "crimson" }}>{errorMsg}</p>}
      </div>
    );
  }

  if (phase.kind === "ae") {
    return (
      <div>
        <h3>フィードバック（読んだら Round 2 へ）</h3>
        {aeLoading && <p>コーチがフィードバックを書いています…</p>}
        {aeSkippedNoRecording && <p>録音がなかったのでフィードバックはありません</p>}
        {ae && (
          <div>
            {ae.praise && <p>👏 {ae.praise}</p>}
            <ul>
              {ae.items.map((item, i) => (
                <li key={i} style={{ marginBottom: "0.6rem" }}>
                  {item.quote && (
                    <div>
                      <s>{item.quote}</s> → <strong>{item.better}</strong> <em>({item.issue})</em>
                    </div>
                  )}
                  <div>{item.why_ja}</div>
                </li>
              ))}
            </ul>
          </div>
        )}
        {errorMsg && <p style={{ color: "crimson" }}>{errorMsg}</p>}
        <button onClick={() => startRound(1)} disabled={aeLoading} style={{ padding: "0.6rem 1.2rem" }}>
          Round 2 を始める（{minLabel(roundsSec[1])}）
        </button>
      </div>
    );
  }

  if (phase.kind === "done") {
    return <p>4/3/2 完了！同じ話を3回、少しずつ速く話せました。</p>;
  }

  return (
    <div>
      <h3>
        Round {roundIndex + 1}（{minLabel(roundsSec[roundIndex])}） — {props.topic.title}
      </h3>
      <p style={{ color: "#666" }}>{LISTENERS[roundIndex]}</p>
      <ul>
        {props.topic.hints.map((h, i) => (
          <li key={i}>{h}</li>
        ))}
      </ul>
      <p style={{ fontSize: "2rem", fontVariantNumeric: "tabular-nums" }}>
        ⏱ {formatMmSs(timer.remaining)} {timer.expired && "— 時間切れ！"}
      </p>
      <button onClick={toggleRecording} disabled={recState === "transcribing"} style={{ padding: "0.6rem 1.2rem" }}>
        {recState === "recording" ? "⏹ 録音を止める" : recState === "transcribing" ? "📝 文字起こし中…" : "🎙 話し始める"}
      </button>{" "}
      <button onClick={finishRound} disabled={recState === "transcribing"} style={{ padding: "0.6rem 1.2rem" }}>
        このラウンドを終える →
      </button>
      {errorMsg && <p style={{ color: "crimson" }}>{errorMsg}</p>}
      {transcripts[roundIndex] && (
        <p style={{ whiteSpace: "pre-wrap" }}>
          <strong>You:</strong> {transcripts[roundIndex]}
        </p>
      )}
    </div>
  );
}
```

- [ ] **Step 3: SessionRunner から roundsSec を渡す**

`app/client/src/screens/SessionRunner.tsx` の `FourThreeTwoScreen` を描画している箇所（`BlockBody` 内の four-three-two ケース）で、props に `roundsSec={block.params.roundsSec}` を追加する。例（既存の行に合わせて調整）:

```tsx
      return <FourThreeTwoScreen topic={block.params.topic} sessionId={sessionId} roundsSec={block.params.roundsSec} />;
```

- [ ] **Step 4: ビルドゲート**

Run: `cd app/client && bun run build`
Expected: tsc --noEmit && vite build が PASS

Run: `cd app && bun test`
Expected: 全 PASS（サーバは Task 1 から不変）

- [ ] **Step 5: 実サーバ・プロキシ経由の軽いスモーク**

```bash
cd app && bun run dev &            # :3111（起動を数秒待つ）
curl -s -X POST http://127.0.0.1:3111/api/coach/prep -H 'content-type: application/json' -d '{"topicId":"nope"}' | head -c 200; echo
curl -s "http://127.0.0.1:3111/api/menu/today?minutes=30" | head -c 400; echo
# 期待: 404 {"error":"unknown topicId"} / メニューJSONの four-three-two params に roundsSec:[120,90,60]
#（注意: 当日キャッシュ menu-YYYY-MM-DD-30.json が既にあると roundsSec が無い旧形状が返る。
#  その場合は data/progress/ の当日キャッシュファイルを削除して再実行し、確認後はそのままでよい）
lsof -ti :3111 | xargs kill
```

実 Claude を呼ぶ prep の正常系はユーザーの実走で確認する（このスモークでは呼ばない）。

- [ ] **Step 6: コミット**

```bash
git add app/client/
git commit -m "feat: 4/3/2に準備フェーズを追加しラウンド長をメニュー設定駆動に変更"
```

---

## Self-Review 結果（プラン作成時に実施済み）

- **スペック整合**: 承認済み3点（準備フェーズ＝チャンク提示＋モデル聴取、2/1.5/1分への較正＝時間圧原則の維持と段階的復帰、日本語ヒント併記）を Task 1/2 でカバー。ブロック分数（16/12分）は不変で spec §5.2/§5.3 と矛盾しない（準備3分＋ラウンド計4.5分＋AE約1分＋操作時間 ≈ 9-10分 < 12分）
- **型整合**: `PrepPack` はサーバ（coach.ts）とクライアント（api.ts）で同形。`roundsSec` は `MenuBlock.params`（`Record<string, unknown>` のためサーバ側は型変更不要）→ クライアント側は明示フィールド化。`FourThreeTwoScreen` の props 追加は optional で SessionRunner 以外の呼び出し元なし
- **プレースホルダスキャン**: なし。テストヘルパー名（`makeHandler`/`post`/`makeTempDeps`）のみ「実ファイルの既存名に合わせる」注記付き — 実装者は既存テストを開いて確認すること
- **注意点**: (1) 当日の古いメニューキャッシュには roundsSec が無い — クライアントフォールバックで動作、スモーク時のみキャッシュ削除が必要になる場合あり（Step 5 に記載）。(2) routes.test.ts の `makeTestDeps` は必須フィールド追加なので、追加を忘れると typecheck が落ちて検出される（意図した設計）。(3) 旧 `ROUNDS` 定数の削除に伴い、`LISTENERS` 配列と `minLabel` が新設され、ラウンド見出しは動的生成になる
