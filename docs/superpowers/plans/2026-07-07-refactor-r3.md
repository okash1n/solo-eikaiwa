# R3: サーバモジュール境界の整理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** サーバ層のモジュール境界を整理し、menu.ts の5責務同居・assessment⇄content-gen の循環 import・defaultRunner 重複・ストア規約の非一貫・swallow のコピペを解消しつつ、P1 最終レビューの繰越修正（F）を取り込む。

**Architecture:** menu.ts から `content.ts`（frontmatter パース + ロード + find）と `rotation.ts`（選択アルゴリズム + 永続化）を抽出。SRS 分析を `srs-analytics.ts` に切り出して循環を断つ。LLM ランナーを converse.ts の単一 `defaultRunner` に集約。各ストアが自テーブルの `ensureSchema(db)` を持ち openDb は合成のみ、`insertReturningId` を `db-util.ts` で共有。ルート層の swallow を `bestEffort(label, fn)` に畳む。

**Tech Stack:** Bun + TypeScript、bun:sqlite、`@anthropic-ai/claude-agent-sdk`、React（クライアントは F の1行のみ）。

## Global Constraints

- **A〜E は純リファクタ**: HTTP 契約不変・クライアント挙動不変。テストは新モジュールへ import 経路を張り替えてよいが、**削除・弱体化しない**（現在 330 テスト）。
- **F は意図的な小さい挙動改善**（空訳 502 / phrase-hint の ja フィルタ緩和 / history 文字数キャップ / translate 単体テスト追加 / クライアント再試行ボタン disabled）。F の各挙動変更は**テストファースト**（失敗テスト→実装→緑）。F5（クライアント1行）だけはサーバのテスト基盤外なので client build を唯一のゲートにする。
- **1 タスク = 1 コミット**。Conventional Commits（`refactor:` / `test:` / `fix:` を使い分ける）。
- **各タスク末尾の検証ゲート**（3 つとも緑を確認してからコミット）:
  - `cd app && bun test`
  - `cd app && bun run typecheck`
  - `cd app/client && bun run build`
- `data/` ・ royal300・生成コンテンツ本体には触れない（コード境界の変更のみ）。
- ログ prefix（`[content]` `[rotation]` 等）は移動先モジュール名に合わせて更新してよい。既存テストは warn 文言を assert していないため影響なし。

---

## Task 1: menu.ts を content.ts / rotation.ts に分解し index.ts の重複検索を解消

**Files:**
- Create: `app/server/content.ts`
- Create: `app/server/rotation.ts`
- Modify: `app/server/menu.ts`（縮小 — 現状 350 行 → ブロック構築＋キャッシュのみ）
- Modify: `app/server/index.ts:1,6,50-67`（loadContent/find クロージャ → findTopic/findScenario）
- Modify: `app/server/content-gen.ts:9`（loadContent の import 元）
- Modify: `app/server/routes/menu.ts:1`（DOMAINS/Domain の import 元）
- Modify（テストの import 張り替え）: `app/server/__tests__/menu.test.ts:5-8`, `app/server/__tests__/content.test.ts:2`, `app/server/__tests__/content-gen.test.ts:6`

**Interfaces:**
- Produces（content.ts）: `type Domain = "daily" | "business" | "it"`、`const DOMAINS: readonly Domain[]`、`type ContentItem`、`parseContentFile(text: string): ContentItem | null`、`loadContent(dir: string): ContentItem[]`、`findTopic(id: string): ContentItem | undefined`、`findScenario(id: string): ContentItem | undefined`
- Produces（rotation.ts）: `type UsageMap`、`type RotationState`、`readJsonSafe<T>(file: string): T | undefined`、`pickNext(items, usage, todayYmd): ContentItem`、`filterInBand(items, stage): ContentItem[]`、`pickInDomain(items, state, todayYmd, stage, domain): ContentItem`、`pickNextByDomain(items, state, todayYmd, stage, kind): ContentItem`、`markUsed(usage, id, ymd): void`、`loadRotation(usageFile): RotationState`、`saveRotation(usageFile, state): void`
- Produces（menu.ts 残置）: `BlockKind`/`BLOCK_KINDS`/`MenuBlock`/`Menu`/`QuickKind`/`QUICK_KINDS`/`MenuDeps`、`roleplayTitle`、`buildTodayMenu`、`buildQuickMenu`、`invalidateTodayMenuCache`

- [ ] **Step 1: content.ts を新規作成**

`app/server/content.ts`（parseDomain/parseLevelRange/parseContentFile/loadContent は menu.ts:36-85 を移動。findTopic/findScenario は新規）:

```ts
import { existsSync, readdirSync, readFileSync } from "node:fs";
import path from "node:path";
import { SCENARIOS_DIR, TOPICS_DIR } from "./paths";

export type Domain = "daily" | "business" | "it";
/** ドメインの巡回順（ラウンドロビンはこの順で次を探す） */
export const DOMAINS: readonly Domain[] = ["daily", "business", "it"];

export type ContentItem = {
  id: string; kind: "topic" | "scenario"; title: string; titleJa: string; hints: string[];
  starters: string[];
  domain: Domain; level: [number, number];
};

function parseDomain(raw: string | undefined): Domain {
  if (raw === undefined) return "it";
  if ((DOMAINS as readonly string[]).includes(raw)) return raw as Domain;
  console.warn(`[content] invalid domain "${raw}", falling back to "it"`);
  return "it";
}

/** level: [min, max]（1..6, min<=max）。省略はデフォルト、不正は警告してデフォルト */
function parseLevelRange(raw: string | undefined): [number, number] {
  if (raw === undefined) return [1, 6];
  const m = raw.match(/^\[\s*(\d+)\s*,\s*(\d+)\s*\]$/);
  if (m) {
    const min = Number(m[1]);
    const max = Number(m[2]);
    if (min >= 1 && max <= 6 && min <= max) return [min, max];
  }
  console.warn(`[content] invalid level "${raw}", falling back to [1, 6]`);
  return [1, 6];
}

export function parseContentFile(text: string): ContentItem | null {
  const m = text.match(/^---\n([\s\S]*?)\n---/);
  if (!m) return null;
  const fields: Record<string, string> = {};
  for (const line of m[1].split("\n")) {
    const i = line.indexOf(":");
    if (i === -1) continue;
    fields[line.slice(0, i).trim()] = line.slice(i + 1).trim().replace(/^"(.*)"$/, "$1");
  }
  if (!fields.id || !fields.title || (fields.kind !== "topic" && fields.kind !== "scenario")) return null;
  const hints = text.slice(m[0].length).split("\n")
    .filter((l) => l.trim().startsWith("- "))
    .map((l) => l.trim().slice(2));
  const starters = text.slice(m[0].length).split("\n")
    .filter((l) => l.trim().startsWith("> "))
    .map((l) => l.trim().slice(2).trim());
  return {
    id: fields.id, kind: fields.kind, title: fields.title, titleJa: fields.title_ja ?? "", hints, starters,
    domain: parseDomain(fields.domain), level: parseLevelRange(fields.level),
  };
}

export function loadContent(dir: string): ContentItem[] {
  if (!existsSync(dir)) return [];
  return readdirSync(dir)
    .filter((f) => f.endsWith(".md"))
    .sort()
    .map((f) => parseContentFile(readFileSync(path.join(dir, f), "utf8")))
    .filter((c): c is ContentItem => c !== null);
}

/** topicId → トピック定義（未知は undefined）。index.ts の配線クロージャの重複検索を1箇所に集約 */
export function findTopic(id: string): ContentItem | undefined {
  return loadContent(TOPICS_DIR).find((t) => t.id === id);
}

/** scenarioId → シナリオ定義（未知は undefined） */
export function findScenario(id: string): ContentItem | undefined {
  return loadContent(SCENARIOS_DIR).find((s) => s.id === id);
}
```

- [ ] **Step 2: rotation.ts を新規作成**

`app/server/rotation.ts`（pickNext/filterInBand/pickInDomain/pickNextByDomain/markUsed/readJsonSafe/freshRotation/sanitizeUsage/loadRotation/saveRotation は menu.ts:88-212 を移動。markUsed は新たに export する）:

```ts
import { existsSync, mkdirSync, readFileSync, writeFileSync } from "node:fs";
import path from "node:path";
import { addDaysYmd } from "./dates";
import { DOMAINS, type ContentItem, type Domain } from "./content";

/** id → 使用日(YYYY-MM-DD)の配列。新しい日付が末尾、最大7件保持 */
export type UsageMap = Record<string, string[]>;

/** rotation 永続化 v2。旧形式（UsageMap 直置き）は読み込み時に移行する */
export type RotationState = {
  version: 2;
  usage: UsageMap;
  lastDomain: { topic: Domain | ""; scenario: Domain | "" };
};

/** JSON ファイルを読み込む。存在しない・パース失敗時は警告のみで undefined を返す（呼び出し側でフォールバック） */
export function readJsonSafe<T>(file: string): T | undefined {
  if (!existsSync(file)) return undefined;
  try {
    return JSON.parse(readFileSync(file, "utf8")) as T;
  } catch {
    console.warn(`[rotation] failed to parse JSON, ignoring: ${file}`);
    return undefined;
  }
}

/**
 * least-recently-used ローテーション。未使用が最優先、次に最終使用が古い順（同着はid順）。
 * 前日・前々日の両方に使ったアイテムは除外する（3日連続の同一素材を避ける。
 * ただし全アイテムが除外される場合は全体から選ぶ）。
 */
export function pickNext(items: ContentItem[], usage: UsageMap, todayYmd: string): ContentItem {
  if (items.length === 0) throw new Error("no content items available");
  const y1 = addDaysYmd(todayYmd, -1);
  const y2 = addDaysYmd(todayYmd, -2);
  const eligible = items.filter((it) => {
    const dates = usage[it.id] ?? [];
    return !(dates.includes(y1) && dates.includes(y2));
  });
  const pool = eligible.length > 0 ? eligible : items;
  const lastUsed = (it: ContentItem) => {
    const d = usage[it.id] ?? [];
    return d.length ? d[d.length - 1] : "";
  };
  return [...pool].sort((a, b) => {
    const la = lastUsed(a);
    const lb = lastUsed(b);
    if (la !== lb) return la < lb ? -1 : 1;
    return a.id.localeCompare(b.id);
  })[0];
}

/** stage 適合プール（空なら全体にフォールバック） */
export function filterInBand(items: ContentItem[], stage: number): ContentItem[] {
  const inBand = items.filter((it) => it.level[0] <= stage && stage <= it.level[1]);
  return inBand.length > 0 ? inBand : items;
}

/** ドメイン明示指定の選択。帯域内→ドメイン全体の順で選び、ラウンドロビンのカーソルは動かさない */
export function pickInDomain(
  items: ContentItem[], state: RotationState, todayYmd: string, stage: number, domain: Domain,
): ContentItem {
  const inDomain = items.filter((it) => it.domain === domain);
  if (inDomain.length === 0) throw new Error(`no content items for domain: ${domain}`);
  return pickNext(filterInBand(inDomain, stage), state.usage, todayYmd);
}

/**
 * 帯域フィルタ → ドメインラウンドロビン → LRU の選択（スペック §7.3）。
 * stage 適合プール（空なら全体にフォールバック）から、前回ドメインの次を優先して
 * 存在する最初のドメインを選び、ドメイン内は pickNext（LRU + 3日連続回避）。
 */
export function pickNextByDomain(
  items: ContentItem[], state: RotationState, todayYmd: string, stage: number, kind: "topic" | "scenario",
): ContentItem {
  if (items.length === 0) throw new Error("no content items available");
  const pool = filterInBand(items, stage);
  const last = state.lastDomain[kind];
  const start = last === "" ? 0 : (DOMAINS.indexOf(last) + 1) % DOMAINS.length;
  for (let i = 0; i < DOMAINS.length; i++) {
    const domain = DOMAINS[(start + i) % DOMAINS.length];
    const sub = pool.filter((it) => it.domain === domain);
    if (sub.length === 0) continue;
    const picked = pickNext(sub, state.usage, todayYmd);
    state.lastDomain[kind] = domain;
    return picked;
  }
  return pickNext(pool, state.usage, todayYmd); // 論理上到達しない安全網
}

export function markUsed(usage: UsageMap, id: string, ymd: string): void {
  const dates = usage[id] ?? [];
  if (!dates.includes(ymd)) dates.push(ymd);
  usage[id] = dates.slice(-7);
}

function freshRotation(): RotationState {
  return { version: 2, usage: {}, lastDomain: { topic: "", scenario: "" } };
}

/** 値が string[] のエントリだけ残す（手動編集で混入した不正値で markUsed が落ちないように） */
function sanitizeUsage(raw: unknown): UsageMap {
  if (typeof raw !== "object" || raw === null) return {};
  const out: UsageMap = {};
  for (const [k, v] of Object.entries(raw)) {
    if (Array.isArray(v) && v.every((d) => typeof d === "string")) out[k] = v as string[];
  }
  return out;
}

/** v2 を読む。旧形式（id→日付配列の直置き）は usage として移行。不明形状は初期状態 */
export function loadRotation(usageFile: string): RotationState {
  const raw = readJsonSafe<Record<string, unknown>>(usageFile);
  if (raw === undefined) return freshRotation();
  if (raw.version === 2 && typeof raw.usage === "object" && raw.usage !== null) {
    const last = (raw.lastDomain ?? {}) as Partial<RotationState["lastDomain"]>;
    const valid = (v: unknown): v is Domain | "" => v === "" || (DOMAINS as readonly string[]).includes(v as string);
    return {
      version: 2,
      usage: sanitizeUsage(raw.usage),
      lastDomain: { topic: valid(last.topic) ? last.topic : "", scenario: valid(last.scenario) ? last.scenario : "" },
    };
  }
  // 旧形式: すべての値が配列なら UsageMap とみなして移行
  if (Object.values(raw).every((v) => Array.isArray(v))) {
    return { ...freshRotation(), usage: sanitizeUsage(raw) };
  }
  console.warn(`[rotation] unknown rotation state shape, starting fresh: ${usageFile}`);
  return freshRotation();
}

export function saveRotation(usageFile: string, state: RotationState): void {
  mkdirSync(path.dirname(usageFile), { recursive: true });
  writeFileSync(usageFile, JSON.stringify(state, null, 2));
}
```

- [ ] **Step 3: menu.ts を縮小する**

`app/server/menu.ts` の全体を次の内容に置き換える（type 群・roleplayTitle・isValidMenuShape・buildTodayMenu・invalidateTodayMenuCache・buildQuickMenu は現状の menu.ts:7-27,129-132,214-349 のロジックそのまま。抽出済み関数は content.ts / rotation.ts から import）:

```ts
import { existsSync, mkdirSync, readdirSync, unlinkSync, writeFileSync } from "node:fs";
import path from "node:path";
import { localYmd } from "./dates";
import { PROGRESS_DIR, SCENARIOS_DIR, TOPICS_DIR } from "./paths";
import { DEFAULT_LEVEL, fttMiniRoundsSec, fttRoundsSec, prepParams, stageOf } from "./progression";
import { loadContent, type ContentItem, type Domain } from "./content";
import {
  filterInBand, loadRotation, markUsed, pickInDomain, pickNext, pickNextByDomain, readJsonSafe, saveRotation,
} from "./rotation";

export type BlockKind = "chunk-placeholder" | "warmup-reading" | "four-three-two" | "roleplay" | "shadowing" | "reflection";
/** BlockKind の全メンバー列挙（単一ソース）。routes のバリデーションなど値配列が必要な箇所から import する */
export const BLOCK_KINDS = [
  "chunk-placeholder", "warmup-reading", "four-three-two", "roleplay", "shadowing", "reflection",
] as const satisfies readonly BlockKind[];

export type MenuBlock = { id: string; kind: BlockKind; title: string; minutes: number; params: Record<string, unknown> };
/** level: メニュー構築時点のレベル。日次キャッシュの有効性判定（isValidMenuShape）に使う */
export type Menu = { minutes: number; date: string; level: number; blocks: MenuBlock[] };

export type QuickKind = "warmup" | "ftt-mini" | "roleplay" | "shadowing";
export const QUICK_KINDS: readonly QuickKind[] = ["warmup", "ftt-mini", "roleplay", "shadowing"];

/** ロールプレイのタイトル接頭辞は選ばれたシナリオの実ドメインで付ける */
export function roleplayTitle(scenario: ContentItem): string {
  const label = scenario.domain === "daily" ? "日常" : scenario.domain === "business" ? "ビジネス" : "IT";
  return `${label}ロールプレイ: ${scenario.title}`;
}

/**
 * JSONとしては妥当でも Menu の形になっていないキャッシュ（手動編集・古いフォーマット等）を弾く。
 * level フィールドを必須にすることで、Phase B 以前（レベル付きファイル名時代）の
 * キャッシュ本体が万一残っていても自動的に無効化され再構築される。
 */
function isValidMenuShape(value: unknown): value is Menu {
  const v = value as Partial<Menu> | undefined;
  return Array.isArray(v?.blocks) && v.blocks.length > 0 && typeof v?.level === "number";
}

export type MenuDeps = {
  topicsDir?: string;
  scenariosDir?: string;
  usageFile?: string;
  menuCacheDir?: string;
  today?: () => Date;
  /** 利用者レベル（1〜）。省略時 DEFAULT_LEVEL。stage・4/3/2秒数・準備支援を駆動する */
  level?: number;
  /** クイックロールプレイのドメイン明示指定。省略時はラウンドロビン */
  domain?: Domain;
};

export function buildTodayMenu(minutes: 60 | 30, deps: MenuDeps = {}): Menu {
  const topicsDir = deps.topicsDir ?? TOPICS_DIR;
  const scenariosDir = deps.scenariosDir ?? SCENARIOS_DIR;
  const usageFile = deps.usageFile ?? path.join(PROGRESS_DIR, "topic-usage.json");
  const menuCacheDir = deps.menuCacheDir ?? PROGRESS_DIR;
  const ymd = localYmd((deps.today ?? (() => new Date()))());

  const level = deps.level ?? DEFAULT_LEVEL;
  const stage = stageOf(level);
  // キャッシュキーに level は含めない: 自動昇格が同日中に何度も起きても当日のメニューは固定する
  // （lv接尾辞ありのファイル名は誤って読まれない＝旧形式は自然に無効化される）。
  // レベル変更を同日に反映したい場合は invalidateTodayMenuCache を呼ぶ（明示的な変更のみ）。
  const cacheFile = path.join(menuCacheDir, `menu-${ymd}-${minutes}.json`);
  const cached = readJsonSafe<Menu>(cacheFile);
  if (cached) {
    if (isValidMenuShape(cached)) return cached;
    console.warn(`[menu] cached menu has unexpected shape, rebuilding: ${cacheFile}`);
  }

  const state = loadRotation(usageFile);
  const topics = loadContent(topicsDir);
  const scenarios = loadContent(scenariosDir);

  const mainTopic = pickNextByDomain(topics, state, ymd, stage, "topic");
  const scenario = pickNextByDomain(scenarios, state, ymd, stage, "scenario");
  // シャドーイング素材は「次にローテーションが選ぶトピック」のプレビュー。
  // 使用済みマーク・ドメインカーソルの前進はしない（帯域フィルタだけ適用）
  const others = topics.filter((t) => t.id !== mainTopic.id);
  const shadowPool = others.length > 0 ? filterInBand(others, stage) : others;
  const shadowTopic = shadowPool.length > 0 ? pickNext(shadowPool, state.usage, ymd) : mainTopic;

  markUsed(state.usage, mainTopic.id, ymd);
  markUsed(state.usage, scenario.id, ymd);
  saveRotation(usageFile, state);

  const warmupTitle = "音読ウォームアップ";
  const blocks: MenuBlock[] =
    minutes === 60
      ? [
          { id: "b1", kind: "warmup-reading", title: warmupTitle, minutes: 8, params: { topic: mainTopic } },
          { id: "b2", kind: "four-three-two", title: `4/3/2: ${mainTopic.title}`, minutes: 16, params: { topic: mainTopic, roundsSec: fttRoundsSec(level), modelTalkMode: prepParams(stage).modelTalk } },
          { id: "b3", kind: "roleplay", title: roleplayTitle(scenario), minutes: 20, params: { scenario } },
          { id: "b4", kind: "shadowing", title: `シャドーイング: ${shadowTopic.title}`, minutes: 8, params: { topic: shadowTopic } },
          { id: "b5", kind: "reflection", title: "振り返り", minutes: 5, params: {} },
        ]
      : [
          { id: "b1", kind: "warmup-reading", title: warmupTitle, minutes: 6, params: { topic: mainTopic } },
          { id: "b2", kind: "four-three-two", title: `4/3/2: ${mainTopic.title}`, minutes: 12, params: { topic: mainTopic, roundsSec: fttRoundsSec(level), modelTalkMode: prepParams(stage).modelTalk } },
          { id: "b3", kind: "roleplay", title: roleplayTitle(scenario), minutes: 10, params: { scenario } },
          { id: "b4", kind: "reflection", title: "振り返り", minutes: 2, params: {} },
        ];

  const menu: Menu = { minutes, date: ymd, level, blocks };
  mkdirSync(menuCacheDir, { recursive: true });
  writeFileSync(cacheFile, JSON.stringify(menu, null, 2));
  return menu;
}

/**
 * 当日分の通しメニューキャッシュ（menu-<ymd>-*.json）を削除する。
 * 自動昇格では呼ばない（次回ビルドまで反映を持ち越すのが仕様）。
 * 明示的なレベル変更（accept/set）の直後にだけ呼び、その日のうちに新レベルを反映させる。
 */
export function invalidateTodayMenuCache(todayYmd?: string, cacheDir?: string): void {
  const menuCacheDir = cacheDir ?? PROGRESS_DIR;
  const ymd = todayYmd ?? localYmd();
  if (!existsSync(menuCacheDir)) return;
  const prefix = `menu-${ymd}-`;
  for (const f of readdirSync(menuCacheDir)) {
    if (f.startsWith(prefix) && f.endsWith(".json")) unlinkSync(path.join(menuCacheDir, f));
  }
}

/**
 * 5〜10分の単品ドリルメニュー。日次のデフォルト導線（第3弾リサーチ: 総時間より頻度・完了数が効く）。
 * トピック/シナリオのローテーションと使用記録は buildTodayMenu と共有する。
 * 通しメニューと違いキャッシュしない（同日の再実行は次のアイテムが出る＝意図どおり）。
 */
export function buildQuickMenu(kind: QuickKind, deps: MenuDeps = {}): Menu {
  const topicsDir = deps.topicsDir ?? TOPICS_DIR;
  const scenariosDir = deps.scenariosDir ?? SCENARIOS_DIR;
  const usageFile = deps.usageFile ?? path.join(PROGRESS_DIR, "topic-usage.json");
  const ymd = localYmd((deps.today ?? (() => new Date()))());
  const level = deps.level ?? DEFAULT_LEVEL;
  const stage = stageOf(level);
  const state = loadRotation(usageFile);

  let block: MenuBlock;
  if (kind === "roleplay") {
    const all = loadContent(scenariosDir);
    // ドメイン明示時はそのドメイン内から（ラウンドロビンのカーソル不変）、省略時は従来のローテーション
    const scenario = deps.domain
      ? pickInDomain(all, state, ymd, stage, deps.domain)
      : pickNextByDomain(all, state, ymd, stage, "scenario");
    markUsed(state.usage, scenario.id, ymd);
    block = { id: "q1", kind: "roleplay", title: roleplayTitle(scenario), minutes: 10, params: { scenario } };
  } else {
    const topic = pickNextByDomain(loadContent(topicsDir), state, ymd, stage, "topic");
    markUsed(state.usage, topic.id, ymd);
    if (kind === "warmup") {
      block = { id: "q1", kind: "warmup-reading", title: "音読ウォームアップ", minutes: 6, params: { topic } };
    } else if (kind === "ftt-mini") {
      block = {
        id: "q1", kind: "four-three-two", title: `4/3/2ミニ: ${topic.title}`, minutes: 8,
        params: { topic, roundsSec: fttMiniRoundsSec(level), modelTalkMode: prepParams(stage).modelTalk },
      };
    } else {
      block = { id: "q1", kind: "shadowing", title: `シャドーイング: ${topic.title}`, minutes: 5, params: { topic } };
    }
  }

  saveRotation(usageFile, state);
  return { minutes: block.minutes, date: ymd, level, blocks: [block] };
}
```

- [ ] **Step 4: index.ts を findTopic/findScenario に張り替える**

`app/server/index.ts`:
- 行 1 の paths import から `SCENARIOS_DIR, TOPICS_DIR` を削除（他で未使用になる）: `import { ensureDirs, RECORDINGS_DIR, sessionLogPath } from "./paths";`
- 行 6 を `import { buildQuickMenu, buildTodayMenu, invalidateTodayMenuCache } from "./menu";` に変更し、新規行 `import { findScenario, findTopic } from "./content";` を追加。
- 3 クロージャの検索を置換:
  - `modelTalk` 内 `const topic = loadContent(TOPICS_DIR).find((t) => t.id === topicId);` → `const topic = findTopic(topicId);`
  - `scenarioPrompt` 内 `const sc = loadContent(SCENARIOS_DIR).find((s) => s.id === scenarioId);` → `const sc = findScenario(scenarioId);`
  - `prepPack` 内 `const topic = loadContent(TOPICS_DIR).find((t) => t.id === topicId);` → `const topic = findTopic(topicId);`

- [ ] **Step 5: content-gen.ts と routes/menu.ts の import 元を更新**

- `app/server/content-gen.ts:9`: `import { loadContent } from "./menu";` → `import { loadContent } from "./content";`
- `app/server/routes/menu.ts:1`: `import { DOMAINS, QUICK_KINDS, type Domain, type Menu, type QuickKind } from "../menu";` を次の2行に:
  ```ts
  import { QUICK_KINDS, type Menu, type QuickKind } from "../menu";
  import { DOMAINS, type Domain } from "../content";
  ```
- `app/server/routes/progress.ts:1`（`import { BLOCK_KINDS } from "../menu";`）は **変更不要**（BLOCK_KINDS は menu.ts に残す）。

- [ ] **Step 6: テストの import 経路を張り替える（削除・弱体化しない）**

- `app/server/__tests__/content.test.ts:2`: `import { DOMAINS, loadContent } from "../menu";` → `from "../content";`
- `app/server/__tests__/content-gen.test.ts:6`: `import { loadContent, parseContentFile } from "../menu";` → `from "../content";`
- `app/server/__tests__/menu.test.ts:5-8` の `from "../menu"` ブロックを3経路に分割:
  ```ts
  import { buildQuickMenu, buildTodayMenu, invalidateTodayMenuCache, QUICK_KINDS, type MenuDeps, type QuickKind } from "../menu";
  import { loadContent, parseContentFile, type ContentItem, type Domain } from "../content";
  import { pickNext, pickNextByDomain, type RotationState, type UsageMap } from "../rotation";
  ```

- [ ] **Step 7: 検証ゲート**

Run: `cd app && bun test` — Expected: PASS（330 テスト。menu/content/content-gen/rotation 系が緑）
Run: `cd app && bun run typecheck` — Expected: エラーなし
Run: `cd app/client && bun run build` — Expected: 成功

- [ ] **Step 8: コミット**

```bash
git add app/server/content.ts app/server/rotation.ts app/server/menu.ts app/server/index.ts app/server/content-gen.ts app/server/routes/menu.ts app/server/__tests__/menu.test.ts app/server/__tests__/content.test.ts app/server/__tests__/content-gen.test.ts
git commit -m "refactor: menu.ts を content.ts/rotation.ts に分解し index.ts の重複検索を集約"
```

---

## Task 2: srs-analytics.ts を新設し assessment ⇄ content-gen の循環 import を解消

**Files:**
- Create: `app/server/srs-analytics.ts`
- Modify: `app/server/assessment.ts:5,10,13-36,53,109`（categoryBadRates/CategoryRate 定義を撤去し import に）
- Modify: `app/server/content-gen.ts:7,18-23,144`（pickWorstCategories 定義を撤去し import に）
- Modify（テスト import 張り替え）: `app/server/__tests__/assessment.test.ts:5`, `app/server/__tests__/content-gen.test.ts:8,11`

**Interfaces:**
- Consumes: `type Sentence`（sentences.ts）
- Produces: `type CategoryRate = { categoryNo: number; category: string; reviewed: number; badRate: number }`、`categoryBadRates(db: Database, sentences: Sentence[]): CategoryRate[]`、`pickWorstCategories(rates: CategoryRate[], minReviewed?: number, top?: number): CategoryRate[]`

**現状の循環（発見事項）:** `assessment.ts:5 import { pickWorstCategories } from "./content-gen"` ⇔ `content-gen.ts:7 import { categoryBadRates, type CategoryRate } from "./assessment"`。関数の遅延束縛で偶然動いている状態。

- [ ] **Step 1: srs-analytics.ts を新規作成**

`app/server/srs-analytics.ts`（categoryBadRates は assessment.ts:13-36、pickWorstCategories は content-gen.ts:18-23 を移動。CategoryRate 型は assessment.ts:10 から移動）:

```ts
import type { Database } from "bun:sqlite";
import type { Sentence } from "./sentences";

export type CategoryRate = { categoryNo: number; category: string; reviewed: number; badRate: number };

/** カテゴリ別の bad率（現在の last_grade スナップショット・reviewed>0 の文のみ）。bad率降順・同率は reviewed 降順 */
export function categoryBadRates(db: Database, sentences: Sentence[]): CategoryRate[] {
  const rows = db
    .query<{ no: number; last_grade: string | null }, []>(
      "SELECT no, last_grade FROM sentence_srs WHERE reviews > 0")
    .all();
  const byNo = new Map(sentences.map((s) => [s.no, s]));
  const agg = new Map<number, { category: string; reviewed: number; bad: number }>();
  for (const r of rows) {
    const s = byNo.get(r.no);
    if (!s) continue;
    const a = agg.get(s.category_no) ?? { category: s.category, reviewed: 0, bad: 0 };
    a.reviewed++;
    if (r.last_grade === "bad") a.bad++;
    agg.set(s.category_no, a);
  }
  return [...agg.entries()]
    .map(([categoryNo, a]) => ({
      categoryNo,
      category: a.category,
      reviewed: a.reviewed,
      badRate: Math.round((a.bad / a.reviewed) * 1000) / 1000,
    }))
    .sort((x, y) => y.badRate - x.badRate || y.reviewed - x.reviewed);
}

/**
 * CLI(sentences): 生成対象カテゴリの選定。評価5文以上・bad率>0 のうち bad率降順（同率は reviewed降順）でワースト3。
 * categoryBadRates() は既にこの順で返すが、入力順序に依存しないようここでも明示的にソートする。
 */
export function pickWorstCategories(rates: CategoryRate[], minReviewed = 5, top = 3): CategoryRate[] {
  return rates
    .filter((r) => r.reviewed >= minReviewed && r.badRate > 0)
    .sort((a, b) => b.badRate - a.badRate || b.reviewed - a.reviewed)
    .slice(0, top);
}
```

- [ ] **Step 2: assessment.ts を srs-analytics に張り替える**

`app/server/assessment.ts`:
- 行 5 `import { pickWorstCategories } from "./content-gen";` を **削除**。
- 行 10 の `export type CategoryRate = ...` を **削除**。
- 行 13-36 の `categoryBadRates` 関数定義を **削除**。
- import 群（行 8 付近）に追加: `import { categoryBadRates, pickWorstCategories, type CategoryRate } from "./srs-analytics";`
- `MonthData.worstCategories: CategoryRate[]`（行 53）と `assembleMonthData` 内の `pickWorstCategories(categoryBadRates(deps.db, deps.sentences))`（行 109）はそのまま（import 済みの記号を使う）。

- [ ] **Step 3: content-gen.ts を srs-analytics に張り替える**

`app/server/content-gen.ts`:
- 行 7 `import { categoryBadRates, type CategoryRate } from "./assessment";` を **削除**。
- 行 18-23 の `pickWorstCategories` 関数定義を **削除**。
- import 群（行 9 付近）に追加: `import { categoryBadRates, pickWorstCategories } from "./srs-analytics";`
  （content-gen.ts は CategoryRate 型を直接参照しなくなるため型は import しない）
- `genSentences` 内 `pickWorstCategories(categoryBadRates(deps.db, sentences))`（行 144）はそのまま。

- [ ] **Step 4: テストの import 経路を張り替える**

- `app/server/__tests__/assessment.test.ts:5`: import から `categoryBadRates` を外し、別行を追加:
  ```ts
  import { generateMonthlyReport, makeAssembleMonthData, makeAssessmentStore } from "../assessment";
  import { categoryBadRates } from "../srs-analytics";
  ```
  （同ファイルの他 import 記号 `SENTENCES` 等は変更不要）
- `app/server/__tests__/content-gen.test.ts:8,11`: `import type { CategoryRate } from "../assessment";` を削除し、行 11 の content-gen import から `pickWorstCategories` を外して srs-analytics へ:
  ```ts
  import { contentToMarkdown, genSentences, genTopics, validateNewSentences, validateTopicCandidate } from "../content-gen";
  import { pickWorstCategories, type CategoryRate } from "../srs-analytics";
  ```
  （content-gen import の残り記号は現状のものを保持。`validateNewSentences` は Task 5 で使うため、既に import されていなければこの行に含めておく）

- [ ] **Step 5: 検証ゲート**

Run: `cd app && bun test` — Expected: PASS
Run: `cd app && bun run typecheck` — Expected: エラーなし（循環 import 解消後も型解決できる）
Run: `cd app/client && bun run build` — Expected: 成功

- [ ] **Step 6: コミット**

```bash
git add app/server/srs-analytics.ts app/server/assessment.ts app/server/content-gen.ts app/server/__tests__/assessment.test.ts app/server/__tests__/content-gen.test.ts
git commit -m "refactor: SRS分析を srs-analytics.ts に切り出し assessment/content-gen の循環importを解消"
```

---

## Task 3: LLM ランナーを converse.ts の単一 defaultRunner に集約

**Files:**
- Modify: `app/server/converse.ts:57,66`（runClaudeTurn → defaultRunner + 規約コメント）
- Modify: `app/server/coach.ts:1,2,13`（local defaultRunner 撤去）
- Modify: `app/server/placement.ts:1,4,49`（local defaultRunner 撤去）
- Modify: `app/server/assessment.ts:1,4,132`（local defaultRunner 撤去）
- Modify: `app/server/__tests__/converse.test.ts:9`（コメントの記号名を更新）

**Interfaces:**
- Produces（converse.ts）: `export const defaultRunner: ClaudeRunner`（唯一の `makeClaudeRunner(query)` 生成点）
- Consumes: coach.ts / placement.ts / assessment.ts が `import { defaultRunner, type ClaudeRunner } from "./converse"`

**発見事項:** `makeClaudeRunner(query)` は converse.ts:57（`runClaudeTurn`）・placement.ts:49・assessment.ts:132・coach.ts:13 の**4箇所**で重複生成されている。`runClaudeTurn` の外部 import は無し（converse.ts 内部の converseTurn フォールバックのみ、テストはコメント参照のみ）。プロンプト本体は既に各ドメインモジュール（coach.ts・placement.ts・assessment.ts・content-gen.ts・converse.ts）に同居しており、「各ドメインモジュールに置く」規約は実態化済み — コード移動は不要で、共有ランナーにその旨をコメントで明文化する。

- [ ] **Step 1: converse.ts に単一 defaultRunner を定義**

`app/server/converse.ts`:
- 行 57 `export const runClaudeTurn: ClaudeRunner = makeClaudeRunner(query);` を次に置換:
  ```ts
  /**
   * 全ドメイン共有の LLM ランナー（唯一の makeClaudeRunner(query) 生成点）。
   * プロンプト配置規約: 各ドメインの system プロンプトはそのドメインモジュール
   * （coach.ts / placement.ts / assessment.ts / content-gen.ts / converse.ts）に置き、
   * ここでは実行器だけを共有する。
   */
  export const defaultRunner: ClaudeRunner = makeClaudeRunner(query);
  ```
- 行 66 `const runner = args.runner ?? runClaudeTurn;` → `const runner = args.runner ?? defaultRunner;`

- [ ] **Step 2: coach.ts の local defaultRunner を撤去**

`app/server/coach.ts`:
- 行 1 `import { query } from "@anthropic-ai/claude-agent-sdk";` を **削除**。
- 行 2 `import { makeClaudeRunner, type ClaudeRunner } from "./converse";` → `import { defaultRunner, type ClaudeRunner } from "./converse";`
- 行 13 `const defaultRunner: ClaudeRunner = makeClaudeRunner(query);` を **削除**。
（各 `generate*` 関数のデフォルト引数 `runner: ClaudeRunner = defaultRunner` は import 済みの記号を参照するのでそのまま）

- [ ] **Step 3: placement.ts の local defaultRunner を撤去**

`app/server/placement.ts`:
- 行 1 `import { query } from "@anthropic-ai/claude-agent-sdk";` を **削除**。
- 行 4 `import { makeClaudeRunner, type ClaudeRunner } from "./converse";` → `import { defaultRunner, type ClaudeRunner } from "./converse";`
- 行 49 `const defaultRunner: ClaudeRunner = makeClaudeRunner(query);` を **削除**。

- [ ] **Step 4: assessment.ts の local defaultRunner を撤去**

`app/server/assessment.ts`:
- 行 1 `import { query } from "@anthropic-ai/claude-agent-sdk";` を **削除**。
- 行 4 `import { makeClaudeRunner, type ClaudeRunner } from "./converse";` → `import { defaultRunner, type ClaudeRunner } from "./converse";`
- 行 132 `const defaultRunner: ClaudeRunner = makeClaudeRunner(query);` を **削除**。

- [ ] **Step 5: converse.test.ts のコメントを更新**

`app/server/__tests__/converse.test.ts:9` のコメント `// Minimal fake message shapes; only the fields runClaudeTurn actually reads are populated.` の `runClaudeTurn` を `defaultRunner` に置換（コメントのみ・機能変更なし）。

- [ ] **Step 6: 検証ゲート**

Run: `cd app && bun test` — Expected: PASS
Run: `cd app && bun run typecheck` — Expected: エラーなし（`makeClaudeRunner` の残参照は converse.ts のみ）
Run: `cd app/client && bun run build` — Expected: 成功

補助確認: `cd app/server && grep -rn "makeClaudeRunner(query)" . --include="*.ts"` が converse.ts の1行だけを返すこと。

- [ ] **Step 7: コミット**

```bash
git add app/server/converse.ts app/server/coach.ts app/server/placement.ts app/server/assessment.ts app/server/__tests__/converse.test.ts
git commit -m "refactor: LLMランナーを converse.ts の単一 defaultRunner に集約"
```

---

## Task 4: ストア規約統一（各ストアの ensureSchema + insertReturningId 共有）

**Files:**
- Create: `app/server/db-util.ts`
- Modify: `app/server/db.ts:23-103`（openDb を合成に縮小 + ensureLibrarySchema/ensureHashCacheSchemas）
- Modify: `app/server/progress-store.ts`（ensureProgressSchema + blockStart で insertReturningId）
- Modify: `app/server/sentences.ts`（ensureSentenceSchema）
- Modify: `app/server/chunks.ts`（ensureChunkSchema）
- Modify: `app/server/placement.ts`（ensurePlacementSchema + save で insertReturningId）
- Modify: `app/server/assessment.ts`（ensureAssessmentSchema + save で insertReturningId）

**Interfaces:**
- Produces（db-util.ts）: `insertReturningId(db: Database): number`
- Produces（各ストア）: `ensureProgressSchema(db)`, `ensureSentenceSchema(db)`, `ensureChunkSchema(db)`, `ensurePlacementSchema(db)`, `ensureAssessmentSchema(db)`, `ensureLibrarySchema(db)`, `ensureHashCacheSchemas(db)`
- Consumes（db.ts openDb）: 上記全 ensure*Schema

**発見事項 / 循環回避:** 現在 `./db` を import しているのは index.ts のみ（各ストアは db.ts 非依存）。openDb が各ストアの ensureSchema を import する形にしても、ストア側が `insertReturningId` を **db-util.ts**（db.ts ではない）から import すれば循環しない。`insertReturningId` を db.ts に置くと `db.ts ⇄ placement/assessment` の循環になるため、必ず db-util.ts に置くこと。`last_insert_rowid()` イディオムは progress-store.ts:210 / placement.ts:109 / assessment.ts:175 の**3箇所**重複。全テーブルは openDb 経由でのみ生成されるため、DDL を ensure*Schema に移して openDb から呼ぶ変更は挙動不変（openDb を使うテストはそのまま緑）。

- [ ] **Step 1: db-util.ts を新規作成**

`app/server/db-util.ts`:

```ts
import type { Database } from "bun:sqlite";

/** 直前の INSERT で採番された rowid を返す。各ストアの save/blockStart が共有する。 */
export function insertReturningId(db: Database): number {
  return db.query<{ id: number }, []>("SELECT last_insert_rowid() AS id").get()!.id;
}
```

- [ ] **Step 2: progress-store.ts に ensureProgressSchema を追加し blockStart を共有ヘルパ化**

`app/server/progress-store.ts`:
- import 追加: `import { insertReturningId } from "./db-util";`
- ファイル冒頭（`makeProgressStore` の前）に、db.ts:41-61 の user_progress / xp_events / level_events / block_attempts DDL を移した関数を追加:
  ```ts
  export function ensureProgressSchema(db: Database): void {
    db.run(`CREATE TABLE IF NOT EXISTS user_progress (
      id INTEGER PRIMARY KEY CHECK (id = 1),
      level INTEGER NOT NULL,
      xp INTEGER NOT NULL,
      xp_into_level INTEGER NOT NULL DEFAULT 0,
      updated_at TEXT NOT NULL
    )`);
    db.run(`CREATE TABLE IF NOT EXISTS xp_events (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      ts TEXT NOT NULL, ymd TEXT NOT NULL, kind TEXT NOT NULL, amount INTEGER NOT NULL, meta TEXT
    )`);
    db.run(`CREATE TABLE IF NOT EXISTS level_events (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      ts TEXT NOT NULL, ymd TEXT NOT NULL, kind TEXT NOT NULL,
      from_level INTEGER NOT NULL, to_level INTEGER NOT NULL, rationale TEXT
    )`);
    db.run(`CREATE TABLE IF NOT EXISTS block_attempts (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      ts TEXT NOT NULL, ymd TEXT NOT NULL, kind TEXT NOT NULL,
      completed INTEGER NOT NULL DEFAULT 0
    )`);
  }
  ```
- `blockStart`（現 208-212）の本体を差し替え:
  ```ts
  blockStart(kind, today = localYmd()) {
    db.run("INSERT INTO block_attempts (ts, ymd, kind, completed) VALUES (?, ?, ?, 0)", [nowTs(), today, kind]);
    return { attemptId: insertReturningId(db) };
  },
  ```

- [ ] **Step 3: sentences.ts に ensureSentenceSchema を追加**

`app/server/sentences.ts`:
- `makeSentenceStore` の前に、db.ts:34-40,67-71 の sentence_srs / sentence_explanations DDL を移した関数を追加:
  ```ts
  export function ensureSentenceSchema(db: Database): void {
    db.run(`CREATE TABLE IF NOT EXISTS sentence_srs (
      no INTEGER PRIMARY KEY,
      stage INTEGER NOT NULL DEFAULT 0,
      due TEXT NOT NULL,
      last_grade TEXT,
      reviews INTEGER NOT NULL DEFAULT 0
    )`);
    db.run(`CREATE TABLE IF NOT EXISTS sentence_explanations (
      no INTEGER PRIMARY KEY,
      text TEXT NOT NULL,
      created TEXT NOT NULL
    )`);
  }
  ```

- [ ] **Step 4: chunks.ts に ensureChunkSchema を追加**

`app/server/chunks.ts`:
- `makeChunkStore` の前に、db.ts:72-84 の collected_chunks DDL を移した関数を追加:
  ```ts
  export function ensureChunkSchema(db: Database): void {
    db.run(`CREATE TABLE IF NOT EXISTS collected_chunks (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      created TEXT NOT NULL,
      source TEXT NOT NULL,
      prompt_text TEXT NOT NULL,
      en TEXT NOT NULL,
      norm_en TEXT NOT NULL UNIQUE,
      note TEXT NOT NULL DEFAULT '',
      stage INTEGER NOT NULL DEFAULT 0,
      due TEXT NOT NULL,
      last_grade TEXT,
      reviews INTEGER NOT NULL DEFAULT 0
    )`);
  }
  ```

- [ ] **Step 5: placement.ts に ensurePlacementSchema を追加し save を共有ヘルパ化**

`app/server/placement.ts`:
- import 追加: `import { insertReturningId } from "./db-util";`
- `makePlacementStore` の前に、db.ts:62-66 の placement_results DDL を移した関数を追加:
  ```ts
  export function ensurePlacementSchema(db: Database): void {
    db.run(`CREATE TABLE IF NOT EXISTS placement_results (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      ts TEXT NOT NULL, stage INTEGER NOT NULL, start_level INTEGER NOT NULL,
      rationale TEXT NOT NULL, metrics TEXT NOT NULL
    )`);
  }
  ```
- `save`（現 103-110）の rowid 取得を差し替え:
  ```ts
  save(r) {
    const ts = new Date().toISOString();
    db.run(
      "INSERT INTO placement_results (ts, stage, start_level, rationale, metrics) VALUES (?, ?, ?, ?, ?)",
      [ts, r.stage, r.startLevel, r.rationale, JSON.stringify(r.metrics)],
    );
    return { id: insertReturningId(db), ts, stage: r.stage, startLevel: r.startLevel, rationale: r.rationale };
  },
  ```

- [ ] **Step 6: assessment.ts に ensureAssessmentSchema を追加し save を共有ヘルパ化**

`app/server/assessment.ts`:
- import 追加: `import { insertReturningId } from "./db-util";`
- `makeAssessmentStore` の前に、db.ts:95-101 の monthly_reports DDL を移した関数を追加:
  ```ts
  export function ensureAssessmentSchema(db: Database): void {
    db.run(`CREATE TABLE IF NOT EXISTS monthly_reports (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      ts TEXT NOT NULL,
      ymd TEXT NOT NULL,
      text TEXT NOT NULL,
      data_json TEXT NOT NULL
    )`);
  }
  ```
- `save`（現 171-177）の rowid 取得を差し替え:
  ```ts
  save(r) {
    const ts = new Date().toISOString();
    db.run("INSERT INTO monthly_reports (ts, ymd, text, data_json) VALUES (?, ?, ?, ?)",
      [ts, r.ymd, r.text, JSON.stringify(r.data)]);
    return { id: insertReturningId(db), ts, ymd: r.ymd, text: r.text };
  },
  ```

- [ ] **Step 7: db.ts の openDb を合成に縮小し library/hashCache スキーマを同居 export**

`app/server/db.ts` の import 群に追加:
```ts
import { ensureProgressSchema } from "./progress-store";
import { ensureSentenceSchema } from "./sentences";
import { ensureChunkSchema } from "./chunks";
import { ensurePlacementSchema } from "./placement";
import { ensureAssessmentSchema } from "./assessment";
```
db.ts 内に library / hashCache 用の ensureSchema を追加（db.ts:27-33,85-94 の DDL を移動）:
```ts
export function ensureLibrarySchema(db: Database): void {
  db.run(`CREATE TABLE IF NOT EXISTS model_talks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    created_at TEXT NOT NULL,
    topic_id TEXT NOT NULL,
    topic_title TEXT NOT NULL,
    text TEXT NOT NULL
  )`);
}

/** hashTextCache 系の2テーブル（訳・解説キャッシュ）をまとめて保証する */
export function ensureHashCacheSchemas(db: Database): void {
  db.run(`CREATE TABLE IF NOT EXISTS talk_explanations (
    hash TEXT PRIMARY KEY,
    text TEXT NOT NULL,
    created TEXT NOT NULL
  )`);
  db.run(`CREATE TABLE IF NOT EXISTS utterance_translations (
    hash TEXT PRIMARY KEY,
    text TEXT NOT NULL,
    created TEXT NOT NULL
  )`);
}
```
`openDb`（現 23-103）を DDL 直書きから合成呼び出しに置換:
```ts
/** DBを開き、各ストアの ensureSchema を合成して呼ぶ（CREATE IF NOT EXISTS のみ。マイグレーション機構はYAGNI） */
export function openDb(dbPath: string = DEFAULT_DB_PATH): Database {
  if (dbPath !== ":memory:") mkdirSync(path.dirname(dbPath), { recursive: true });
  const db = new Database(dbPath, { create: true });
  db.run("PRAGMA journal_mode = WAL");
  ensureLibrarySchema(db);
  ensureHashCacheSchemas(db);
  ensureProgressSchema(db);
  ensureSentenceSchema(db);
  ensureChunkSchema(db);
  ensurePlacementSchema(db);
  ensureAssessmentSchema(db);
  return db;
}
```
（`makeHashTextCache` / `makeTalkExplainCache` / `makeTranslationCache` / `makeLibraryStore` は現状のまま db.ts に残す）

- [ ] **Step 8: 検証ゲート**

Run: `cd app && bun test` — Expected: PASS（db.test.ts の「スキーマ自動作成」も緑。全テーブルは openDb 経由で従来どおり生成される）
Run: `cd app && bun run typecheck` — Expected: エラーなし（新規循環なし）
Run: `cd app/client && bun run build` — Expected: 成功

- [ ] **Step 9: コミット**

```bash
git add app/server/db-util.ts app/server/db.ts app/server/progress-store.ts app/server/sentences.ts app/server/chunks.ts app/server/placement.ts app/server/assessment.ts
git commit -m "refactor: 各ストアに ensureSchema/insertReturningId を持たせ openDb を合成に縮小"
```

---

## Task 5: 小粒リファクタ（bestEffort ヘルパ / dates 移行完了 / 空 ja 拒否）

**Files:**
- Modify: `app/server/routes/http.ts`（bestEffort 追加）
- Modify: `app/server/routes/coach.ts:57-65,95-99`（swallow 2箇所）
- Modify: `app/server/routes/chunks.ts:35-39`（swallow 1箇所）
- Modify: `app/server/routes/sentences.ts:26-32,47-51,66-70`（swallow 3箇所）
- Modify: `app/server/routes/placement.ts:49-53`（swallow 1箇所）
- Modify: `app/server/routes/system.ts:33-41`（swallow 1箇所）
- Modify: `app/server/chunks.ts:2`, `app/server/progress-store.ts:5`, `app/server/sentences.ts:6`（dates 移行）
- Modify: `app/server/__tests__/sentences.test.ts:6`（dates import 張り替え）
- Modify: `app/server/content-gen.ts:44-56`（validateNewSentences の空 ja 拒否）
- Test: `app/server/__tests__/content-gen.test.ts`（空 ja のテスト追加）

**Interfaces:**
- Produces（http.ts）: `bestEffort(label: string, fn: () => void): void`
- 挙動: bestEffort は fn の例外を握りつぶし `console.warn(label, String(err))` するだけ。8箇所の swallow を統一する（返り値を持つ `collectBestEffort` は対象外で現状維持）。

**発見事項:** `continuing:` の swallow は routes 層に**8箇所**（system:metrics / coach:saveModelTalk / coach:cache.save / chunks:xp / sentences:dueChunks / sentences:xp / sentences:saveExplanation / placement:xp）。`routes/chunks.ts:collectBestEffort` は返り値ありの既存ヘルパで、void シグネチャの bestEffort には適合しないため据え置く。dates は sentences.ts が互換 re-export を残し chunks.ts / progress-store.ts が `./sentences` 経由で日付関数を import している2系統併存状態。

- [ ] **Step 1: http.ts に bestEffort を追加**

`app/server/routes/http.ts` の末尾に追加:

```ts
/** ベストエフォート副作用: 失敗しても握りつぶし警告だけ出す（親レスポンスを失敗させないため）。 */
export function bestEffort(label: string, fn: () => void): void {
  try {
    fn();
  } catch (err) {
    console.warn(label, String(err));
  }
}
```

- [ ] **Step 2: routes/system.ts の metrics swallow を bestEffort 化**

`app/server/routes/system.ts`:
- import に `bestEffort` を追加: `import { json, parseJsonBody, exact, bestEffort, type RouteEntry } from "./http";`
- handleStt の metrics try/catch（現 32-41）を差し替え（**失敗時に metrics を undefined に保つため、両ステップ成功後に commit する**）:
  ```ts
  let metrics: UtteranceMetrics | undefined;
  bestEffort("[metrics] compute/record failed, continuing:", () => {
    const m = computeUtteranceMetrics(segments);
    appendEvent(deps.logFile(), {
      ts: new Date().toISOString(), type: "stt_result", sessionId: "stt", text, meta: { metrics: m },
    });
    metrics = m;
  });
  ```

- [ ] **Step 3: routes/coach.ts の swallow 2箇所を bestEffort 化**

`app/server/routes/coach.ts`:
- import に `bestEffort` を追加: `import { json, parseJsonBody, exact, bestEffort, type RouteEntry } from "./http";`
- `handleModelTalk`（現 51-67）を差し替え（topicId を const に束縛して closure でも narrowing を保つ）:
  ```ts
  async function handleModelTalk(req: Request, deps: CoachRoutesDeps): Promise<Response> {
    const parsed = await parseJsonBody<{ topicId?: string }>(req);
    if (!parsed.ok) return parsed.response;
    const topicId = parsed.body.topicId;
    if (!topicId?.trim()) return json({ error: "topicId is required" }, 400);
    const talk = await deps.modelTalk(topicId);
    if (!talk) return json({ error: "unknown topicId" }, 404);
    bestEffort("[library] saveModelTalk failed, continuing:", () =>
      deps.libraryStore.saveModelTalk({ topicId, topicTitle: talk.topicTitle ?? "", text: talk.text }));
    return json({ text: talk.text });
  }
  ```
- `respondHashCached` 内の cache.save try/catch（現 94-99）を差し替え:
  ```ts
  bestEffort(cacheWarnLabel, () => cache.save(hash, generated.text, new Date().toISOString()));
  ```
  （respondHashCached の空訳 502 化は Task 6 で追加する）

- [ ] **Step 4: routes/chunks.ts の xp swallow を bestEffort 化**

`app/server/routes/chunks.ts`:
- import に `bestEffort` を追加: `import { json, parseJsonBody, exact, prefix, bestEffort, type RouteEntry } from "./http";`
- handleChunkGrade の xp try/catch（現 35-39）を差し替え:
  ```ts
  bestEffort("[progress] srs-grade xp (chunk) failed, continuing:", () =>
    deps.progressStore.addXp("srs-grade", xpForGrade(grade as Grade), { chunkId: id }));
  ```
  （`collectBestEffort` は返り値ありの既存ヘルパのため変更しない）

- [ ] **Step 5: routes/sentences.ts の swallow 3箇所を bestEffort 化**

`app/server/routes/sentences.ts`:
- import に `bestEffort` を追加: `import { json, parseJsonBody, exact, bestEffort, type RouteEntry } from "./http";`
- handleSentenceQueue の dueChunks try/catch（現 25-32）を差し替え（フォールバック `[]` を保つため commit-at-end）:
  ```ts
  let chunks: Array<{ kind: "chunk" } & Omit<Chunk, "created" | "source">> = [];
  bestEffort("[chunks] dueChunks failed, continuing with sentences only:", () => {
    chunks = deps.chunkStore.dueChunks().map((c) => ({
      kind: "chunk" as const, id: c.id, promptText: c.promptText, en: c.en, note: c.note, srs: c.srs,
    }));
  });
  ```
- handleSentenceGrade の xp try/catch（現 47-51）を差し替え:
  ```ts
  bestEffort("[progress] srs-grade xp failed, continuing:", () =>
    deps.progressStore.addXp("srs-grade", xpForGrade(grade as Grade), { no }));
  ```
- handleSentenceExplain の saveExplanation try/catch（現 66-70）を差し替え:
  ```ts
  bestEffort("[sentences] explanation cache write failed, continuing:", () =>
    deps.sentenceStore.saveExplanation(no, generated.text));
  ```

- [ ] **Step 6: routes/placement.ts の xp swallow を bestEffort 化**

`app/server/routes/placement.ts`:
- import に `bestEffort` を追加: `import { json, parseJsonBody, exact, bestEffort, type RouteEntry } from "./http";`
- handlePlacementSubmit の xp try/catch（現 49-53）を差し替え:
  ```ts
  bestEffort("[placement] xp grant failed, continuing:", () =>
    deps.progressStore.addXp("placement", PLACEMENT_XP, {}));
  ```

- [ ] **Step 7: dates 移行を完了させる**

- `app/server/chunks.ts:2` を分割:
  ```ts
  import { srsTransition, type Grade, type SrsState } from "./sentences";
  import { addDaysYmd, localYmd } from "./dates";
  ```
- `app/server/progress-store.ts:5`: `import { addDaysYmd, localYmd } from "./sentences";` → `from "./dates";`
- `app/server/sentences.ts:5-6`: 互換 re-export 行 `export { addDaysYmd, localYmd } from "./dates";`（と直上のコメント行）を **削除**。
- `app/server/__tests__/sentences.test.ts:6` を分割:
  ```ts
  import { loadSentences, makeSentenceStore, type Sentence } from "../sentences";
  import { addDaysYmd, localYmd } from "../dates";
  ```

- [ ] **Step 8: validateNewSentences の空 ja 拒否テストを書く（失敗させる）**

`app/server/__tests__/content-gen.test.ts` の `validateNewSentences` を扱う describe（無ければ新設）に追加。冒頭 import に `validateNewSentences`・`type Sentence`（未 import なら）を含める。

```ts
test("空の ja は候補全体を不採用にする（en 空拒否と同じ扱い）", () => {
  const existing = [
    { no: 1, category_no: 1, category: "現在形", domain: "daily", en: "I work here.", ja: "ここで働いています", note: "" },
  ] as Sentence[];
  const cands = [{ domain: "daily", en: "I test this daily.", ja: "   ", note: "現在形" }];
  expect(validateNewSentences(cands, existing, 1, "現在形")).toBeNull();
});
```

- [ ] **Step 9: テストを実行して失敗を確認**

Run: `cd app && bun test content-gen.test.ts` — Expected: FAIL（現状は ja を trim して空のまま採用するため null にならない）

- [ ] **Step 10: validateNewSentences に空 ja 拒否を実装**

`app/server/content-gen.ts` の validateNewSentences ループ内（現 44-49 付近）を差し替え:
```ts
    const c = raw as NewSentenceCandidate;
    if (typeof c?.en !== "string" || typeof c?.ja !== "string" || typeof c?.note !== "string") return null;
    if (!(DOMAINS as readonly string[]).includes(c.domain)) return null;
    const en = c.en.trim();
    if (!en || en.length > 200) return null;
    const ja = c.ja.trim();
    if (!ja) return null;
    const norm = normalizeEn(en);
    if (!norm || norms.has(norm)) return null;
    norms.add(norm);
    no++;
    out.push({
      no, category_no: categoryNo, category,
      domain: c.domain as Sentence["domain"],
      en, ja, note: c.note.trim(),
    });
```

- [ ] **Step 11: 検証ゲート**

Run: `cd app && bun test` — Expected: PASS（空 ja テストが緑。swallow/dates 変更で回帰なし）
Run: `cd app && bun run typecheck` — Expected: エラーなし
Run: `cd app/client && bun run build` — Expected: 成功

補助確認: `cd app/server && grep -rn "continuing:" routes` が `collectBestEffort` の1件のみを残すこと（他8箇所は bestEffort 経由）。

- [ ] **Step 12: コミット**

```bash
git add app/server/routes/http.ts app/server/routes/coach.ts app/server/routes/chunks.ts app/server/routes/sentences.ts app/server/routes/placement.ts app/server/routes/system.ts app/server/chunks.ts app/server/progress-store.ts app/server/sentences.ts app/server/__tests__/sentences.test.ts app/server/content-gen.ts app/server/__tests__/content-gen.test.ts
git commit -m "refactor: swallow を bestEffort に統一し dates 移行を完了・空ja例文を拒否"
```

---

## Task 6: P1 最終レビュー繰越（F — coach 系の挙動改善、テストファースト）

**Files:**
- Modify: `app/server/routes/coach.ts`（respondHashCached の空訳 502 / handlePhraseHint の history 文字数キャップ）
- Modify: `app/server/coach.ts:110-130`（generatePhraseHints の ja フィルタ緩和）
- Test: `app/server/__tests__/routes-coach.test.ts`（空訳 502 ×2 / history キャップ）
- Test: `app/server/__tests__/coach.test.ts`（ja 緩和 / generateUtteranceTranslation 単体）
- Modify: `app/client/src/screens/FreeTalkScreen.tsx:141`（再試行ボタン disabled）

**Interfaces:**
- respondHashCached: 生成結果が空文字（trim 後 0 長）なら **キャッシュせず 502**。translate / talk-explain 両方に効く。
- generatePhraseHints: `en` が有効なら `ja` 欠落・非文字列でも `ja: ""` で採用する。
- handlePhraseHint: history 各 `text` を先頭 500 字に切り詰める。

- [ ] **Step 1: 空訳 502 のテストを書く（失敗させる）**

`app/server/__tests__/routes-coach.test.ts` の talk-explain describe（現 97-150）に追加:
```ts
  test("POST /api/coach/talk-explain は空生成を 502 にしキャッシュしない", async () => {
    let saved = 0;
    const { deps } = makeTestDeps({
      explainTalk: async () => ({ text: "   " }),
      talkExplainCache: makeFakeTalkExplainCache({ get: () => null, save: () => { saved++; } }),
    });
    const res = await makeFetchHandler(deps)(new Request("http://x/api/coach/talk-explain", {
      method: "POST", headers: { "content-type": "application/json" },
      body: JSON.stringify({ text: "Some talk text." }),
    }));
    expect(res.status).toBe(502);
    expect(saved).toBe(0);
  });
```
translate describe（現 152-203）に追加:
```ts
  test("POST /api/coach/translate は空生成を 502 にしキャッシュしない", async () => {
    let saved = 0;
    const { deps } = makeTestDeps({
      translate: async () => ({ text: "" }),
      translationCache: makeFakeTalkExplainCache({ get: () => null, save: () => { saved++; } }),
    });
    const res = await makeFetchHandler(deps)(new Request("http://x/api/coach/translate", {
      method: "POST", headers: { "content-type": "application/json" },
      body: JSON.stringify({ text: "Any line." }),
    }));
    expect(res.status).toBe(502);
    expect(saved).toBe(0);
  });
```

- [ ] **Step 2: history キャップのテストを書く（失敗させる）**

`app/server/__tests__/routes-coach.test.ts` の phrase-hint describe（現 205-276）に追加:
```ts
  test("POST /api/coach/phrase-hint は history の各 text を500字に切り詰める", async () => {
    let receivedLen = -1;
    const { deps } = makeTestDeps({
      phraseHint: async (args) => { receivedLen = args.history?.[0]?.text.length ?? -1; return { suggestions: [{ en: "ok", ja: "" }] }; },
    });
    const res = await makeFetchHandler(deps)(new Request("http://x/api/coach/phrase-hint", {
      method: "POST", headers: { "content-type": "application/json" },
      body: JSON.stringify({ jaText: "はい", history: [{ role: "ai", text: "a".repeat(1200) }] }),
    }));
    expect(res.status).toBe(200);
    expect(receivedLen).toBe(500);
  });
```

- [ ] **Step 3: ja 緩和 と generateUtteranceTranslation 単体テストを書く（失敗させる）**

`app/server/__tests__/coach.test.ts`:
- 行 3 の import に `generateUtteranceTranslation` を追加。
- generatePhraseHints describe（現 157-208）に追加:
  ```ts
  test("ja 欠落・非文字列でも en が有効なら ja:'' で採用する", async () => {
    const malformed = { suggestions: [
      { en: "Give me a second." },       // ja missing
      { en: "Let me think.", ja: 123 },   // ja not a string
    ] };
    const { runner } = runnerReturning(JSON.stringify(malformed));
    const result = await generatePhraseHints({ jaText: "はい" }, runner);
    expect(result.suggestions).toEqual([
      { en: "Give me a second.", ja: "" },
      { en: "Let me think.", ja: "" },
    ]);
  });
  ```
- 新規 describe を追加:
  ```ts
  describe("generateUtteranceTranslation", () => {
    test("本文がプロンプトに入り、翻訳用 systemPrompt でトリムした訳を返す", async () => {
      const { runner, seen } = runnerReturning("  私はたいていコーヒーで一日を始めます。  ");
      const result = await generateUtteranceTranslation({ text: "I usually start my day with coffee." }, runner);
      expect(result.text).toBe("私はたいていコーヒーで一日を始めます。");
      expect(seen[0].prompt).toBe("I usually start my day with coffee.");
      expect(seen[0].systemPrompt).toContain("translate");
    });
  });
  ```

- [ ] **Step 4: 全 F テストを実行して失敗を確認**

Run: `cd app && bun test routes-coach.test.ts coach.test.ts` — Expected: FAIL
- talk-explain/translate: 現状は空生成でも 200 で返す → 502 期待に不一致
- history キャップ: 現状は 1200 字のまま渡す → 500 期待に不一致
- ja 緩和: 現状は ja 欠落を除外する → 2件期待に不一致
- generateUtteranceTranslation: import 解決はできるが describe 未存在

- [ ] **Step 5: respondHashCached を空訳 502 に修正**

`app/server/routes/coach.ts` の `respondHashCached`（Task 5 で bestEffort 化済み）を最終形に:
```ts
async function respondHashCached(
  req: Request,
  cache: TalkExplainCache,
  generate: (text: string) => Promise<{ text: string }>,
  cacheWarnLabel: string,
): Promise<Response> {
  const parsed = await parseJsonBody<{ text?: unknown }>(req);
  if (!parsed.ok) return parsed.response;
  const { text } = parsed.body;
  if (typeof text !== "string" || text.trim().length === 0) return json({ error: "text must be a non-empty string" }, 400);
  if (text.length > 3000) return json({ error: "text too long" }, 400);
  const hash = createHash("sha256").update(text).digest("hex");
  const cached = cache.get(hash);
  if (cached !== null) return json({ text: cached });
  const generated = await generate(text);
  // LLM が空文字を返した場合はキャッシュせず 502（空訳を永久キャッシュしない・再試行で回復可能に）
  if (generated.text.trim().length === 0) {
    return json({ error: "generation returned empty — please try again" }, 502);
  }
  bestEffort(cacheWarnLabel, () => cache.save(hash, generated.text, new Date().toISOString()));
  return json({ text: generated.text });
}
```

- [ ] **Step 6: handlePhraseHint に history 文字数キャップを追加**

`app/server/routes/coach.ts` の handlePhraseHint の safeHistory 構築（現 110-115）に `.map` を追加:
```ts
  const safeHistory = Array.isArray(history)
    ? history
        .filter((h): h is { role: "you" | "ai"; text: string } =>
          !!h && typeof h === "object" && (h.role === "you" || h.role === "ai") && typeof h.text === "string")
        .slice(-6)
        .map((h) => ({ role: h.role, text: h.text.slice(0, 500) }))
    : undefined;
```

- [ ] **Step 7: generatePhraseHints の ja フィルタを緩和**

`app/server/coach.ts` の generatePhraseHints（現 121-127）の suggestions 整形を差し替え:
```ts
  const parsed = extractJson<{ suggestions: PhraseHint[] }>(text);
  if (parsed && Array.isArray(parsed.suggestions)) {
    const suggestions = parsed.suggestions
      .filter((s) => typeof s?.en === "string" && s.en)
      .map((s) => ({ en: s.en, ja: typeof s.ja === "string" ? s.ja : "" }));
    if (suggestions.length > 0) return { suggestions };
  }
```

- [ ] **Step 8: F テストを実行して緑を確認**

Run: `cd app && bun test routes-coach.test.ts coach.test.ts` — Expected: PASS（新規4テスト＋既存が緑。既存の「不正な suggestion 項目をサニタイズ」テストは en 欠落・空 en を除外する条件のままなので影響なし）

- [ ] **Step 9: クライアントのヒント再試行ボタンを disabled 化（F5）**

`app/client/src/screens/FreeTalkScreen.tsx:141` の再試行ボタンに `disabled` を追加（先頭の主ヒントボタン 134 行が既に持つ `!hintInput.trim()` ガードに揃える）:
```tsx
            <Button variant="ghost" onClick={requestHints} disabled={!hintInput.trim()}>再試行</Button>
```
（クライアントには単体テスト基盤が無いため、検証は client build のコンパイル成功と、既存主ボタンと同一ガードである点で担保する）

- [ ] **Step 10: 検証ゲート**

Run: `cd app && bun test` — Expected: PASS（全 334 テスト = 330 + F の4テスト。Task 5 の空 ja テストを含めれば更に増）
Run: `cd app && bun run typecheck` — Expected: エラーなし
Run: `cd app/client && bun run build` — Expected: 成功（FreeTalkScreen が型・ビルドを通る）

- [ ] **Step 11: コミット**

```bash
git add app/server/routes/coach.ts app/server/coach.ts app/server/__tests__/routes-coach.test.ts app/server/__tests__/coach.test.ts app/client/src/screens/FreeTalkScreen.tsx
git commit -m "fix: 空訳を502化しphrase-hintのjaフィルタ緩和・history上限・再試行disabledを追加"
```

---

## Self-Review

**1. Spec coverage（spec §5 R3 の各項目 → タスク対応）:**
- content.ts / rotation.ts 抽出・menu.ts 縮小・index.ts 重複クロージャ解消 → **Task 1** ✓
- srs-analytics.ts で循環 import 解消 → **Task 2** ✓
- defaultRunner 1箇所共有・プロンプト配置規約の実態化 → **Task 3** ✓
- 各ストア ensureSchema・openDb 合成化・insertReturningId 共有・db.ts 内2ストア同規約 → **Task 4** ✓
- bestEffort で swallow 8箇所統一 → **Task 5**（collectBestEffort は返り値ありのため据置と明記） ✓
- dates 移行完了（sentences.ts re-export 削除） → **Task 5** ✓
- validateNewSentences 空 ja 拒否 → **Task 5** ✓
- F（空訳502・ja緩和・historyキャップ・translate単体・クライアント再試行disabled） → **Task 6** ✓
- R4/R5 は本計画のスコープ外（spec §6 の実施順どおり別計画）。

**2. Placeholder scan:** 「TBD」「適切に処理」「Task N と同様」等は不使用。機械的移動は移動元行番号＋移動先を明示、新規コード（content.ts/rotation.ts/srs-analytics.ts/db-util.ts の公開 API・ensureSchema 群・bestEffort・validateNewSentences 差分・F の実装とテスト）は完全なコードを記載済み。

**3. Type consistency:**
- content.ts の `Domain`/`DOMAINS`/`ContentItem`/`loadContent`/`parseContentFile`/`findTopic`/`findScenario`、rotation.ts の `pickNext`/`pickNextByDomain`/`RotationState`/`UsageMap` は Task 1 の Produces と menu.ts・test の import で一致。
- srs-analytics.ts の `categoryBadRates`/`pickWorstCategories`/`CategoryRate` は Task 2 の import 先（assessment.ts / content-gen.ts / 各 test）と一致。
- converse.ts の `defaultRunner` は coach/placement/assessment の import 名と一致。
- db-util.ts `insertReturningId(db): number`（`.id` まで返す）は progress-store/placement/assessment の呼び出し（`insertReturningId(db)` を id として使用）と一致。
- ensure*Schema 群（ensureLibrarySchema / ensureHashCacheSchemas / ensureProgressSchema / ensureSentenceSchema / ensureChunkSchema / ensurePlacementSchema / ensureAssessmentSchema）は openDb の呼び出しと定義で一致。
- bestEffort `(label, fn) => void` は全 8 呼び出しの形（フォールバック値は closure 外の let で保持）と一致。

**4. 注意点（実装時の落とし穴）:**
- Task 4 の `insertReturningId` は **db-util.ts に置くこと必須**（db.ts に置くと db⇄placement/assessment の循環）。
- Task 5 の system.ts / sentences.ts のフォールバック付き swallow は「両ステップ成功後に commit」の形で書き、失敗時に既定値（undefined / []）を保つこと（挙動不変の要）。
- Task 6 の respondHashCached は Task 5 の bestEffort 化を前提に最終形へ差し替える（タスク順に実行すること）。
