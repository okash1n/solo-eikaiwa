# R1+R2 リファクタリング（サーバHTTP層の解体 + クライアント基盤の共通化）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** routes.ts の神境界（576行・26ハンドラ・37ルート・RouteDeps 30フィールド）を機能別ルータモジュール＋テーブルディスパッチに解体し、クライアントの非同期ロードイディオム（9ファイル11箇所の手書きコピー）を `useLoad()` に一本化する。純リファクタで、唯一の意図的挙動変更は ReflectionScreen の StrictMode 二重POST解消のみ。

**Architecture:**
- サーバ: 各ドメインが `makeXRoutes(deps: XDeps)`（狭い deps 型）で `RouteEntry[]`（`{ method, match, handler }`）を返し、`routes.ts` は全モジュールの配列を合成して先頭一致でディスパッチするだけに縮小する。XP換算・メニューキャッシュ無効化判定などのドメインポリシーは HTTP 層から `progression.ts` / `progress-store.ts`（`levelChanged` 戻り値）へ移す。
- クライアント: `useLoad<T>(fn)` フック（loading/ready/error + StrictMode once ガード + alive ガード + エラー整形を内蔵）へ単一値ロード画面を移行し、StartSelection を MenuSource 透過に一本化、`localYmd` を共有 `dates.ts` に集約する。

**Tech Stack:** Bun（`bun test` / `bun:sqlite` / `Bun.serve`）、TypeScript（`tsc --noEmit`）、React + Vite。テストは `bun:test`。

## Global Constraints

- **HTTP契約は完全不変**: パス・メソッド・リクエスト/レスポンス形状・ステータスコードを一切変えない。レスポンス JSON はキー・型・順序まで現状維持。
- **クライアント挙動不変**: 例外は ReflectionScreen の二重POST解消（Task 4）のみ。他画面の描画・エラー表示・再試行・再生・購読挙動は同一。
- **テストカバレッジ維持**: サーバの全テスト（`cd app && bun test` が緑。監査時点で約307ケース）を維持する。テストは分割・移設してよいが、削除・弱体化しない。
- **検証ゲート（各タスク末尾で全て緑にしてからコミット）**:
  - `cd app && bun test`（全緑）
  - `cd app && bun run typecheck`（`tsc --noEmit`、0エラー）
  - `cd app/client && bun run build`（`tsc --noEmit && vite build`、成功）
- **コミット規約**: Conventional Commits（`refactor:` / `test:` など、サマリは日本語可。既存ログに準拠）。1タスク1コミットを基本とし、focused に保つ。
- **既存の数値・ポリシーの一元化先を尊重**: レベル/XP/難易度の数値は `app/server/progression.ts` に一元定義する（他ファイルに複製しない）という既存規約に従う。

---

## File Structure（このリファクタで作る/変える境界）

**サーバ（Task 1〜3）**

- 新規 `app/server/routes/http.ts` — 共有 HTTP ヘルパ（`json` / `parseJsonBody`）＋ ルートテーブル型 `RouteEntry` と生成子 `exact` / `prefix`。
- 新規 `app/server/routes/{system,converse,session,menu,settings,library,coach,sentences,chunks,progress,placement,metrics,assessment}.ts` — 各ドメインの `makeXRoutes(deps: XDeps): RouteEntry[]` とハンドラ。狭い `XRoutesDeps` 型を各自 export。
- 変更 `app/server/routes.ts` — 全 `makeXRoutes` を合成して `RouteEntry[]` をループ・ディスパッチする `makeFetchHandler` と、全 deps 型の交差である `RouteDeps` の再 export だけに縮小。`index.ts` の import（`import { makeFetchHandler, type RouteDeps } from "./routes"`）は不変。
- 変更 `app/server/progression.ts` — `xpForGrade` と `PLACEMENT_XP` を追加（ドメインポリシーの受け皿）。
- 変更 `app/server/progress-store.ts` — `levelAction` / `placementSet` の戻り値を `{ summary; levelChanged } | null` にし、無効化判断をストアへ集約。
- 新規 `app/server/__tests__/helpers/route-deps.ts` — `makeTestDeps` ＋ `FAKE_*` 定数 ＋ ストア別フェイクファクトリ（`satisfies` で型完全）。
- 新規 `app/server/__tests__/routes-*.test.ts` — `routes.test.ts` を describe 境界でドメイン別に分割。

**クライアント（Task 4〜6）**

- 新規 `app/client/src/useLoad.ts` — `useLoad<T>(fn)` フックと `LoadState<T>` 型。
- 新規 `app/client/src/dates.ts` — 共有 `localYmd`。
- 変更 `app/client/src/screens/{ReflectionScreen,LibraryScreen,WarmupReadingScreen,ProgressScreen,SentencesScreen}.tsx` — `useLoad` へ移行。
- 変更 `app/client/src/screens/StartScreen.tsx`・`app/client/src/App.tsx` — StartSelection の MenuSource 透過化、`localYmd` 共有化。

**スコープ外（このリファクタでは触れない）**: R3〜R5（menu.ts/content.ts 抽出、srs-analytics.ts、api.ts 分割、i18n 再構成、SentencesScreen のファイル分割、テスト Request ヘルパ等）。ShadowingScreen / PlacementScreen / StartScreen の多重ロード / App の PracticeStat / ProgressScreen の MonthlyReview は `useLoad` 移行の対象外（後述 Task 4 の注記参照）。

---

## Task 1: サーバのドメインポリシーを HTTP 層から移設する

XP換算とメニューキャッシュ無効化判定を HTTP ハンドラから `progression.ts` / `progress-store.ts` に移す。これにより Task 2 で移動するハンドラが「ポリシーを持たない薄い入出力層」になる。

**Files:**
- Modify: `app/server/progression.ts`（末尾に追加）
- Modify: `app/server/progress-store.ts:7-25`（型）, `:28`（XP_CAPS）, `:158-238`（setLevelTo / levelAction / placementSet）
- Modify: `app/server/routes.ts:410-446`（grade 2ハンドラ）, `:303-324`（handleProgressLevel）, `:358-363`（placement XP）, `:367-389`（handlePlacementConfirm）
- Test: `app/server/__tests__/progress-store.test.ts`（結果参照箇所）, `app/server/__tests__/routes.test.ts`（progress/placement のフェイク）

**Interfaces:**
- Produces: `xpForGrade(grade: "good" | "soso" | "bad"): number`、`PLACEMENT_XP: number`（`app/server/progression.ts`）。`LevelChangeResult = { summary: ProgressSummary; levelChanged: boolean }`、`ProgressStore.levelAction(...) : LevelChangeResult | null`、`ProgressStore.placementSet(...) : LevelChangeResult | null`（`app/server/progress-store.ts`）。
- Consumes: 既存 `ProgressSummary`, `Grade`(値は `"good"|"soso"|"bad"`)。

- [ ] **Step 1: progression.ts にポリシー関数を追加**

`app/server/progression.ts` の末尾に追記する（`Grade` を import せず、値ユニオンを直接受ける — progression をリーフのまま保つ）:

```ts
/** 自己評価1枚ごとの努力XP（スペック§4.1: good=2 / soso・bad=1）。routes 側の分散リテラルを一元化 */
export function xpForGrade(grade: "good" | "soso" | "bad"): number {
  return grade === "good" ? 2 : 1;
}

/** プレースメント測定完了の固定XP（スペック§4.1）。progress-store の XP_CAPS.placement と routes の二重定義を一元化 */
export const PLACEMENT_XP = 10;
```

- [ ] **Step 2: progress-store の型に levelChanged を導入**

`app/server/progress-store.ts` の import に `PLACEMENT_XP` を追加し、`ProgressStore` 型と戻り値型を変更する。

import 行（現 `:2-5`）を次に変更:

```ts
import {
  BOUNDARY_LEVELS, DEFAULT_LEVEL, demotionTargetLevel, needXp, stageOf, PLACEMENT_XP,
} from "./progression";
```

`ProgressSummary` 型定義（`:11-14`）の直後に追加し、`ProgressStore` 型（`:15-25`）の `levelAction` / `placementSet` を差し替える:

```ts
/** レベル変更系の戻り値。summary は従来どおり、levelChanged で「当日メニューキャッシュを無効化すべきか」を伝える */
export type LevelChangeResult = { summary: ProgressSummary; levelChanged: boolean };
```

```ts
  /** accept/decline は提案が無ければ null。set は不正レベルで null。levelChanged=実際にレベルが動いたか */
  levelAction(action: "accept" | "decline" | "set", level?: number, today?: string): LevelChangeResult | null;
  /** プレースメント確定によるレベル設定（level_events kind: placement-set）。同一レベルは no-op（levelChanged=false） */
  placementSet(level: number, today?: string): LevelChangeResult | null;
```

`XP_CAPS`（`:28`）を差し替え、placement のリテラルを一元化:

```ts
const XP_CAPS: Record<XpKind, number> = { block: 60, "srs-grade": 2, placement: PLACEMENT_XP };
```

- [ ] **Step 3: progress-store の実装を levelChanged に対応**

`setLevelTo`（`:159-169`）を差し替え:

```ts
  function setLevelTo(level: number | undefined, eventKind: "manual-set" | "placement-set", today: string): LevelChangeResult | null {
    const row = ensureRow();
    if (level === undefined || !Number.isInteger(level) || level < 1 || level > 999) return null;
    // 同一レベルへの set は no-op（xp_into_level を維持し、level_events も記録しない）
    if (level === row.level) return { summary: summarize(row, today), levelChanged: false };
    recordLevelEvent(eventKind, row.level, level, null, today);
    row.level = level;
    row.xp_into_level = 0;
    save(row);
    return { summary: summarize(row, today), levelChanged: true };
  }
```

`levelAction`（`:212-234`）を差し替え:

```ts
    levelAction(action, level, today = localYmd()) {
      if (action === "set") return setLevelTo(level, "manual-set", today);
      const row = ensureRow();
      const proposal = computeProposal(row, today);
      if (!proposal) return null;
      if (action === "decline") {
        recordLevelEvent(proposal.kind === "up" ? "decline-up" : "decline-down", row.level, proposal.toLevel, proposal.rationale, today);
        return { summary: summarize(row, today), levelChanged: false };
      }
      // accept
      const fromLevel = row.level; // 変異前に捕捉（up のカスケード / down のレベル上書きで壊れないように）
      if (proposal.kind === "up") {
        row.xp_into_level -= needXp(row.level);
        row.level += 1;
        autoLevelUp(row);
      } else {
        row.level = proposal.toLevel;
        row.xp_into_level = 0;
      }
      recordLevelEvent(proposal.kind === "up" ? "accept-up" : "accept-down", fromLevel, row.level, proposal.rationale, today);
      save(row);
      return { summary: summarize(row, today), levelChanged: true };
    },
```

`placementSet`（`:236-238`）は本体不変・戻り値型が変わるのみ（`setLevelTo` を返すため自動追従）。変更不要。

- [ ] **Step 4: routes.ts の grade / placement XP を xpForGrade / PLACEMENT_XP に置換**

`app/server/routes.ts` の import に追加:

```ts
import { xpForGrade, PLACEMENT_XP } from "./progression";
```

`handleSentenceGrade` の XP 付与（`:422`）を差し替え:

```ts
    deps.progressStore.addXp("srs-grade", xpForGrade(grade as Grade), { no });
```

`handleChunkGrade` の XP 付与（`:441`）を差し替え:

```ts
    deps.progressStore.addXp("srs-grade", xpForGrade(grade as Grade), { chunkId: id });
```

`handlePlacementSubmit` の XP 付与（`:360`）を差し替え:

```ts
    deps.progressStore.addXp("placement", PLACEMENT_XP, {});
```

- [ ] **Step 5: routes.ts の無効化判定を levelChanged ベースに置換**

`handleProgressLevel`（`:305-324`）の後半（`:313-323`、`previousLevel` 捕捉〜`return json(s)`）を差し替え:

```ts
  const r = deps.progressStore.levelAction(action as "accept" | "decline" | "set", level as number | undefined);
  if (!r) {
    return json({ error: action === "set" ? "level must be an integer between 1 and 999" : "no active proposal" }, 400);
  }
  // 明示的なレベル変更（accept/set）で実際にレベルが動いたときだけ当日メニューを再構築する。
  // decline や同一レベルへの set は levelChanged=false なので無効化しない。
  if (r.levelChanged) deps.invalidateMenuCache();
  return json(r.summary);
```

（`const previousLevel = ...` 行と `const setWasNoop = ...` 行は削除される。`const s = deps.progressStore.levelAction(...)` を上記 `const r = ...` が置き換える。）

`handlePlacementConfirm` の accept 経路末尾（`:383-388`、`const previous = ...` 〜 `return json(s)`）を差し替え:

```ts
  const r = deps.progressStore.placementSet(target);
  if (!r) return json({ error: "level must be an integer between 1 and 999" }, 400);
  // レベルが実際に変わったときだけ当日メニューを再構築する（manual-set と同じ規則）
  if (r.levelChanged) deps.invalidateMenuCache();
  return json(r.summary);
```

- [ ] **Step 6: progress-store.test.ts の結果参照を .summary へ更新し levelChanged を検証**

`app/server/__tests__/progress-store.test.ts` の以下の箇所を修正する（`levelAction`/`placementSet` の非 null 結果は `LevelChangeResult` になったため、summary フィールド参照を `.summary.` に変える。結果を使わない `store.levelAction("set", N, T);` 行は変更不要）:

- `:140-141`: `expect(s.level).toBe(22);` → `expect(s.summary.level).toBe(22);` / `expect(s.xpIntoLevel).toBe(0);` → `expect(s.summary.xpIntoLevel).toBe(0);`
- `:148-149`: `expect(s.level).toBe(22);` → `expect(s.summary.level).toBe(22);`
- `:199-202`: `s.level` → `s.summary.level`、`s.xp` → `s.summary.xp`、`s.xpIntoLevel` → `s.summary.xpIntoLevel`
- `:208-209`: `expect(s.level).toBe(20);` → `expect(s.summary.level).toBe(20);`
- `:230-232`: `s.level` → `s.summary.level`、`s.xpIntoLevel` → `s.summary.xpIntoLevel`
- `:250-256`（同一レベル set の no-op テスト）: `s.level` → `s.summary.level`、`s.xpIntoLevel` → `s.summary.xpIntoLevel`。さらに `const s = store.levelAction("set", 13, T)!;` の直後に `expect(s.levelChanged).toBe(false);` を追加。
- `:264-266`（placementSet レベル変更テスト）: `s!.level` → `s!.summary.level`、`s!.xpIntoLevel` → `s!.summary.xpIntoLevel`。`const s = store.placementSet(23, "2026-07-06");` の直後に `expect(s!.levelChanged).toBe(true);` を追加。
- `:276-277`（placementSet 同一レベル no-op テスト）: `s!.xpIntoLevel` → `s!.summary.xpIntoLevel`。`const s = store.placementSet(13, "2026-07-06");` の直後に `expect(s!.levelChanged).toBe(false);` を追加。

- [ ] **Step 7: routes.test.ts の progress/placement フェイクを levelChanged 対応に更新**

`app/server/__tests__/routes.test.ts` のフェイク progressStore が返す `FAKE_SUMMARY` を `{ summary: FAKE_SUMMARY, levelChanged: ... }` へ変える。無効化判定がストア戻り値ベースになったため、テスト側フェイクが levelChanged を供給する。

`makeTestDeps` 既定の progressStore（`:98-100`）を差し替え:

```ts
      levelAction: (action: string, level?: number) =>
        action === "set" && Number.isInteger(level) && (level as number) >= 1
          ? { summary: FAKE_SUMMARY, levelChanged: true } : null,
      placementSet: (_level: number) => ({ summary: FAKE_SUMMARY, levelChanged: true }),
```

「accept/set 成功時のみ...decline...呼ばない」テストのフェイク（`:1002-1003`）を差し替え（action で levelChanged を出し分け: decline のみ false）:

```ts
        levelAction: (action: string) => ({ summary: FAKE_SUMMARY, levelChanged: action !== "decline" }),
        placementSet: () => ({ summary: FAKE_SUMMARY, levelChanged: true }),
```

「同一レベルへの set は無効化しない」テストのフェイク（`:1031-1032`）を差し替え（現在レベル13との差で levelChanged を出す — 実ストアの no-op 検知を模す）:

```ts
        levelAction: (_action: string, level?: number) => ({ summary: FAKE_SUMMARY, levelChanged: level !== 13 }),
        placementSet: () => ({ summary: FAKE_SUMMARY, levelChanged: true }),
```

「sentences/grade は srs-grade XP付与」テストのフェイク（`:1053-1054`）を差し替え:

```ts
        blockStart: () => ({ attemptId: 1 }), levelAction: () => null,
        placementSet: () => ({ summary: FAKE_SUMMARY, levelChanged: false }),
```

placement confirm 3テストの事後代入 `placementSet`（`:1139-1140`, `:1159-1160`, `:1172-1173`）を、いずれも `{ summary: FAKE_SUMMARY, levelChanged: true }` を返す形に差し替え。例（`:1139-1140`）:

```ts
    (deps.progressStore as { placementSet: (l: number) => unknown }).placementSet =
      (l: number) => { placementSetCalls.push(l); return { summary: FAKE_SUMMARY, levelChanged: true }; };
```

（3箇所とも同形。`placementSetCalls.push(l)` の変数名は各テストの既存名に合わせる。）

- [ ] **Step 8: 検証ゲート**

```bash
cd app && bun test && bun run typecheck
cd app/client && bun run build
```
Expected: `bun test` 全緑 / `typecheck` 0エラー / `build` 成功。

- [ ] **Step 9: コミット**

```bash
cd <repo-root>
git add app/server/progression.ts app/server/progress-store.ts app/server/routes.ts app/server/__tests__/progress-store.test.ts app/server/__tests__/routes.test.ts
git commit -m "refactor: XP換算とメニュー無効化判定をHTTP層からドメインへ移設"
```

---

## Task 2: routes.ts を機能別ルータモジュール＋テーブルディスパッチに分割

`routes.ts` の 26 ハンドラ・37 ルートを、狭い deps を持つドメイン別 `makeXRoutes` に切り出す。`routes.ts` は合成とディスパッチだけに縮小する。ハンドラ本体は原則そのまま移動し、`collectBestEffort` のシグネチャだけ狭める。

**Files:**
- Create: `app/server/routes/http.ts`
- Create: `app/server/routes/system.ts` / `converse.ts` / `session.ts` / `menu.ts` / `settings.ts` / `library.ts` / `coach.ts` / `sentences.ts` / `chunks.ts` / `progress.ts` / `placement.ts` / `metrics.ts` / `assessment.ts`
- Modify: `app/server/routes.ts`（合成＋ RouteDeps 再 export に縮小）
- 影響なし（import 不変）: `app/server/index.ts`

**Interfaces:**
- Produces: `RouteEntry`、`json`、`parseJsonBody`、`exact`、`prefix`（`routes/http.ts`）。各モジュールの `makeXRoutes(deps: XRoutesDeps): RouteEntry[]` と `XRoutesDeps` 型。`routes.ts` は `makeFetchHandler(deps: RouteDeps)` と `RouteDeps`（全 deps 型の交差）を維持。
- Consumes: Task 1 の `xpForGrade` / `PLACEMENT_XP`、`ProgressStore.levelAction`/`placementSet` の `LevelChangeResult`。

- [ ] **Step 1: 共有 HTTP 基盤 routes/http.ts を作る（新規コード）**

`app/server/routes/http.ts`:

```ts
/** ルータモジュール共通の HTTP ヘルパとテーブル型。旧 routes.ts の json/parseJsonBody をここへ集約する。 */

export function json(data: unknown, status = 200): Response {
  return new Response(JSON.stringify(data), { status, headers: { "content-type": "application/json" } });
}

export type ParsedBody<T> = { ok: true; body: T } | { ok: false; response: Response };

/** req.json() の失敗（不正なJSON）を 500 ではなく 400 として扱うための共通ラッパー */
export async function parseJsonBody<T>(req: Request): Promise<ParsedBody<T>> {
  try {
    return { ok: true, body: (await req.json()) as T };
  } catch {
    return { ok: false, response: json({ error: "invalid JSON body" }, 400) };
  }
}

/** ルートテーブルの1エントリ。合成側は method 一致 + match(pathname) で先頭一致ディスパッチする */
export type RouteEntry = {
  method: string;
  match: (pathname: string) => boolean;
  handler: (req: Request, url: URL) => Response | Promise<Response>;
};

/** 完全一致ルート */
export function exact(method: string, pathname: string, handler: RouteEntry["handler"]): RouteEntry {
  return { method, match: (p) => p === pathname, handler };
}

/** 前方一致ルート（パスパラメータ付き。例: DELETE /api/chunks/:id） */
export function prefix(method: string, pathnamePrefix: string, handler: RouteEntry["handler"]): RouteEntry {
  return { method, match: (p) => p.startsWith(pathnamePrefix), handler };
}
```

- [ ] **Step 2: 代表モジュール routes/sentences.ts を作る（分割パターンの確定）**

このモジュールを最初に作り、以降の全モジュールはこの形（狭い deps 型・ハンドラ移動・`makeXRoutes` が `RouteEntry[]` を返す）に倣う。

`app/server/routes/sentences.ts`（`handleSentenceQueue`=旧 `routes.ts:391-408`、`handleSentenceGrade`=旧 `:410-427`、`handleSentenceExplain`=旧 `:474-491` をそのまま移動。XP 付与は Task 1 で `xpForGrade` 済み）:

```ts
import { json, parseJsonBody, exact, type RouteEntry } from "./http";
import { xpForGrade } from "../progression";
import type { Grade, SentenceStore } from "../sentences";
import type { Chunk, ChunkStore } from "../chunks";
import type { ProgressStore } from "../progress-store";

export type SentenceRoutesDeps = {
  sentenceStore: SentenceStore;
  chunkStore: ChunkStore;
  progressStore: ProgressStore;
  /** 例文の詳しい解説を生成（キャッシュは sentenceStore 側。実体は coach.ts、テストはフェイク） */
  explainSentence: (s: { en: string; ja: string; note: string }) => Promise<{ text: string }>;
};

const GRADES = ["good", "soso", "bad"] as const;

function handleSentenceQueue(url: URL, deps: SentenceRoutesDeps): Response {
  const raw = url.searchParams.get("new") ?? "10";
  const n = Number(raw);
  if (!Number.isInteger(n) || n < 0 || n > 50) {
    return json({ error: "new must be an integer between 0 and 50" }, 400);
  }
  const sentences = deps.sentenceStore.queue(n).map((s) => ({ kind: "sentence" as const, ...s }));
  let chunks: Array<{ kind: "chunk" } & Omit<Chunk, "created" | "source">> = [];
  try {
    chunks = deps.chunkStore.dueChunks().map((c) => ({
      kind: "chunk" as const, id: c.id, promptText: c.promptText, en: c.en, note: c.note, srs: c.srs,
    }));
  } catch (err) {
    console.warn("[chunks] dueChunks failed, continuing with sentences only:", String(err));
  }
  return json({ queue: [...chunks, ...sentences] });
}

async function handleSentenceGrade(req: Request, deps: SentenceRoutesDeps): Promise<Response> {
  const parsed = await parseJsonBody<{ no?: unknown; grade?: unknown }>(req);
  if (!parsed.ok) return parsed.response;
  const { no, grade } = parsed.body;
  if (typeof no !== "number" || !Number.isInteger(no)) return json({ error: "no must be an integer" }, 400);
  if (!(GRADES as readonly string[]).includes(grade as string)) {
    return json({ error: `grade must be one of: ${GRADES.join(", ")}` }, 400);
  }
  const r = deps.sentenceStore.grade(no, grade as Grade);
  if (!r) return json({ error: `unknown sentence no: ${no}` }, 400);
  try {
    deps.progressStore.addXp("srs-grade", xpForGrade(grade as Grade), { no });
  } catch (err) {
    console.warn("[progress] srs-grade xp failed, continuing:", String(err));
  }
  return json(r);
}

async function handleSentenceExplain(req: Request, deps: SentenceRoutesDeps): Promise<Response> {
  const parsed = await parseJsonBody<{ no?: unknown }>(req);
  if (!parsed.ok) return parsed.response;
  const { no } = parsed.body;
  if (typeof no !== "number" || !Number.isInteger(no)) return json({ error: "no must be an integer" }, 400);
  const cached = deps.sentenceStore.getExplanation(no);
  if (cached !== null) return json({ no, text: cached });
  const sentence = deps.sentenceStore.find(no);
  if (!sentence) return json({ error: `unknown sentence no: ${no}` }, 400);
  const generated = await deps.explainSentence(sentence);
  try {
    deps.sentenceStore.saveExplanation(no, generated.text);
  } catch (err) {
    console.warn("[sentences] explanation cache write failed, continuing:", String(err));
  }
  return json({ no, text: generated.text });
}

export function makeSentenceRoutes(deps: SentenceRoutesDeps): RouteEntry[] {
  return [
    exact("GET", "/api/sentences", () => json({ sentences: deps.sentenceStore.list() })),
    exact("GET", "/api/sentences/queue", (_req, url) => handleSentenceQueue(url, deps)),
    exact("POST", "/api/sentences/grade", (req) => handleSentenceGrade(req, deps)),
    exact("POST", "/api/sentences/explain", (req) => handleSentenceExplain(req, deps)),
  ];
}
```

- [ ] **Step 3: 残りモジュールを同一パターンで作る（ハンドラは機械的移動）**

各ファイルは Step 2 と同じ構造（`import { ... } from "./http"` → 狭い `XRoutesDeps` 型 export → 移動したハンドラ → `makeXRoutes` が `RouteEntry[]` を返す）。ハンドラ本体は旧 `routes.ts` の該当行をそのまま移す（`deps: RouteDeps` を `deps: XRoutesDeps` に読み替えるだけ。`json`/`parseJsonBody` は `./http` から import）。旧 `routes.ts` にあった `GRADES`/`BLOCK_EVENT_TYPES`/`LEVEL_ACTIONS` などの module 定数は、それを使うハンドラのモジュールへ一緒に移す。

| モジュール | Deps 型（フィールド） | 移動元ハンドラ（旧 routes.ts 行） | `makeXRoutes` が返す RouteEntry |
|---|---|---|---|
| `system.ts` `SystemRoutesDeps` | `health: () => ReturnType<typeof checkHealth>; transcribe: typeof transcribeAudio; synthesize: typeof synthesize; logFile: () => string; recordingsDir?: string` | `handleStt`(93-115), `handleTts`(117-124) | `exact("GET","/api/health",()=>json(deps.health()))`, `exact("POST","/api/stt",(req)=>handleStt(req,deps))`, `exact("POST","/api/tts",(req)=>handleTts(req,deps))` |
| `converse.ts` `ConverseRoutesDeps` | `converse: typeof converseTurn; scenarioPrompt: (id: string) => string \| null` | `handleConverse`(126-139) | `exact("POST","/api/converse",(req)=>handleConverse(req,deps))` |
| `session.ts` `SessionRoutesDeps` | `logFile: () => string` | `handleSessionStart`(253-265), `handleSessionEnd`(267-274), `handleSessionEvent`(232-246)（`BLOCK_EVENT_TYPES`/`BlockEventType` 229-230 も移す） | `exact("POST","/api/session/start",...)`, `exact("POST","/api/session/end",...)`, `exact("POST","/api/session/event",...)` |
| `menu.ts` `MenuRoutesDeps` | `buildMenu: (minutes: 60\|30) => Menu; buildQuick: (kind: QuickKind, domain?: Domain) => Menu` | `handleMenuToday`(141-146), `handleMenuQuick`(148-159) | `exact("GET","/api/menu/today",(_req,url)=>handleMenuToday(url,deps))`, `exact("GET","/api/menu/quick",(_req,url)=>handleMenuQuick(url,deps))` |
| `settings.ts` `SettingsRoutesDeps` | `getSettings: () => Settings; saveSettings: (s: Settings) => void` | `handleSettingsPut`(161-170) | `exact("GET","/api/settings",()=>json(deps.getSettings()))`, `exact("PUT","/api/settings",(req)=>handleSettingsPut(req,deps))` |
| `library.ts` `LibraryRoutesDeps` | `libraryStore: LibraryStore` | （専用ハンドラなし） | `exact("GET","/api/library/model-talks",()=>json({ entries: deps.libraryStore.listModelTalks() }))` |
| `coach.ts` `CoachRoutesDeps` | `aeFeedback; modelTalk; libraryStore; reflection; prepPack; explainTalk; talkExplainCache; chunkStore`（型は旧 RouteDeps と同一） | `handleAeFeedback`(172-182), `handleReflection`(194-200), `handleModelTalk`(202-218), `handlePrep`(220-227), `handleTalkExplain`(455-472) | `exact("POST","/api/feedback/ae",...)`, `exact("POST","/api/coach/model-talk",...)`, `exact("POST","/api/coach/prep",...)`, `exact("POST","/api/coach/reflection",()=>handleReflection(deps))`, `exact("POST","/api/coach/talk-explain",...)` |
| `chunks.ts` `ChunkRoutesDeps` | `chunkStore: ChunkStore; progressStore: ProgressStore` | `handleChunkGrade`(429-446), `handleChunkDelete`(448-453)。加えて `collectBestEffort` を移設（下記 Step 4） | `exact("GET","/api/chunks",()=>json({ chunks: deps.chunkStore.list() }))`, `exact("POST","/api/chunks/grade",(req)=>handleChunkGrade(req,deps))`, `prefix("DELETE","/api/chunks/",(_req,url)=>handleChunkDelete(url,deps))` |
| `progress.ts` `ProgressRoutesDeps` | `practiceDays: () => string[]; progressStore: ProgressStore; invalidateMenuCache: () => void` | `handleProgressXp`(278-291), `handleProgressBlockStart`(293-301), `handleProgressLevel`(305-324)（`GRADES` は sentences/chunks 側に持つので不要。`LEVEL_ACTIONS` 303 を移す） | `exact("GET","/api/progress/days",()=>json({ days: deps.practiceDays() }))`, `exact("GET","/api/progress/summary",()=>json(deps.progressStore.getSummary()))`, `exact("POST","/api/progress/xp",...)`, `exact("POST","/api/progress/block-start",...)`, `exact("POST","/api/progress/level",...)` |
| `placement.ts` `PlacementRoutesDeps` | `evaluatePlacement; placementStore; progressStore; invalidateMenuCache` | `handlePlacementSubmit`(326-365), `handlePlacementConfirm`(367-389) | `exact("GET","/api/placement/tasks",()=>json({ tasks: PLACEMENT_TASKS }))`, `exact("POST","/api/placement/submit",...)`, `exact("POST","/api/placement/confirm",...)`, `exact("GET","/api/placement/latest",()=>json({ result: deps.placementStore.latest() }))` |
| `metrics.ts` `MetricsRoutesDeps` | `metricsSummary: (days: number) => MetricsSummary` | `handleMetricsSummary`(493-500) | `exact("GET","/api/metrics/summary",(_req,url)=>handleMetricsSummary(url,deps))` |
| `assessment.ts` `AssessmentRoutesDeps` | `assessmentStore; assembleMonthData; generateMonthlyReport` | `handleAssessmentGenerate`(502-514) | `exact("POST","/api/assessment/generate",...)`, `exact("GET","/api/assessment/latest",()=>json({ report: deps.assessmentStore.latest() }))`, `exact("GET","/api/assessment/list",()=>json({ reports: deps.assessmentStore.list() }))` |

各モジュールの import は必要な型・値のみ（`system.ts` は `checkHealth`/`transcribeAudio`/`synthesize`/`localYmd`/`RECORDINGS_DIR`/`appendEvent`/`computeUtteranceMetrics` 等、旧 routes.ts の対応 import を引き継ぐ。`placement.ts` は `PLACEMENT_TASKS`/`PlacementSubmission` 等、`PLACEMENT_XP` は Task 1 で import 済みの `../progression` から）。`localYmd` は `../dates` から（`system.ts` の stt と `assessment.ts` の generate が使用）。

- [ ] **Step 4: collectBestEffort のシグネチャを狭めて chunks.ts へ移設**

旧 `routes.ts:184-192` の `collectBestEffort(deps: RouteDeps, ...)` を `chunks.ts` に移し、`ChunkStore` を直接受ける形にする（coach モジュールから再利用するため export）:

```ts
// routes/chunks.ts 内
import type { ChunkStore, CollectCandidate } from "../chunks";

/** 収集はベストエフォート — 失敗しても親レスポンスを失敗させない（XP付与と同じ方針） */
export function collectBestEffort(chunkStore: ChunkStore, cands: CollectCandidate[]): number {
  try {
    return chunkStore.collect(cands);
  } catch (err) {
    console.warn("[chunks] collect failed, continuing:", String(err));
    return 0;
  }
}
```

`coach.ts` の `handleAeFeedback` / `handleReflection` は `import { collectBestEffort } from "./chunks";` して `collectBestEffort(deps.chunkStore, cands)` を呼ぶ形に読み替える（旧 `collectBestEffort(deps, cands)` の呼び出し2箇所）。

- [ ] **Step 5: routes.ts を合成＋ディスパッチに縮小（新規コード）**

`app/server/routes.ts` の全内容を次に置き換える:

```ts
import { appendEvent, isErrorLogged } from "./session-log";
import { json, type RouteEntry } from "./routes/http";
import { makeSystemRoutes, type SystemRoutesDeps } from "./routes/system";
import { makeConverseRoutes, type ConverseRoutesDeps } from "./routes/converse";
import { makeSessionRoutes, type SessionRoutesDeps } from "./routes/session";
import { makeMenuRoutes, type MenuRoutesDeps } from "./routes/menu";
import { makeSettingsRoutes, type SettingsRoutesDeps } from "./routes/settings";
import { makeLibraryRoutes, type LibraryRoutesDeps } from "./routes/library";
import { makeCoachRoutes, type CoachRoutesDeps } from "./routes/coach";
import { makeSentenceRoutes, type SentenceRoutesDeps } from "./routes/sentences";
import { makeChunkRoutes, type ChunkRoutesDeps } from "./routes/chunks";
import { makeProgressRoutes, type ProgressRoutesDeps } from "./routes/progress";
import { makePlacementRoutes, type PlacementRoutesDeps } from "./routes/placement";
import { makeMetricsRoutes, type MetricsRoutesDeps } from "./routes/metrics";
import { makeAssessmentRoutes, type AssessmentRoutesDeps } from "./routes/assessment";

/**
 * HTTP ハンドラが依存する副作用の総体。各ドメインの狭い Deps 型の交差で構成する。
 * 実サーバ（index.ts）は実装を、テスト（__tests__）はフェイクを渡す。
 * エンドポイント追加は「ドメインモジュールに1ハンドラ＋1エントリ」、新ドメインなら
 * 「新モジュール＋この合成配列に1行＋この交差型に1項」で完結する。
 */
export type RouteDeps =
  SystemRoutesDeps & ConverseRoutesDeps & SessionRoutesDeps & MenuRoutesDeps &
  SettingsRoutesDeps & LibraryRoutesDeps & CoachRoutesDeps & SentenceRoutesDeps &
  ChunkRoutesDeps & ProgressRoutesDeps & PlacementRoutesDeps & MetricsRoutesDeps &
  AssessmentRoutesDeps;

/** 現在の index.ts の全ルーティング・ハンドラをソケットを開かずにテストできる形に切り出したもの */
export function makeFetchHandler(deps: RouteDeps): (req: Request) => Promise<Response> {
  const routes: RouteEntry[] = [
    ...makeSystemRoutes(deps),
    ...makeConverseRoutes(deps),
    ...makeSessionRoutes(deps),
    ...makeMenuRoutes(deps),
    ...makeSettingsRoutes(deps),
    ...makeLibraryRoutes(deps),
    ...makeCoachRoutes(deps),
    ...makeSentenceRoutes(deps),
    ...makeChunkRoutes(deps),
    ...makeProgressRoutes(deps),
    ...makePlacementRoutes(deps),
    ...makeMetricsRoutes(deps),
    ...makeAssessmentRoutes(deps),
  ];
  return async function fetch(req: Request): Promise<Response> {
    const url = new URL(req.url);
    try {
      for (const r of routes) {
        if (req.method === r.method && r.match(url.pathname)) return await r.handler(req, url);
      }
      return json({ error: "not found" }, 404);
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      if (!isErrorLogged(err)) {
        try {
          appendEvent(deps.logFile(), {
            ts: new Date().toISOString(), type: "error", sessionId: "server", text: message,
          });
        } catch (logErr) {
          // ロギング自体の失敗で「常に{error}JSONを返す」保証を崩さないためのガード
          console.error(`routes: failed to append error event: ${String(logErr)}`);
        }
      }
      return json({ error: message }, 500);
    }
  };
}
```

（ルート順序: パス×メソッドは全ドメインで一意なので先頭一致で挙動不変。唯一の前方一致 `DELETE /api/chunks/` は method が他の `/api/chunks*`(GET/POST) と異なるため衝突しない。）

- [ ] **Step 6: 検証ゲート**

```bash
cd app && bun test && bun run typecheck
cd app/client && bun run build
```
Expected: 全緑（`routes.test.ts` は未分割のまま `makeFetchHandler`/`RouteDeps` 経由で通る）/ 0エラー / 成功。

- [ ] **Step 7: コミット**

```bash
cd <repo-root>
git add app/server/routes.ts app/server/routes/
git commit -m "refactor: HTTP層を機能別ルータモジュール＋テーブルディスパッチに分割"
```

**この分割が P1 で満たす規約（実証）**: 例えば P1 の「言い方ヒント」`POST /api/coach/say-it` を足す場合 —「1ハンドラ＋1エントリ」を `routes/coach.ts` に追加し（`CoachRoutesDeps` に必要な依存を1項追加）、`index.ts` の `realDeps` にその依存を1行配線するだけで済む。全く新しいドメイン（例 `routes/hints.ts`）なら「新モジュール作成」＋ `routes.ts` の合成配列に `...makeHintRoutes(deps)` の1行 ＋ `RouteDeps` 交差に `& HintRoutesDeps` の1項 ＋ `index.ts` 配線。旧構造で必須だった「RouteDeps 30フィールドへの追記・if連鎖への挿入」は消える。

---

## Task 3: routes.test.ts を型付きフェイクファクトリ＋ドメイン別ファイルに分割

1,463行のモノリスと `as RouteDeps["…"]` アサーション43箇所を、`satisfies` で型完全な共有フェイクファクトリに置き換え、describe 境界でファイル分割する。ストアにメソッドを足したとき tsc が全フェイクを列挙するようにする。

**Files:**
- Create: `app/server/__tests__/helpers/route-deps.ts`
- Create: `app/server/__tests__/routes-system.test.ts` / `routes-converse.test.ts` / `routes-session.test.ts` / `routes-menu.test.ts` / `routes-coach.test.ts` / `routes-library.test.ts` / `routes-sentences.test.ts` / `routes-chunks.test.ts` / `routes-progress.test.ts` / `routes-placement.test.ts` / `routes-metrics.test.ts` / `routes-assessment.test.ts` / `routes-errors.test.ts`
- Delete: `app/server/__tests__/routes.test.ts`（内容は上記へ移設後）

**Interfaces:**
- Produces: `makeTestDeps(overrides?: Partial<RouteDeps>): { deps: RouteDeps; logFile: string; recordingsDir: string }`、`FAKE_HEALTH/FAKE_MENU/FAKE_QUICK_MENU/FAKE_AE/FAKE_REFLECTION/FAKE_SENTENCE/FAKE_SUMMARY`、`makeFakeSentenceStore/makeFakeChunkStore/makeFakeProgressStore/makeFakePlacementStore/makeFakeLibraryStore/makeFakeAssessmentStore/makeFakeTalkExplainCache`（各 `(overrides?: Partial<Store>) => Store`）。全て `app/server/__tests__/helpers/route-deps.ts` から export。

- [ ] **Step 1: フェイクファクトリ helpers/route-deps.ts を作る（新規コード）**

`app/server/__tests__/helpers/route-deps.ts`。各ストアはフル実装を `satisfies` で型検査し、`overrides` を後勝ちマージする（`as` アサーションは使わない）:

```ts
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import path from "node:path";
import type { RouteDeps } from "../../routes";
import type { SentenceStore } from "../../sentences";
import type { ChunkStore } from "../../chunks";
import type { ProgressStore } from "../../progress-store";
import type { PlacementStore } from "../../placement";
import type { LibraryStore, TalkExplainCache } from "../../db";
import type { AssessmentStore } from "../../assessment";
import type { QuickKind } from "../../menu";

export const FAKE_HEALTH = { ok: true, whisper: true, ffmpeg: true, claude: true, ttsKey: true, modelFile: true };
export const FAKE_MENU = {
  minutes: 60 as const,
  date: "2026-07-05",
  level: 13,
  blocks: [{ id: "b1", kind: "reflection", title: "振り返り", minutes: 5, params: {} }],
};
export const FAKE_QUICK_MENU = {
  minutes: 6,
  date: "2026-07-05",
  level: 13,
  blocks: [{ id: "q1", kind: "warmup-reading", title: "音読ウォームアップ", minutes: 6, params: {} }],
};
export const FAKE_AE = { items: [{ quote: "q", issue: "i", better: "b", why_ja: "w" }], praise: "p" };
export const FAKE_REFLECTION = { goodPhrases: ["g"], fixes: [], noteForTomorrow_ja: "n" };
export const FAKE_SENTENCE = {
  no: 1, category_no: 1, category: "現在形", domain: "daily" as const,
  en: "I usually start work at nine.", ja: "たいてい9時に仕事を始めます。", note: "習慣の現在形",
  srs: null,
};
export const FAKE_SUMMARY = {
  level: 13, xp: 0, xpIntoLevel: 0, xpToNext: 25, stage: 2, difficultyMaxed: false, proposal: null,
};

export function makeFakeSentenceStore(overrides: Partial<SentenceStore> = {}): SentenceStore {
  return {
    list: () => [FAKE_SENTENCE],
    queue: (_newCount: number) => [FAKE_SENTENCE],
    grade: (no: number, _grade: "good" | "soso" | "bad") =>
      no === 1 ? { no: 1, stage: 1, due: "2026-07-09" } : null,
    getExplanation: (_no: number) => null,
    saveExplanation: (_no: number, _text: string) => {},
    find: (no: number) => (no === 1 ? FAKE_SENTENCE : undefined),
    ...overrides,
  } satisfies SentenceStore;
}

export function makeFakeChunkStore(overrides: Partial<ChunkStore> = {}): ChunkStore {
  return {
    collect: (_c) => 0,
    list: () => [],
    dueChunks: () => [],
    grade: (id, _g) => (id === 1 ? { id: 1, stage: 1, due: "2026-07-09" } : null),
    remove: (id) => id === 1,
    ...overrides,
  } satisfies ChunkStore;
}

export function makeFakeProgressStore(overrides: Partial<ProgressStore> = {}): ProgressStore {
  return {
    getLevel: () => 13,
    getSummary: () => FAKE_SUMMARY,
    addXp: (kind, amount) =>
      kind === "block" && Number.isInteger(amount) && amount >= 1 && amount <= 60 ? FAKE_SUMMARY
      : kind === "srs-grade" ? FAKE_SUMMARY : null,
    blockStart: (_kind) => ({ attemptId: 7 }),
    levelAction: (action, level) =>
      action === "set" && Number.isInteger(level) && (level as number) >= 1
        ? { summary: FAKE_SUMMARY, levelChanged: true } : null,
    placementSet: (_level) => ({ summary: FAKE_SUMMARY, levelChanged: true }),
    ...overrides,
  } satisfies ProgressStore;
}

export function makeFakePlacementStore(overrides: Partial<PlacementStore> = {}): PlacementStore {
  return {
    save: (r) => ({ id: 1, ts: "2026-07-06T00:00:00.000Z", stage: r.stage, startLevel: r.startLevel, rationale: r.rationale }),
    latest: () => null,
    ...overrides,
  } satisfies PlacementStore;
}

export function makeFakeLibraryStore(overrides: Partial<LibraryStore> = {}): LibraryStore {
  return {
    saveModelTalk: (_e) => {},
    listModelTalks: () => [],
    ...overrides,
  } satisfies LibraryStore;
}

export function makeFakeAssessmentStore(overrides: Partial<AssessmentStore> = {}): AssessmentStore {
  return {
    save: (r) => ({ id: 1, ts: "2026-07-06T00:00:00.000Z", ymd: r.ymd, text: r.text }),
    latest: () => null,
    list: () => [],
    findByMonth: () => null,
    ...overrides,
  } satisfies AssessmentStore;
}

export function makeFakeTalkExplainCache(overrides: Partial<TalkExplainCache> = {}): TalkExplainCache {
  return {
    get: (_hash) => null,
    save: (_hash, _text, _created) => {},
    ...overrides,
  } satisfies TalkExplainCache;
}

/** テストごとに独立した temp dir/log を持つフェイク RouteDeps を組み立てる */
export function makeTestDeps(overrides: Partial<RouteDeps> = {}): {
  deps: RouteDeps;
  logFile: string;
  recordingsDir: string;
} {
  const dir = mkdtempSync(path.join(tmpdir(), "routes-"));
  const logFile = path.join(dir, "log.jsonl");
  const recordingsDir = path.join(dir, "recordings");
  const deps: RouteDeps = {
    transcribe: async (_inputPath: string) => ({ text: "fake transcript", segments: [] }),
    synthesize: async (_text: string) => ({ audio: new Uint8Array([1, 2, 3]), mime: "audio/mpeg", engine: "say" as const }),
    converse: async (args: { userText: string; sessionId?: string }) => ({
      replyText: `echo: ${args.userText}`, sessionId: args.sessionId ?? "sess-fake",
    }),
    health: () => FAKE_HEALTH,
    logFile: () => logFile,
    recordingsDir,
    buildMenu: (_minutes) => FAKE_MENU,
    aeFeedback: async () => FAKE_AE,
    modelTalk: async (topicId: string) => (topicId === "known-topic" ? { text: "model talk" } : null),
    reflection: async () => FAKE_REFLECTION,
    scenarioPrompt: (id: string) => (id === "known-scenario" ? "ROLEPLAY PROMPT" : null),
    prepPack: async () => ({ chunks: [{ en: "The main problem was ...", ja: "一番の問題は…" }], outline: ["Opening"] }),
    buildQuick: (_kind: QuickKind) => FAKE_QUICK_MENU,
    practiceDays: () => ["2026-07-01", "2026-07-03"],
    getSettings: () => ({ anchor: "" }),
    saveSettings: (_s) => {},
    libraryStore: makeFakeLibraryStore(),
    sentenceStore: makeFakeSentenceStore(),
    chunkStore: makeFakeChunkStore(),
    progressStore: makeFakeProgressStore(),
    invalidateMenuCache: () => {},
    placementStore: makeFakePlacementStore(),
    evaluatePlacement: async () => ({ stage: 2, startLevel: 13, rationaleJa: "簡単な文は安定しています。" }),
    explainSentence: async () => ({ text: "be getting + 比較級は進行中の変化を表します。" }),
    metricsSummary: (days: number) => ({
      days: Array.from({ length: days }, (_, i) => ({
        ymd: `2026-07-${String(i + 1).padStart(2, "0")}`,
        utterances: 0, speakingSec: 0, avgArticulationWpm: 0, avgPauseRatio: 0, repetitionRatio: 0,
      })),
      level: { current: 13, history: [] },
    }),
    explainTalk: async () => ({ text: "日本語訳: テスト訳\n\n表現ポイント:\n- test — テスト" }),
    talkExplainCache: makeFakeTalkExplainCache(),
    assessmentStore: makeFakeAssessmentStore(),
    assembleMonthData: () => ({ windowDays: 30 }) as ReturnType<RouteDeps["assembleMonthData"]>,
    generateMonthlyReport: async () => "今月の振り返りテキスト",
    ...overrides,
  };
  return { deps, logFile, recordingsDir };
}
```

（`transcribe`/`synthesize`/`converse`/`buildMenu` 等の関数フェイクは、`RouteDeps` の各フィールド型に代入可能なため `as` を外せる。もし推論が緩くて代入エラーになる箇所があれば、その関数リテラルにのみ引数型注釈を足す — オブジェクト全体の `as RouteDeps` は使わない。）

- [ ] **Step 2: describe ブロックをドメイン別テストファイルへ移設**

旧 `routes.test.ts` の各 describe を、下表のファイルへ **本文そのまま** 移す。各ファイル先頭は次の共通 import にする（使う FAKE_/ファクトリだけを列挙）:

```ts
import { describe, expect, test } from "bun:test";
import { makeFetchHandler } from "../routes";
import { makeTestDeps, FAKE_SUMMARY /* 等、そのファイルで使うものだけ */ } from "./helpers/route-deps";
```

移設時の置換ルール:
- インラインのフルスペックフェイク（`{ ... } as RouteDeps["chunkStore"]` 等）は、対応する `makeFakeXStore({ 差し替えたいメソッドだけ })` に置換する。
- 事後代入 `(deps.progressStore as {...}).placementSet = ...`（placement confirm 3テスト）は、`makeTestDeps({ progressStore: makeFakeProgressStore({ placementSet: (l) => { ...; return { summary: FAKE_SUMMARY, levelChanged: true }; } }) })` に置換する（呼び出し記録用のクロージャ変数は describe スコープに残す）。
- `...makeTestDeps().deps.progressStore` スプレッド（placement submit テスト）は `makeFakeProgressStore({ addXp: ... })` に置換する（temp dir を余分に作る二重 makeTestDeps 呼びを解消）。
- `markErrorLogged`/`readEvents`/`existsSync`/`readdirSync`/`localYmd`/`QuickKind` 等、個々の describe が使う import は各ファイルに必要な分だけ足す（`routes-errors.test.ts` は `markErrorLogged`、`routes-system.test.ts`/`routes-session.test.ts` は `readEvents`/`existsSync` 等）。

| 新ファイル | 移設する describe（旧 routes.test.ts 行） |
|---|---|
| `routes-system.test.ts` | `routes: health`(133-142), `routes: stt`(143-201), `routes: tts`(202-247) |
| `routes-converse.test.ts` | `routes: converse`(248-291), `routes: converse + scenarioId`(485-529) |
| `routes-session.test.ts` | `routes: session`(292-361), `routes: session/event`(435-484) |
| `routes-menu.test.ts` | `routes: menu`(362-384), `routes: quick menu / progress / settings`(646-708) |
| `routes-coach.test.ts` | `routes: coach`(385-434), `routes: coach/prep`(604-645), `routes: モデルトーク解説`(891-945) |
| `routes-library.test.ts` | `library`(709-775) |
| `routes-sentences.test.ts` | `sentences ルート`(776-890) |
| `routes-chunks.test.ts` | `chunks: 収集フックと API`(1206-1348) |
| `routes-progress.test.ts` | `routes: progress`(946-1064) |
| `routes-placement.test.ts` | `placement API`(1066-1204) |
| `routes-metrics.test.ts` | `routes: metrics`(1349-1390) |
| `routes-assessment.test.ts` | `routes: assessment`(1391-1463) |
| `routes-errors.test.ts` | `routes: 404 と 500`(530-603) |

（`routes: quick menu / progress / settings` は menu/progress/settings が混在する既存 describe。まとめて `routes-menu.test.ts` に置くか、progress 部分だけ `routes-progress.test.ts` へ切り出すかは実装者判断でよい。分割する場合も本文は改変しない。）

- [ ] **Step 3: 旧 routes.test.ts を削除**

全 describe を移設したら `app/server/__tests__/routes.test.ts` を削除する（`FAKE_*`/`makeTestDeps` は helpers に移行済み）。

```bash
git rm app/server/__tests__/routes.test.ts
```

- [ ] **Step 4: 検証ゲート**

```bash
cd app && bun test && bun run typecheck
cd app/client && bun run build
```
Expected: 全緑（ケース総数は移設前と同じ）/ 0エラー / 成功。テスト総数を移設前と突き合わせて減っていないことを確認する（`cd app && bun test 2>&1 | tail -5` で pass 数を確認）。

- [ ] **Step 5: コミット**

```bash
cd <repo-root>
git add app/server/__tests__/
git commit -m "test: routes.test を型付きフェイクファクトリ＋ドメイン別ファイルに分割"
```

---

## Task 4: useLoad フックを抽出し単一値ロード画面を移行（Reflection の二重POST解消）

`aliveRef + fetchedRef + LoadState + エラー整形` の手書きコピーを `useLoad<T>` に一本化し、単一値ロード＋エラーUI＋再試行の6箇所を移行する。ReflectionScreen はガード漏れによる StrictMode 二重POST（本計画で唯一の意図的挙動変更）を解消する。

**移行対象と対象外の線引き（重要）**: `useLoad<T>` は「マウント時に1回 fetch → loading/ready/error を表示 → 再試行」という単一値ロードに適合する。適合する6箇所のみ移行する: ReflectionScreen / LibraryScreen / WarmupReadingScreen / ProgressScreen（メイン） / SentencesScreen の PracticeTab・BrowseTab。以下は `useLoad<T>` に素直に写像できず、純リファクタの挙動不変を崩すリスクがあるため **本タスクの対象外**とする（監査所見の11箇所のうち5箇所）: ShadowingScreen（`prefetchModelTalkAudio` が段階コールバックで state を更新＋2値ロード＋playing状態）、PlacementScreen（ロード結果が `Step` 状態機械へ遷移）、StartScreen（3値の寛容ロード＋summary を提案操作で後続変異）、App の PracticeStat（2値の寛容ロード＋`onProgressUpdate` 購読＋レベル編集で変異）、ProgressScreen の MonthlyReview（`Promise.all` の2値＋寛容＋生成で変異）。これらは `setData` を返す拡張版フックが必要で、R2 の「挙動不変」の安全域外。→ 完了報告で team-lead に明示する。

**Files:**
- Create: `app/client/src/useLoad.ts`
- Modify: `app/client/src/screens/ReflectionScreen.tsx`
- Modify: `app/client/src/screens/LibraryScreen.tsx`
- Modify: `app/client/src/screens/WarmupReadingScreen.tsx`
- Modify: `app/client/src/screens/ProgressScreen.tsx:26-62`
- Modify: `app/client/src/screens/SentencesScreen.tsx`（PracticeTab 36-77, BrowseTab 220-264）

**Interfaces:**
- Produces: `LoadState<T> = { status: "loading" } | { status: "ready"; data: T } | { status: "error"; error: string }`、`useLoad<T>(fn: () => Promise<T>): { state: LoadState<T>; reload: () => void }`（`app/client/src/useLoad.ts`）。

- [ ] **Step 1: useLoad フックを作る（新規コード）**

`app/client/src/useLoad.ts`:

```ts
import { useEffect, useRef, useState } from "react";

export type LoadState<T> =
  | { status: "loading" }
  | { status: "ready"; data: T }
  | { status: "error"; error: string };

/**
 * マウント時に fn() を1回だけ実行し、loading/ready/error を管理する。
 * StrictMode の二重マウント（once ガード）とアンマウント後の setState（alive ガード）を内蔵し、
 * 各画面が手書きしていた aliveRef + fetchedRef + LoadState + エラー整形を一本化する。
 * reload() は fn() を再実行する（再試行ボタン用）。
 */
export function useLoad<T>(fn: () => Promise<T>): { state: LoadState<T>; reload: () => void } {
  const [state, setState] = useState<LoadState<T>>({ status: "loading" });
  const aliveRef = useRef(true);
  const fetchedRef = useRef(false);
  // fn は呼び出しごとに新参照になりうるため ref に保持し、effect の依存から外す（挙動は「マウント時1回」）
  const fnRef = useRef(fn);
  fnRef.current = fn;

  function run() {
    setState({ status: "loading" });
    fnRef.current()
      .then((data) => { if (aliveRef.current) setState({ status: "ready", data }); })
      .catch((err) => {
        if (aliveRef.current) setState({ status: "error", error: err instanceof Error ? err.message : String(err) });
      });
  }

  useEffect(() => {
    aliveRef.current = true;
    if (!fetchedRef.current) {
      fetchedRef.current = true;
      run();
    }
    return () => { aliveRef.current = false; };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  return { state, reload: run };
}
```

- [ ] **Step 2: ReflectionScreen を移行（＝二重POST解消）**

`app/client/src/screens/ReflectionScreen.tsx` を全面差し替え:

```tsx
import { fetchReflection } from "../api";
import { useLoad } from "../useLoad";
import { Banner } from "../ui/Banner";
import { Button } from "../ui/Button";
import { Card } from "../ui/Card";

export function ReflectionScreen() {
  const { state, reload } = useLoad(fetchReflection);

  if (state.status === "error") {
    return (
      <div>
        <Banner kind="error" action={<Button onClick={reload}>再試行</Button>}>{state.error}</Banner>
      </div>
    );
  }
  if (state.status === "loading") return <p className="text-muted">コーチが今日のセッションを振り返っています…</p>;

  const reflection = state.data;
  return (
    <div className="stack">
      {reflection.goodPhrases.length > 0 && (
        <Card header={<h3>👏 良かった表現</h3>}>
          <ul>{reflection.goodPhrases.map((p, i) => <li key={i}>{p}</li>)}</ul>
        </Card>
      )}
      {reflection.fixes.length > 0 && (
        <Card header={<h3>✏️ 直したい表現</h3>}>
          <ul>
            {reflection.fixes.map((f, i) => (
              <li key={i}><s>{f.original}</s> → <strong>{f.better}</strong></li>
            ))}
          </ul>
        </Card>
      )}
      <Card header={<h3>📝 明日へ</h3>}>
        <p>{reflection.noteForTomorrow_ja}</p>
      </Card>
    </div>
  );
}
```

（旧 `useEffect(() => { loadReflection(); }, [])` の無ガード呼び出しが `useLoad` の once ガードに置き換わり、開発時 StrictMode での `POST /api/coach/reflection` 二重発火が解消する。）

- [ ] **Step 3: ProgressScreen（メイン）を移行**

`app/client/src/screens/ProgressScreen.tsx`。冒頭の `import { useEffect, useRef, useState } from "react";` に `useLoad` を追加 import（`import { useLoad } from "../useLoad";`）。`type LoadState = ...`（`:11`）は削除。関数本体先頭（`:27-62`）の state/aliveRef/fetchedRef/useEffect/load とガード分岐を次に差し替え:

```tsx
export function ProgressScreen({ lang }: { lang: Lang }) {
  const t = STR[lang].progress;
  const { state, reload } = useLoad(() => fetchMetricsSummary(14));

  if (state.status === "loading") return <p className="text-muted">{t.loading}</p>;
  if (state.status === "error") {
    return <Banner kind="error" action={<Button onClick={reload}>{t.retry}</Button>}>{state.error}</Banner>;
  }
  const summary = state.data;
```

以降（`const days = summary.days;` 〜）はそのまま。`useRef` が他で未使用になる場合は import から外す（`MonthlyReview` は本タスク対象外で `useRef` を使い続けるため、ファイル内で `useRef` は残る想定）。

- [ ] **Step 4: LibraryScreen を移行（再生用 aliveRef は保持）**

`app/client/src/screens/LibraryScreen.tsx`。`type State = ...`（`:8`）を削除、`import { useLoad } from "../useLoad";` を追加。関数本体先頭（`:12-45`）を差し替え（ロードは useLoad、再生 `play()` 用の aliveRef と unmount 時 `stopPlayback` は残す）:

```tsx
export function LibraryScreen() {
  const { state, reload } = useLoad(fetchModelTalkLibrary);
  const [errorMsg, setErrorMsg] = useState("");
  const [playingId, setPlayingId] = useState<number | null>(null);
  const aliveRef = useRef(true);

  useEffect(() => {
    aliveRef.current = true;
    return () => { aliveRef.current = false; stopPlayback(); };
  }, []);

  async function play(entry: ModelTalkEntry) {
    setErrorMsg("");
    setPlayingId(entry.id);
    try {
      await playTtsCached(entry.text);
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
    } finally {
      if (aliveRef.current) setPlayingId(null);
    }
  }
```

描画（`:60-99`）の状態分岐を `useLoad` の state に読み替える:
- `{state === "loading" && ...}` → `{state.status === "loading" && <p className="text-muted">読み込み中…</p>}`
- `{state === "error" && (<Banner ... action={<Button onClick={load}>再試行</Button>}>{errorMsg}</Banner>)}` → `{state.status === "error" && (<Banner kind="error" action={<Button onClick={reload}>再試行</Button>}>{state.error}</Banner>)}`
- `{state === "ready" && entries.length === 0 && (...)}` → `{state.status === "ready" && state.data.length === 0 && (...)}`
- `{state === "ready" && entries.map((e) => (...))}` → `{state.status === "ready" && state.data.map((e) => (...))}`
- `{state === "ready" && errorMsg && <Banner kind="error">{errorMsg}</Banner>}` → `{state.status === "ready" && errorMsg && <Banner kind="error">{errorMsg}</Banner>}`

（`entries`/`setEntries` state と `load()` 関数は削除。再生失敗の `errorMsg` は別 state として残す。）

- [ ] **Step 5: WarmupReadingScreen を移行（再生用 aliveRef は保持）**

`app/client/src/screens/WarmupReadingScreen.tsx`。`type State = ...`（`:9`）を削除、`import { useLoad } from "../useLoad";` を追加。関数本体先頭（`:16-50`）を差し替え:

```tsx
export function WarmupReadingScreen(props: { topic: ContentItem }) {
  const load = useLoad(() => fetchPrepPack(props.topic.id));
  const [playErr, setPlayErr] = useState("");
  const [playingIdx, setPlayingIdx] = useState<number | null>(null);
  const aliveRef = useRef(true);

  useEffect(() => {
    aliveRef.current = true;
    return () => { aliveRef.current = false; stopPlayback(); };
  }, []);

  async function playChunk(i: number, text: string) {
    setPlayErr("");
    setPlayingIdx(i);
    try {
      await playTtsCached(text);
    } catch (err) {
      if (!aliveRef.current) return;
      setPlayErr(err instanceof Error ? err.message : String(err));
    } finally {
      if (aliveRef.current) setPlayingIdx(null);
    }
  }

  const prep = load.state.status === "ready" ? load.state.data : null;
  const chunks = prep?.chunks.filter((c) => typeof c.en === "string" && c.en) ?? [];
```

描画（`:67-101`）の状態分岐を読み替える:
- `{state === "loading" && ...}` → `{load.state.status === "loading" && <p>コーチが表現チャンクを用意しています…</p>}`
- `{state === "error" && (...)}` ブロックの外側条件 → `{load.state.status === "error" && (...)}`、その中の `action={<Button onClick={load}>再試行</Button>}` → `action={<Button onClick={load.reload}>再試行</Button>}`、`{errorMsg}` → `{load.state.error}`
- `{state === "ready" && prep && (...)}` → `{load.state.status === "ready" && prep && (...)}`（`prep` は上で導出済み）

（`state`/`prep`/`errorMsg` state と `load()` 関数は削除。`playErr`/`playingIdx` は残す。）

- [ ] **Step 6: SentencesScreen の PracticeTab を移行**

`app/client/src/screens/SentencesScreen.tsx`。冒頭 import に `import { useLoad } from "../useLoad";` を追加。`type LoadState = ...`（`:18`）は BrowseTab でも使うため Step 7 まで残す。PracticeTab の state/useEffect/load（`:36-77`）のうちロード部分を差し替える（`idx`/`phase`/`gradedCount`/`dueTomorrow`/`explain`/`busy` と再生用 aliveRef は残す）:

差し替え後の先頭:

```tsx
function PracticeTab({ lang, hideNote }: { lang: Lang; hideNote: boolean }) {
  const t = STR[lang].sentences;
  const load = useLoad(() => fetchSentenceQueue(NEW_PER_DAY));
  const [idx, setIdx] = useState(0);
  const [phase, setPhase] = useState<Phase>("prompt");
  const [gradedCount, setGradedCount] = useState(0);
  const [dueTomorrow, setDueTomorrow] = useState<number | null>(null);
  const [errorMsg, setErrorMsg] = useState("");
  const [busy, setBusy] = useState(false);
  const [explain, setExplain] = useState<string | null>(null);
  const aliveRef = useRef(true);

  useEffect(() => {
    aliveRef.current = true;
    return () => { aliveRef.current = false; stopPlayback(); };
  }, []);

  const queue = load.state.status === "ready" ? load.state.data : [];
  const current = queue[idx];
  const done = load.state.status === "ready" && !current;
```

その後の `useEffect([done])`（`:82-95`、dueTomorrow 取得）はそのまま。ガード分岐（`:126-129`）を読み替え:
- `if (state === "loading") return <p className="text-muted">{t.loading}</p>;` → `if (load.state.status === "loading") return <p className="text-muted">{t.loading}</p>;`
- `if (state === "error") { return <Banner ... action={<Button onClick={load}>{t.retry}</Button>}>{errorMsg}</Banner>; }` → `if (load.state.status === "error") { return <Banner kind="error" action={<Button onClick={load.reload}>{t.retry}</Button>}>{load.state.error}</Banner>; }`

（`state`/`queue` state と `load()` 関数は削除。採点失敗用の `errorMsg` は別 state として残す。`grade()` 内の `setErrorMsg` はそのまま。旧 `load()` にあった `setIdx(0)`/`setPhase("prompt")` は初期値と同一で、再試行は初回ロード失敗時のみ発火するため省略可 — 挙動は不変。）

- [ ] **Step 7: SentencesScreen の BrowseTab を移行**

同ファイル BrowseTab（`:218-264`）。ロード部分を差し替える（`items`/`chunks`/`filter`/再生・削除系 state と再生用 aliveRef は残す）:

```tsx
function BrowseTab({ lang }: { lang: Lang }) {
  const t = STR[lang].sentences;
  const load = useLoad(async () => {
    const all = await fetchSentences();
    // チャンクは補助セクション — 取得失敗でも例文一覧は表示する
    let cs: ChunkListItem[] = [];
    try { cs = await fetchChunks(); } catch { /* ignore */ }
    return { items: all, chunks: cs };
  });
  const [filter, setFilter] = useState<"all" | SentenceItem["domain"]>("all");
  const [playingNo, setPlayingNo] = useState<number | null>(null);
  const [playingChunkId, setPlayingChunkId] = useState<number | null>(null);
  const [deletingId, setDeletingId] = useState<number | null>(null);
  const [errorMsg, setErrorMsg] = useState("");
  const [removedIds, setRemovedIds] = useState<number[]>([]);
  const aliveRef = useRef(true);

  useEffect(() => {
    aliveRef.current = true;
    return () => { aliveRef.current = false; stopPlayback(); };
  }, []);

  const items = load.state.status === "ready" ? load.state.data.items : [];
  const chunks = (load.state.status === "ready" ? load.state.data.chunks : []).filter((c) => !removedIds.includes(c.id));
```

`onDeleteChunk`（`:291-306`）の成功時 `setChunks((cs) => cs.filter((c) => c.id !== id))` は、ロード結果が useLoad 管理になったため、削除済み ID を別 state で持つ方式に変える（挙動＝削除したチャンクが即座に一覧から消える、は不変）:

```tsx
  async function onDeleteChunk(id: number) {
    if (deletingId !== id) {
      setDeletingId(id);
      return;
    }
    try {
      await deleteChunk(id);
      if (!aliveRef.current) return;
      setRemovedIds((prev) => [...prev, id]);
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
    } finally {
      if (aliveRef.current) setDeletingId(null);
    }
  }
```

ガード分岐（`:308-311`）を読み替え:
- `if (state === "loading") return <p className="text-muted">{t.loading}</p>;` → `if (load.state.status === "loading") return <p className="text-muted">{t.loading}</p>;`
- `if (state === "error") { return <Banner ... action={<Button onClick={load}>{t.retry}</Button>}>{errorMsg}</Banner>; }` → `if (load.state.status === "error") { return <Banner kind="error" action={<Button onClick={load.reload}>{t.retry}</Button>}>{load.state.error}</Banner>; }`

`play`/`playChunk`（`:266-288`）はそのまま（再生失敗の `errorMsg` は残した state を使う）。BrowseTab 移行後、ファイル内で `type LoadState`（`:18`）が未使用になるので削除する。

- [ ] **Step 8: 検証ゲート**

```bash
cd app/client && bun run build
```
Expected: `tsc --noEmit` 0エラー / `vite build` 成功。加えて開発サーバ（`cd app && bun run dev`）で ReflectionブロックがStrictModeでも `POST /api/coach/reflection` を1回だけ送ることを、`app/data` のセッションログまたはサーバログで確認する（任意の手動確認）。

- [ ] **Step 9: コミット**

```bash
cd <repo-root>
git add app/client/src/useLoad.ts app/client/src/screens/ReflectionScreen.tsx app/client/src/screens/LibraryScreen.tsx app/client/src/screens/WarmupReadingScreen.tsx app/client/src/screens/ProgressScreen.tsx app/client/src/screens/SentencesScreen.tsx
git commit -m "refactor: 非同期ロードをuseLoadフックに一本化しReflectionの二重POSTを解消"
```

---

## Task 5: StartSelection を MenuSource 透過に一本化

`StartSelection` の session 系（quick/daily）を `MenuSource` そのものにし、`App.onSelect` の field 単位の手詰め替えを消す。ロールプレイの `domain` 脱落（実バグ 73c4ad1、現在は修正済み）を生んだ二重型＋詰め替え構造を根治する。

**Files:**
- Modify: `app/client/src/screens/StartScreen.tsx:9-14`（型）, `:165`（quick カード）, `:182,190`（daily ボタン）
- Modify: `app/client/src/App.tsx:60-66`（onSelect）

**Interfaces:**
- Consumes: `MenuSource`（`app/client/src/screens/SessionRunner.tsx:18-20`、変更なし）。
- Produces: `StartSelection = { type: "session"; source: MenuSource } | { type: "free" } | { type: "library" } | { type: "placement" }`（`StartScreen.tsx`）。

- [ ] **Step 1: StartScreen で MenuSource を import し StartSelection を再定義**

`app/client/src/screens/StartScreen.tsx` の import に追加（`QuickDrillKind`/`RoleplayDomain` は QUICK_DRILLS の型付けで引き続き使うため api.ts からの import は残す）:

```ts
import { type MenuSource } from "./SessionRunner";
```

`StartSelection` 型（`:9-14`）を差し替え:

```ts
export type StartSelection =
  | { type: "session"; source: MenuSource }
  | { type: "free" }
  | { type: "library" }
  | { type: "placement" };
```

- [ ] **Step 2: StartScreen の onSelect 呼び出しを MenuSource 直渡しに**

quick カードの onClick（`:165`）を差し替え:

```tsx
              <button key={q.drillKey} className="drill-card" onClick={() => props.onSelect({ type: "session", source: { type: "quick", drill: q.drill, domain: q.domain } })}>
```

daily ボタン2箇所（`:182` と `:190`）の onClick を差し替え:

```tsx
          <button className="drill-card" onClick={() => props.onSelect({ type: "session", source: { type: "daily", minutes: 30 } })}>
```

```tsx
          <button className="drill-card" onClick={() => props.onSelect({ type: "session", source: { type: "daily", minutes: 60 } })}>
```

- [ ] **Step 3: App.onSelect を透過に縮小**

`app/client/src/App.tsx` の `onSelect`（`:60-66`）を差し替え:

```tsx
  function onSelect(sel: StartSelection) {
    if (sel.type === "free") setMode({ kind: "free" });
    else if (sel.type === "library") setMode({ kind: "library" });
    else if (sel.type === "placement") setMode({ kind: "placement" });
    else setMode({ kind: "session", source: sel.source });
  }
```

（`{ type: "quick", drill: sel.drill, domain: sel.domain }` / `{ type: "daily", minutes: sel.minutes }` の再構築が消え、`sel.source` をそのまま透過。domain 脱落は型的に不可能になる。）

- [ ] **Step 4: 検証ゲート**

```bash
cd app/client && bun run build
```
Expected: 0エラー / 成功。

- [ ] **Step 5: コミット**

```bash
cd <repo-root>
git add app/client/src/screens/StartScreen.tsx app/client/src/App.tsx
git commit -m "refactor: StartSelectionのsession系をMenuSource透過に一本化"
```

---

## Task 6: 共有 dates.ts で localYmd を一元化

クライアント3実装（StartScreen / SentencesScreen / App のインライン）の `localYmd` を共有モジュールに集約し、UTC 実装が混入して SRS due 比較が日付境界でずれる余地を消す。

**Files:**
- Create: `app/client/src/dates.ts`
- Modify: `app/client/src/screens/StartScreen.tsx:32-35`
- Modify: `app/client/src/screens/SentencesScreen.tsx:20-23`
- Modify: `app/client/src/App.tsx:157-159`

**Interfaces:**
- Produces: `localYmd(d: Date): string`（`app/client/src/dates.ts`）。

- [ ] **Step 1: 共有 dates.ts を作る（新規コード）**

`app/client/src/dates.ts`:

```ts
/**
 * ローカルタイムゾーンの YYYY-MM-DD。UTC の toISOString().slice(0,10) と違い日付境界でずれない。
 * サーバ app/server/dates.ts の localYmd と同一セマンティクス（SRS の due 比較の一致に必要）。
 */
export function localYmd(d: Date): string {
  const p = (n: number) => String(n).padStart(2, "0");
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}`;
}
```

- [ ] **Step 2: StartScreen をインポートに差し替え**

`app/client/src/screens/StartScreen.tsx` のローカル `localYmd`（`:31-35`、コメント行含む）を削除し、import を追加:

```ts
import { localYmd } from "../dates";
```

使用箇所（`:70-71`）は不変（`localYmd(date)` / `localYmd(today)`）。

- [ ] **Step 3: SentencesScreen をインポートに差し替え**

`app/client/src/screens/SentencesScreen.tsx` のローカル `localYmd`（`:20-23`）を削除し、import を追加:

```ts
import { localYmd } from "../dates";
```

使用箇所（`:90`）は不変。

- [ ] **Step 4: App の PracticeStat をインポートに差し替え**

`app/client/src/App.tsx` の import に追加:

```ts
import { localYmd } from "./dates";
```

PracticeStat 内のインライン `p`/`ymd`（`:157-158`）を削除し、`thisWeek` の算出（`:159`）を差し替え:

```tsx
  const thisWeek = days.filter((d) => d >= localYmd(weekAgo) && d <= localYmd(now)).length;
```

（`const now`/`const weekAgo` の宣言 `:154-156` は残す。）

- [ ] **Step 5: 検証ゲート**

```bash
cd app/client && bun run build
```
Expected: 0エラー / 成功。

- [ ] **Step 6: コミット**

```bash
cd <repo-root>
git add app/client/src/dates.ts app/client/src/screens/StartScreen.tsx app/client/src/screens/SentencesScreen.tsx app/client/src/App.tsx
git commit -m "refactor: クライアントのlocalYmdを共有dates.tsに一元化"
```

---

## 最終検証（全タスク完了後）

```bash
cd app && bun test && bun run typecheck
cd app/client && bun run build
```
Expected: `bun test` 全緑（ケース総数は着手前と同数以上）/ `typecheck` 0エラー / `build` 成功。HTTP契約・クライアント挙動は Reflection の二重POST解消を除き不変。
