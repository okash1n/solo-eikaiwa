# R4 + P4: クライアント神モジュール分割と練習系画面の i18n 一括対応 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** クライアントの神モジュール（`api.ts` / `i18n.ts` / `SentencesScreen.tsx`）をドメイン別に分割し、その i18n 基盤の上に練習系8画面＋サーバ生成メニュータイトルを EN/JA 対応させる。

**Architecture:** R4 は純リファクタ（挙動不変、ただし繰越の useExplain 統一による ShadowingScreen の「再試行」ボタン追加だけは小改善）。`api.ts` は re-export バレル（`api/index.ts`）で分割し、全画面の `import ... from "../api"` を無改修に保つ。`i18n.ts` は画面別サブ辞書の合成へ再構成し `drills` を union キー型に戻して型検査を復活。P4 はサーバがメニュータイトルを `titleKey`＋`topicTitle`（HTTP additive）で返し、クライアントが `STR[lang].menuTitle` で組み立てる方式へ変え、8画面の日本語ハードコードを辞書化する。

**Tech Stack:** Bun / TypeScript / React（Vite）/ bun:test。サーバとクライアントは別パッケージ（別 tsconfig・別テストランナー）。

## Global Constraints

- **検証ゲート（各タスク末尾で該当するものを実行し、緑を確認してからコミット）:**
  - サーバのユニットテスト: `cd app && bun test`（36ファイル・347テスト。**クライアントテストは含まれない**）
  - サーバの型検査: `cd app && bun run typecheck`（`tsc --noEmit`・`server/` のみ）
  - クライアントのユニットテスト: `cd app/client && bun test`（`client/src/**/*.test.ts` のみ。**`cd app && bun test` では走らない** — bun はネストパッケージへ降りないため）
  - クライアントの型検査＋ビルド: `cd app/client && bun run build`（`tsc --noEmit && vite build`・`client/src` を型検査）
- **挙動契約:**
  - R4（Task 1〜5）は純リファクタ。**JA・EN いずれの UI 出力も従来と一字一句同一**。唯一の例外は ShadowingScreen の解説取得失敗時（Task 4）で、旧実装はエラー文をそのまま解説本文に流用し再試行不能だったのを、useExplain 統一でエラー表示＋「再試行」ボタンに変える（spec §5 R4 の許容改善）。
  - P4（Task 6〜8）は文言の辞書化のみで画面挙動不変。EN 表示は新規追加のため回帰基準は「**JA 表示が従来と一字一句同一**」。
- **研究制約:** 情報的フィードバックのトーンを崩さない。EN 文言も判定・警告調（"wrong" 等）を持ち込まず、中立・情報提示に徹する。
- **i18n 規約:** EN 既定・JA トグルの既存挙動（`loadLang`/`saveLang`、`App` の EN/日本語トグル）は不変。lang は既存の prop ドリリング方式に合わせて配る（context 化はしない）。
- **サーバ規約:** メニュータイトル変更は R1 ルータ規約に沿い HTTP additive（`title` は残し `titleKey`/`topicTitle` を追加）。既存テストの意図的更新は各タスクで明示する。
- **リポジトリ規約:** `00-` 始まりディレクトリに言及しない。`data/` や royal300 に触れない。コミットは Conventional Commits（1タスク1コミット）。

**このプランで新規追加する i18n キー（型 `Strings` へ合成する画面別サブ辞書）:** `menuTitle` / `session`（Task 6）、`warmup` / `ftt432` / `reflection` / `chunkList`（Task 7）、`shadowing` / `library` / `roleplay` / `freeTalkScreen`（Task 8）。`drills` は `Record<DrillKey, …>` へ（Task 2）。

**明示的にスコープ外（今回は触れない・別 PR 送り）と根拠:**
- `App.tsx` のサイドバー「← メニューに戻る」(App.tsx:91) と 3 種のヘルス/サーバ Banner（App.tsx:109/112/115）、`aria-label="Text size"/"Language"`（App.tsx:94/100）。→ アプリ外殻の文言で「練習系8画面」に含まれない。
- `ChunkPlaceholderScreen.tsx` の日本語（M3 プレースホルダ）。→ 8画面（ChunkList ≠ ChunkPlaceholder）に含まれない。
- `SentencesScreen` 一覧の SRS バッジ `st${stage} ・ ${due}` の中黒「・」（EN 表示でも残る、監査で「軽微」）。→ 8画面の辞書化対象外。
- `FourThreeTwoScreen` の `LISTENERS`（ftt432 の聞き手指示・英語ハードコード, 432:19-23）。→ 現状 JA モードでも英語表示のため、辞書化して JA 訳を入れると「JA 一字一句同一」を破る。回帰基準維持のため据え置き（Task 7 で明記）。
- `showJaFromPrep` は spec が「3箇所」と見積もるが、`prep.hintDefault` を参照する実サイトは 2 箇所のみ（WarmupReadingScreen:45 / FourThreeTwoScreen:278）。Task 5 で 2 箇所に適用する。

---

## File Structure

**Task 1（api 分割）で新規作成する `app/client/src/api/`（`api.ts` は削除し、同名ディレクトリへ）:**
- `api/http.ts` — 共有 `extractErrorMessage`
- `api/system.ts` — `Health` / `getHealth` / `sttUpload`
- `api/converse.ts` — `converse` / `ttsFetch` / `sessionStart` / `sessionEnd` / `sessionEndKeepalive` / `sendSessionEvent`
- `api/menu.ts` — `ContentItem` / `MenuBlock` / `Menu` / `QuickDrillKind` / `RoleplayDomain` / `fetchMenu` / `fetchQuickMenu`
- `api/coach.ts` — `AeItem` / `AeFeedback` / `Reflection` / `PrepPack` / `PhraseHint` / `fetchAeFeedback` / `fetchModelTalk` / `fetchReflection` / `fetchPrepPack`(+`prepPackCache`) / `fetchTalkExplanation` / `fetchUtteranceTranslation` / `fetchPhraseHints` / `fetchFixExplanation`
- `api/progress.ts` — `LevelProposal` / `ProgressSummary` / 進捗 Pub/Sub（`onProgressUpdate`/`notifyProgress`）/ 共通ラッパ `postForSummary` / `fetchProgressSummary` / `progressBlockStart` / `progressBlockXp` / `progressLevelAction` / `fetchPracticeDays`
- `api/settings.ts` — `Settings` / `fetchSettings` / `saveSettings`
- `api/library.ts` — `ModelTalkEntry` / `fetchModelTalkLibrary`
- `api/tts.ts` — `playTtsCached`(+`ttsBlobCache`) / `prefetchModelTalkAudio`(+`modelTalkCache`)
- `api/sentences.ts` — `SentenceSrs` / `SentenceItem` / `ChunkSrs` / `ChunkQueueItem` / `SentenceQueueItem` / `QueueItem` / `ChunkListItem` / `fetchChunks` / `gradeChunk` / `deleteChunk` / `fetchSentences` / `fetchSentenceQueue` / `gradeSentence` / `fetchSentenceExplanation`
- `api/placement.ts` — `PlacementTaskDef` / `PlacementResult` / `PlacementLatest` / `fetchPlacementTasks` / `submitPlacement` / `confirmPlacement`（`postForSummary` 経由）/ `fetchPlacementLatest`
- `api/metrics.ts` — `DayMetrics` / `MetricsSummary` / `fetchMetricsSummary`
- `api/assessment.ts` — `MonthlyReport` / `MonthlyReportPreview` / `fetchLatestMonthlyReport` / `fetchMonthlyReportList` / `requestMonthlyReport`
- `api/index.ts` — バレル（`export * from "./system"` …全モジュール）。既存の `import … from "../api"` は無改修でこの index に解決される。
- `api/progress.test.ts` — 新規（notify 自動化の pub/sub テスト）

**新規作成する共有フック/ヘルパ:**
- `app/client/src/usePlayRow.ts`（Task 3）— 行 TTS 再生イディオムのフック
- `app/client/src/useExplain.ts`（Task 4）— 「もっと詳しく/解説」の非同期状態フック

**分割する画面:**
- `app/client/src/screens/SentencesScreen.tsx` → `PracticeTab.tsx` / `BrowseTab.tsx` を切り出し（Task 3）

**設計判断（要約）:**
- **api 分割の import 戦略 = バレル再エクスポート。** `api.ts` を消して同名 `api/` ディレクトリ＋`api/index.ts` を置くと、全 11 画面＋App の `from "../api"` はディレクトリ index へ解決され無改修。一括張り替え（数十ファイル）より遥かに低リスク。
- **i18n 合成構造 = 単一 `Strings` を画面別 named 型の intersection へ。** `STR` リテラル値は不変（JA/EN 一字一句同一）。P4 は `& WarmupStrings & …` を足すだけで拡張でき、typecheck が EN/JA 両方の網羅を強制する。
- **P4 の分割方針 = 3タスク。** Task 6（サーバ titleKey＋SessionRunner 組み立て＋lang 配線＋テスト）→ Task 7（辞書バッチ1: Warmup/4-3-2/Reflection/ChunkList）→ Task 8（辞書バッチ2: Shadowing/Library/Roleplay/FreeTalk）。spec §4「1タスクで一括より2分割が安全」に従い辞書化を 2 バッチへ。

---

## Task 1: api.ts をドメイン別に分割し notifyProgress を共通ラッパで自動化

**Files:**
- Create: `app/client/src/api/http.ts`, `system.ts`, `converse.ts`, `menu.ts`, `coach.ts`, `progress.ts`, `settings.ts`, `library.ts`, `tts.ts`, `sentences.ts`, `placement.ts`, `metrics.ts`, `assessment.ts`, `index.ts`（すべて `app/client/src/api/` 配下）
- Create: `app/client/src/api/progress.test.ts`
- Delete: `app/client/src/api.ts`

**Interfaces:**
- Produces: `import { … } from "../api"` の公開 API は**完全に不変**（型・関数名・シグネチャすべて）。加えて `api/progress.ts` から `postForSummary(url: string, body: unknown) => Promise<ProgressSummary>` を内部エクスポート（同モジュール内利用のみ・バレルからは出さなくてよい）。
- Consumes: なし（既存コードの移動）。

各ファイルの中身は、現行 `app/client/src/api.ts` の該当宣言を **そのまま移動**する（File Structure の割り当てどおり）。モジュール間依存は import で解決する:
- `http.ts` は他に依存なし。全 fetch 系モジュールは `import { extractErrorMessage } from "./http"`。
- `tts.ts` は `import { ttsFetch } from "./converse"`, `import { fetchModelTalk } from "./coach"`, `import { playBlob } from "../audio"`。
- `placement.ts` は `import { type ProgressSummary, postForSummary } from "./progress"`。
- 型の相互参照（例: `ChunkSrs = SentenceSrs`）は同一ファイル内に収まる（すべて `sentences.ts`）。

- [ ] **Step 1: 進捗 Pub/Sub の共通ラッパ挙動を先にテストで固定する（`api/progress.ts` を作る前に、テストを置く）**

`app/client/src/api/progress.test.ts` を作成:

```ts
import { afterEach, describe, expect, mock, test } from "bun:test";
import { onProgressUpdate, progressBlockXp, progressLevelAction, type ProgressSummary } from "./progress";

const SUMMARY: ProgressSummary = {
  level: 3, xp: 10, xpIntoLevel: 10, xpToNext: 5, stage: 1, difficultyMaxed: false, proposal: null,
};

const realFetch = globalThis.fetch;
afterEach(() => { globalThis.fetch = realFetch; });

function stubFetchOk(body: unknown): void {
  globalThis.fetch = mock(async () =>
    new Response(JSON.stringify(body), { status: 200, headers: { "content-type": "application/json" } }),
  ) as unknown as typeof fetch;
}

describe("progress の summary 自動通知", () => {
  test("progressBlockXp は summary を購読者へ通知し、同じ値を返す", async () => {
    stubFetchOk(SUMMARY);
    const seen: ProgressSummary[] = [];
    const unsub = onProgressUpdate((s) => seen.push(s));
    const returned = await progressBlockXp(2, 1);
    unsub();
    expect(returned).toEqual(SUMMARY);
    expect(seen).toEqual([SUMMARY]);
  });

  test("progressLevelAction も同じラッパで通知する", async () => {
    stubFetchOk(SUMMARY);
    const seen: ProgressSummary[] = [];
    const unsub = onProgressUpdate((s) => seen.push(s));
    await progressLevelAction("accept");
    unsub();
    expect(seen).toEqual([SUMMARY]);
  });
});
```

- [ ] **Step 2: テストが失敗（モジュール未存在）することを確認**

Run: `cd app/client && bun test src/api/progress.test.ts`
Expected: FAIL（`Cannot find module "./progress"`）

- [ ] **Step 3: `api/http.ts` を作成（`extractErrorMessage` を移動）**

```ts
/**
 * 非2xxレスポンスからエラーメッセージを取り出す。サーバ停止時にプロキシ/ブラウザが
 * 返すHTMLなど非JSONボディでも例外を投げず、`HTTP <status>` にフォールバックする。
 */
export async function extractErrorMessage(res: Response): Promise<string> {
  try {
    const body = (await res.json()) as { error?: string };
    if (body?.error) return body.error;
  } catch {
    // 非JSONボディ（サーバ停止時のエラーページ等）はフォールバックメッセージを使う
  }
  return `HTTP ${res.status}`;
}
```

- [ ] **Step 4: `api/progress.ts` を作成（進捗系を移動し、`postForSummary` ラッパで notify を自動化）**

現行 `api.ts` の `LevelProposal`/`ProgressSummary`/`progressListeners`/`onProgressUpdate`/`notifyProgress`/`fetchProgressSummary`/`progressBlockStart`/`fetchPracticeDays` をこのファイルへ移動。`progressBlockXp` と `progressLevelAction` は下記のとおり `postForSummary` を使う形に置き換える（手動 `notifyProgress` 呼び出しを消す）:

```ts
import { extractErrorMessage } from "./http";

export type LevelProposal = {
  kind: "up" | "down";
  toLevel: number;
  rationale: { xpReached?: boolean; practicedDays14?: number; completionRate?: number | null; fttAborts?: number };
};
export type ProgressSummary = {
  level: number; xp: number; xpIntoLevel: number; xpToNext: number;
  stage: number; difficultyMaxed: boolean; proposal: LevelProposal | null;
};

/**
 * summary 更新の軽量Pub/Sub。サイドバーのゲージ等、複数箇所で summary を表示する画面が
 * XP付与・レベル操作の直後に最新値へ追従できるようにする（再取得のポーリングは行わない）。
 */
let progressListeners: Array<(s: ProgressSummary) => void> = [];

/** 購読する。戻り値を呼ぶと購読解除される */
export function onProgressUpdate(fn: (s: ProgressSummary) => void): () => void {
  progressListeners.push(fn);
  return () => { progressListeners = progressListeners.filter((f) => f !== fn); };
}

export function notifyProgress(s: ProgressSummary): void {
  for (const fn of progressListeners) fn(s);
}

/**
 * summary を返す POST の共通ラッパ。成功時に必ず notifyProgress を呼ぶことで、
 * XP付与・レベル操作・プレースメント確定の3経路で手書きしていた notify 結線を1箇所に集約する。
 */
export async function postForSummary(url: string, body: unknown): Promise<ProgressSummary> {
  const res = await fetch(url, {
    method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body),
  });
  if (!res.ok) throw new Error(await extractErrorMessage(res));
  const summary = (await res.json()) as ProgressSummary;
  notifyProgress(summary);
  return summary;
}

export async function fetchProgressSummary(): Promise<ProgressSummary> {
  const res = await fetch("/api/progress/summary");
  if (!res.ok) throw new Error(`progress summary failed: ${await extractErrorMessage(res)}`);
  return res.json();
}

export async function progressBlockStart(kind: string): Promise<number> {
  const res = await fetch("/api/progress/block-start", {
    method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ kind }),
  });
  if (!res.ok) throw new Error(`block-start failed: ${await extractErrorMessage(res)}`);
  return ((await res.json()) as { attemptId: number }).attemptId;
}

export function progressBlockXp(amount: number, attemptId: number | null): Promise<ProgressSummary> {
  return postForSummary("/api/progress/xp", { kind: "block", amount, attemptId: attemptId ?? undefined });
}

export function progressLevelAction(
  action: "accept" | "decline" | "set", level?: number,
): Promise<ProgressSummary> {
  return postForSummary("/api/progress/level", { action, level });
}

export async function fetchPracticeDays(): Promise<string[]> {
  const res = await fetch("/api/progress/days");
  if (!res.ok) throw new Error(`practice days failed: ${await extractErrorMessage(res)}`);
  return ((await res.json()) as { days: string[] }).days;
}
```

注: 旧 `progressBlockXp`/`progressLevelAction` のエラー文言（`xp failed:` / `level action failed:`）は `postForSummary` の `extractErrorMessage` 直返しに変わる。これらの文言を assert している既存テストは無い（クライアント側にテスト無し）ため回帰は起きない。

- [ ] **Step 5: 残りのモジュールを作成（現行 `api.ts` から該当宣言を File Structure どおり移動）**

`system.ts` / `converse.ts` / `menu.ts` / `coach.ts` / `settings.ts` / `library.ts` / `tts.ts` / `sentences.ts` / `metrics.ts` / `assessment.ts` を作成し、各宣言を移動する。各ファイル冒頭に `import { extractErrorMessage } from "./http";` を置く（fetch を持つモジュールのみ）。`tts.ts` は追加で `import { playBlob } from "../audio";`, `import { ttsFetch } from "./converse";`, `import { fetchModelTalk } from "./coach";`。`placement.ts` を作成し、`confirmPlacement` は `postForSummary` を使う形へ置き換える:

```ts
import { type ProgressSummary, postForSummary } from "./progress";
import { extractErrorMessage } from "./http";

export type PlacementTaskDef = {
  id: string; durationSec: number; instructionEn: string; instructionJa: string; promptText: string;
};
export type PlacementResult = { stage: number; startLevel: number; rationale: string };
export type PlacementLatest = { id: number; ts: string; stage: number; startLevel: number; rationale: string } | null;

export async function fetchPlacementTasks(): Promise<PlacementTaskDef[]> {
  const res = await fetch("/api/placement/tasks");
  if (!res.ok) throw new Error(`placement tasks failed: ${await extractErrorMessage(res)}`);
  return ((await res.json()) as { tasks: PlacementTaskDef[] }).tasks;
}

export async function submitPlacement(
  tasks: Array<{ taskId: string; transcript: string; durationSec: number; wordCount: number }>,
): Promise<PlacementResult> {
  const res = await fetch("/api/placement/submit", {
    method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ tasks }),
  });
  if (!res.ok) throw new Error(`placement submit failed: ${await extractErrorMessage(res)}`);
  return res.json();
}

export function confirmPlacement(accept: boolean, level?: number): Promise<ProgressSummary> {
  return postForSummary("/api/placement/confirm", { accept, level });
}

export async function fetchPlacementLatest(): Promise<PlacementLatest> {
  const res = await fetch("/api/placement/latest");
  if (!res.ok) throw new Error(`placement latest failed: ${await extractErrorMessage(res)}`);
  return ((await res.json()) as { result: PlacementLatest }).result;
}
```

（`confirmPlacement` も旧 `placement confirm failed:` 文言が `postForSummary` 直返しへ変わる。assert 元のテストは無い。）

- [ ] **Step 6: `api/index.ts`（バレル）を作成**

```ts
export * from "./http";
export * from "./system";
export * from "./converse";
export * from "./menu";
export * from "./coach";
export * from "./progress";
export * from "./settings";
export * from "./library";
export * from "./tts";
export * from "./sentences";
export * from "./placement";
export * from "./metrics";
export * from "./assessment";
```

- [ ] **Step 7: 旧 `api.ts` を削除**

Run: `git rm app/client/src/api.ts`

- [ ] **Step 8: pub/sub テストが緑になることを確認**

Run: `cd app/client && bun test src/api/progress.test.ts`
Expected: PASS（2 tests）

- [ ] **Step 9: クライアント全体・サーバの検証ゲート**

Run: `cd app/client && bun test` → Expected: PASS（cloze/support/progress = 15 tests）
Run: `cd app/client && bun run build` → Expected: 型エラー0・ビルド成功（全画面の `from "../api"` がバレルへ解決）
Run: `cd app && bun test` → Expected: 347 pass（サーバ無影響）

- [ ] **Step 10: コミット**

```bash
git add app/client/src/api docs/superpowers/plans
git commit -m "refactor: api.ts をドメイン別モジュールへ分割し進捗通知を共通ラッパで自動化"
```

---

## Task 2: i18n.ts を画面別サブ辞書の合成へ再構成し drills を union キー型へ

**Files:**
- Modify: `app/client/src/i18n.ts:13-99`（`type Strings` の再構成と `DrillKey` 追加）
- Modify: `app/client/src/screens/StartScreen.tsx:18`（`QUICK_DRILLS` の `drillKey: string` → `drillKey: DrillKey`）

**Interfaces:**
- Produces: `export type DrillKey = "warmup" | "ftt-mini" | "shadowing" | "roleplay-daily" | "roleplay-business" | "roleplay-it";`。`type Strings` は画面別 named 型の intersection（値は不変）。
- Consumes: なし。

- [ ] **Step 1: `i18n.ts` の `type Strings`（13-99行）を named 型の合成へ置き換える**

現行の単一 `type Strings = { … }` を削除し、以下に置換する（**各メンバの内容は現行と完全一致**。`drills` のみ `Record<string, …>` → `Record<DrillKey, …>`）:

```ts
export type DrillKey =
  | "warmup" | "ftt-mini" | "shadowing"
  | "roleplay-daily" | "roleplay-business" | "roleplay-it";

type NavStrings = { nav: { home: string; placement: string; free: string; library: string; sentences: string; progress: string } };
type UiScaleStrings = { uiScale: { small: string; medium: string; large: string; xlarge: string } };
type SupportStrings = {
  support: {
    title: string;
    presetAuto: string; presetMore: string; presetLess: string;
    jaHint: string; modelTalk: string; cloze: string;
    optAuto: string; optOn: string; optOff: string;
  };
};
type StatStrings = { stat: { title: string; thisWeekUnit: string; total: (n: number) => string } };
type HeroStrings = { hero: { title: string; date: (d: Date) => string } };
type QuickStrings = { quick: { label: string; note: string } };
type IntensiveStrings = { intensive: { label: string; note: string } };
type DrillsStrings = { drills: Record<DrillKey, { title: string; minutes: string; desc: string }> };
type SessionCardStrings = {
  fullSession: { title: string; minutes: string; desc: string };
  shortSession: { title: string; minutes: string; desc: string };
};
type CalendarStrings = { calendar: { title: string; practiced: string; notYet: string } };
type FreeTalkHeaderStrings = { freeTalk: { title: string; desc: string } };
type ProgressStrings = {
  progress: {
    levelLabel: (n: number) => string;
    toNext: (xp: number) => string;
    maxed: string;
    editTitle: string; editSave: string; editCancel: string; editError: string;
    gaugeLabel: string;
    upTitle: string; upBody: (toLevel: number) => string;
    downTitle: string; downBody: (toLevel: number) => string;
    xpReached: string;
    practicedDays: (n: number) => string;
    completionRate: (pct: number) => string;
    fttAborts: (n: number) => string;
    acceptUp: string; acceptDown: string; decline: string;
    actionError: string;
    title: string;
    speakingTime: string; speakingMinUnit: string;
    articulation: string; articulationUnit: string;
    pauseCard: string; repetitionCard: string; weekOverWeek: string;
    levelHistory: string; currentLevel: (n: number) => string;
    empty: string;
    loading: string; retry: string;
    monthlyReview: string;
    mrGenerate: string; mrGenerating: string;
    mrEmpty: string; mrError: string;
    mrPast: string;
    mrDate: (ymd: string) => string;
  };
};
type PlacementStrings = {
  placement: {
    cardTitleNew: string; cardBodyNew: string;
    cardTitleMonthly: string; cardBodyMonthly: string;
    introTitle: string; introBody: string; introStart: string;
    taskLabel: (i: number, total: number) => string;
    promptLabel: string;
    recordStart: string; recordStop: string; transcribing: string;
    yourAnswer: string; redo: string; next: string; submit: string;
    submitting: string; submitError: string; retry: string;
    resultTitle: string; resultStage: (stage: number) => string;
    resultStartAt: (level: number) => string; chooseOwn: string; notNow: string;
    chooseLabel: string; apply: string; confirmError: string;
    xpNote: string;
    micError: (detail: string) => string;
  };
};
type SentencesStrings = {
  sentences: {
    heroTitle: string; heroDesc: string;
    tabPractice: string; tabBrowse: string;
    hideNoteLabel: string;
    loading: string; retry: string;
    remaining: (left: number, graded: number) => string;
    sayItFirst: string;
    showCloze: string; showAnswer: string;
    clozeHint: string;
    playAgain: string;
    explainMore: string; explainLoading: string; explainError: string;
    gradeGood: string; gradeSoso: string; gradeBad: string;
    doneTitle: (n: number) => string;
    dueTomorrow: (n: number) => string;
    doneBody: string;
    filterAll: string;
    domain: { daily: string; business: string; it: string };
    srsNew: string;
    playAria: (no: number) => string;
    chunkLabel: string;
    chunkSayIt: string;
    myChunks: string;
    deleteConfirm: string;
    deleteAria: (id: number) => string;
    playChunkAria: (id: number) => string;
  };
};

type Strings =
  & NavStrings & UiScaleStrings & SupportStrings & StatStrings & HeroStrings
  & QuickStrings & IntensiveStrings & DrillsStrings & SessionCardStrings
  & CalendarStrings & FreeTalkHeaderStrings & ProgressStrings & PlacementStrings & SentencesStrings;
```

`export const STR: Record<Lang, Strings> = { … }` 以降のリテラル本体（105-334行）は**一切変更しない**。

- [ ] **Step 2: `StartScreen.tsx` の `QUICK_DRILLS` を `DrillKey` で型付け**

`app/client/src/screens/StartScreen.tsx:6` の import を `import { STR, type DrillKey, type Lang } from "../i18n";` にし、18行の配列型を変更:

```ts
const QUICK_DRILLS: Array<{ drill: QuickDrillKind; domain?: RoleplayDomain; drillKey: DrillKey; icon: string; tile: string }> = [
```

（配列要素の `drillKey` 文字列値は現行と同一。これで `const d = t.drills[q.drillKey]`(158行) が `DrillKey` インデックスとして型安全になる。）

- [ ] **Step 3: 型検査で drills union が効くこと・値が不変であることを確認**

Run: `cd app/client && bun run build`
Expected: 型エラー0・ビルド成功

- [ ] **Step 4: 回帰確認（JA/EN 出力不変）とクライアントテスト**

Run: `cd app/client && bun test`
Expected: PASS（15 tests・辞書構造変更のみで値は不変）

- [ ] **Step 5: コミット**

```bash
git add app/client/src/i18n.ts app/client/src/screens/StartScreen.tsx
git commit -m "refactor: i18n Strings を画面別サブ辞書の合成へ再構成し drills を union キー型に戻す"
```

---

## Task 3: SentencesScreen を PracticeTab/BrowseTab へファイル分割し行TTS再生をフック化

**Files:**
- Create: `app/client/src/usePlayRow.ts`
- Create: `app/client/src/screens/PracticeTab.tsx`
- Create: `app/client/src/screens/BrowseTab.tsx`
- Modify: `app/client/src/screens/SentencesScreen.tsx`（親コンポーネントのみ残す）
- Modify: `app/client/src/screens/WarmupReadingScreen.tsx:28-39`（`playChunk` を `usePlayRow` へ）
- Modify: `app/client/src/screens/LibraryScreen.tsx:13-32`（`play` を `usePlayRow` へ）
- Modify: `app/client/src/screens/FourThreeTwoScreen.tsx:70,159-170`（`playChunk` を `usePlayRow` へ）

**Interfaces:**
- Produces: `export function usePlayRow<K>(): { playingKey: K | null; error: string; play: (key: K, text: string) => Promise<void> }`。`PracticeTab` / `BrowseTab`（`{ lang: Lang; … }` を受ける、現行と同一 props）。
- Consumes: Task 1 の `playTtsCached`（`from "../api"`）。

**行TTS再生イディオムの5サイト:** SentencesScreen BrowseTab の `play`(sentence) / `playChunk`(chunk)、WarmupReadingScreen `playChunk`、FourThreeTwoScreen `playChunk`、LibraryScreen `play`。

- [ ] **Step 1: `usePlayRow.ts` を作成**

```ts
import { useEffect, useRef, useState } from "react";
import { playTtsCached } from "./api";
import { stopPlayback } from "./audio";

/**
 * リスト各行の🔊再生（テキスト単位TTS）の共通フック。再生中の行キーを保持し、
 * 再生失敗を error 文字列へ整形する。アンマウント時に stopPlayback する。
 * K は行を一意に識別する値の型（例文の no・チャンクの id・チャンクリストの index 等）。
 */
export function usePlayRow<K>(): { playingKey: K | null; error: string; play: (key: K, text: string) => Promise<void> } {
  const [playingKey, setPlayingKey] = useState<K | null>(null);
  const [error, setError] = useState("");
  const aliveRef = useRef(true);
  useEffect(() => {
    aliveRef.current = true;
    return () => { aliveRef.current = false; stopPlayback(); };
  }, []);
  async function play(key: K, text: string) {
    setError("");
    setPlayingKey(key);
    try {
      await playTtsCached(text);
    } catch (err) {
      if (!aliveRef.current) return;
      setError(err instanceof Error ? err.message : String(err));
    } finally {
      if (aliveRef.current) setPlayingKey(null);
    }
  }
  return { playingKey, error, play };
}
```

- [ ] **Step 2: `PracticeTab.tsx` を作成（現行 `SentencesScreen.tsx` の `PracticeTab` 関数＋定数を移動）**

現行 `SentencesScreen.tsx` の以下を移動する: 定数 `NEW_PER_DAY`(16行)、`type Phase`(20行)、`PracticeTab` 関数（30-186行）。ファイル冒頭 import:

```tsx
import { useEffect, useRef, useState } from "react";
import {
  fetchSentenceExplanation, fetchSentenceQueue, fetchSentences, gradeChunk, gradeSentence, playTtsCached,
} from "../api";
import { stopPlayback } from "../audio";
import { clozeText } from "../cloze";
import { STR, type Lang } from "../i18n";
import { useLoad } from "../useLoad";
import { Banner } from "../ui/Banner";
import { Button } from "../ui/Button";
import { Card } from "../ui/Card";
import { localYmd } from "../dates";
```

`PracticeTab` 本体は変更しない（`reveal`/`playAgain` は単一アイテムの再生で「行リスト」ではないため `usePlayRow` の対象外。現状の `playTtsCached(current.en)` のままにする）。`export function PracticeTab(...)` にする。

- [ ] **Step 3: `BrowseTab.tsx` を作成（現行 `BrowseTab` を移動し、2つの再生を `usePlayRow` へ）**

現行 `SentencesScreen.tsx` の `BrowseTab`（188-328行）を移動。`play`/`playChunk`/`playingNo`/`playingChunkId`/`errorMsg`(再生分)/`aliveRef`/`stopPlayback` の useEffect を `usePlayRow` に置換する。移動先の要点:

```tsx
import { useState } from "react";
import {
  deleteChunk, fetchChunks, fetchSentences, type ChunkListItem, type SentenceItem,
} from "../api";
import { STR, type Lang } from "../i18n";
import { useLoad } from "../useLoad";
import { usePlayRow } from "../usePlayRow";
import { Banner } from "../ui/Banner";
import { Button } from "../ui/Button";
import { Card } from "../ui/Card";

/** 行キー: 例文は no、チャンクは id。種別を混ぜないよう kind でタグ付けする */
type RowKey = { kind: "sentence"; no: number } | { kind: "chunk"; id: number };

export function BrowseTab({ lang }: { lang: Lang }) {
  const t = STR[lang].sentences;
  const load = useLoad(async () => {
    const all = await fetchSentences();
    let cs: ChunkListItem[] = [];
    try { cs = await fetchChunks(); } catch { /* ignore */ }
    return { items: all, chunks: cs };
  });
  const [filter, setFilter] = useState<"all" | SentenceItem["domain"]>("all");
  const [deletingId, setDeletingId] = useState<number | null>(null);
  const [deleteError, setDeleteError] = useState("");
  const [removedIds, setRemovedIds] = useState<number[]>([]);
  const row = usePlayRow<RowKey>();
  const anyPlaying = row.playingKey !== null;
  // …items/chunks の算出は現行どおり…
```

置換対応（現行 → 移動後）:
- `play(s)` 呼び出し → `row.play({ kind: "sentence", no: s.no }, s.en)`
- `playChunk(c)` 呼び出し → `row.play({ kind: "chunk", id: c.id }, c.en)`
- 例文行の `disabled={playingNo !== null || playingChunkId !== null}` → `disabled={anyPlaying}`（チャンク行も同様）
- 例文行アイコン `playingNo === s.no ? "🔊" : "▶"` → `row.playingKey?.kind === "sentence" && row.playingKey.no === s.no ? "🔊" : "▶"`
- チャンク行アイコン `playingChunkId === c.id ? "🔊" : "▶"` → `row.playingKey?.kind === "chunk" && row.playingKey.id === c.id ? "🔊" : "▶"`
- 再生失敗 Banner（現行 `errorMsg`）→ `row.error`。削除失敗は別 state `deleteError` に分離（`onDeleteChunk` の catch は `setDeleteError` を使う）。表示は `{(row.error || deleteError) && <Banner kind="error">{row.error || deleteError}</Banner>}`。

  注（挙動）: 現行は再生失敗と削除失敗が同一 `errorMsg` を共有し「後勝ち」。分離後は両方を OR 表示するため、JA 文言・表示位置は不変（同じ Banner・同じメッセージ）。同時失敗は稀で、いずれのメッセージも欠落しない点でむしろ改善。回帰基準（JA 一字一句同一）は満たす。

- [ ] **Step 4: `SentencesScreen.tsx` を親のみに縮小**

```tsx
import { useState } from "react";
import { STR, type Lang } from "../i18n";
import { resolveSupport, useSupport } from "../support";
import { PracticeTab } from "./PracticeTab";
import { BrowseTab } from "./BrowseTab";

const HIDE_NOTE_KEY = "sentences.hideNote";
type Tab = "practice" | "browse";

function loadHideNote(): boolean { return localStorage.getItem(HIDE_NOTE_KEY) === "1"; }
function saveHideNote(v: boolean): void { localStorage.setItem(HIDE_NOTE_KEY, v ? "1" : "0"); }

export function SentencesScreen({ lang }: { lang: Lang }) {
  const t = STR[lang].sentences;
  const [tab, setTab] = useState<Tab>("practice");
  const [hideNote, setHideNote] = useState(() => loadHideNote());
  const support = useSupport();
  const clozeDefault = resolveSupport(support.cloze, support.preset, false);

  function toggleHideNote() {
    setHideNote((v) => { saveHideNote(!v); return !v; });
  }

  return (
    <div className="stack">
      <div className="hero">
        <h2 className="hero-title">{t.heroTitle}</h2>
        <p className="hero-date">{t.heroDesc}</p>
      </div>
      <div className="filter-row sentences-toolbar">
        <button className={`filter-chip${tab === "practice" ? " is-active" : ""}`} onClick={() => setTab("practice")}>
          {t.tabPractice}
        </button>
        <button className={`filter-chip${tab === "browse" ? " is-active" : ""}`} onClick={() => setTab("browse")}>
          {t.tabBrowse}
        </button>
        <label className="hide-note-toggle text-sm text-muted">
          <input type="checkbox" checked={hideNote} onChange={toggleHideNote} />
          {t.hideNoteLabel}
        </label>
      </div>
      {tab === "practice" ? <PracticeTab lang={lang} hideNote={hideNote} clozeDefault={clozeDefault} /> : <BrowseTab lang={lang} />}
    </div>
  );
}
```

- [ ] **Step 5: WarmupReadingScreen の `playChunk` を `usePlayRow` へ**

`WarmupReadingScreen.tsx` の `playErr`/`playingIdx`/`aliveRef`/`playChunk`/`useEffect(stopPlayback)`（18-39行）を削除し、`usePlayRow<number>()` に置換:
- import 追加: `import { usePlayRow } from "../usePlayRow";`。`stopPlayback` の import と `useEffect`/`useRef`（この画面での唯一の用途が再生だったため）を削除。`useState` は他用途（`clozeStep`）で残す。
- 本体: `const playRow = usePlayRow<number>();`
- `<ChunkList … playingIdx={playingIdx} onPlay={playChunk} … />` → `playingIdx={playRow.playingKey} onPlay={(i, text) => playRow.play(i, text)}`
- `{playErr && <Banner kind="error">{playErr}</Banner>}` → `{playRow.error && <Banner kind="error">{playRow.error}</Banner>}`

- [ ] **Step 6: LibraryScreen の `play` を `usePlayRow` へ**

`LibraryScreen.tsx` の `errorMsg`/`playingId`/`aliveRef`/`play`/`useEffect`（13-32行）を削除し `usePlayRow<number>()` へ:
- import: `import { usePlayRow } from "../usePlayRow";`、`stopPlayback` import と `useEffect`/`useRef` を削除。
- `const row = usePlayRow<number>();`
- `onClick={() => play(e)}` → `onClick={() => row.play(e.id, e.text)}`
- `disabled={playingId !== null}` → `disabled={row.playingKey !== null}`
- `playingId === e.id ? "🔊 再生中…" : "▶"` → `row.playingKey === e.id ? "🔊 再生中…" : "▶"`
- 末尾の `{state.status === "ready" && errorMsg && …}` → `{state.status === "ready" && row.error && <Banner kind="error">{row.error}</Banner>}`

- [ ] **Step 7: FourThreeTwoScreen の `playChunk` を `usePlayRow` へ**

`FourThreeTwoScreen.tsx`:
- import 追加: `import { usePlayRow } from "../usePlayRow";`
- `const [playingIdx, setPlayingIdx] = useState<number | null>(null);`(70行) と `playChunk`(159-170行) を削除し、コンポーネント上部に `const playRow = usePlayRow<number>();` を追加。
- prep ビューの `<ChunkList … playingIdx={playingIdx} onPlay={playChunk} … />` → `playingIdx={playRow.playingKey} onPlay={(i, text) => playRow.play(i, text)}`
- prep ビュー末尾のエラー表示（315行）を `{prepState !== "error" && (errorMsg || playRow.error) && <Banner kind="error">{errorMsg || playRow.error}</Banner>}` に変更（チャンク再生失敗を従来と同じ prep エラー Banner 位置・同じ文言で表示。`errorMsg` は他要因のエラー用に残す）。

  注（挙動）: 現行は `playChunk` 失敗も汎用 `errorMsg` を使い prep Banner に出していた。分離後も同じ Banner・同じ文言で表示されるため JA 出力は不変。

- [ ] **Step 8: 検証ゲート**

Run: `cd app/client && bun run build` → Expected: 型エラー0・ビルド成功
Run: `cd app/client && bun test` → Expected: PASS（15 tests）
Run: `cd app && bun test` → Expected: 347 pass（サーバ無影響）

- [ ] **Step 9: コミット**

```bash
git add app/client/src/usePlayRow.ts app/client/src/screens
git commit -m "refactor: SentencesScreen を PracticeTab/BrowseTab へ分割し行TTS再生を usePlayRow へ集約"
```

---

## Task 4: useExplain フックを切り出し FixItem/AeItemView/Shadowing を統一

**Files:**
- Create: `app/client/src/useExplain.ts`
- Modify: `app/client/src/screens/ReflectionScreen.tsx:8-43`（`FixItem`）
- Modify: `app/client/src/screens/FourThreeTwoScreen.tsx:387-427`（`AeItemView`）
- Modify: `app/client/src/screens/ShadowingScreen.tsx:20-21,91-110`（explain state を旧センチネルから useExplain へ）

**Interfaces:**
- Produces: `export type ExplainState = { status: "idle" } | { status: "loading" } | { status: "error" } | { status: "done"; text: string };` と `export function useExplain(fetcher: () => Promise<string>): { state: ExplainState; request: () => void }`。
- Consumes: Task 1 の `fetchFixExplanation` / `fetchTalkExplanation`（`from "../api"`）。

- [ ] **Step 1: `useExplain.ts` を作成**

```ts
import { useEffect, useRef, useState } from "react";

/**
 * 「もっと詳しく／解説」ボタンの非同期状態機械。判別可能ユニオンで idle/loading/error/done を表し、
 * 旧来の文字列センチネル（"loading"/"error" と本文の混同・エラー文を本文へ流用して再試行不能）を排する。
 * アンマウント後の setState はガードする。
 */
export type ExplainState =
  | { status: "idle" }
  | { status: "loading" }
  | { status: "error" }
  | { status: "done"; text: string };

export function useExplain(fetcher: () => Promise<string>): { state: ExplainState; request: () => void } {
  const [state, setState] = useState<ExplainState>({ status: "idle" });
  const aliveRef = useRef(true);
  useEffect(() => {
    aliveRef.current = true;
    return () => { aliveRef.current = false; };
  }, []);
  function request() {
    setState({ status: "loading" });
    fetcher()
      .then((text) => { if (aliveRef.current) setState({ status: "done", text }); })
      .catch(() => { if (aliveRef.current) setState({ status: "error" }); });
  }
  return { state, request };
}
```

- [ ] **Step 2: ReflectionScreen の `FixItem` を useExplain で書き換え（文言は JA のまま）**

`ReflectionScreen.tsx` の import に `import { useExplain } from "../useExplain";` を足し、`FixItem`（8-43行）を置換:

```tsx
/** 直したい表現1件。「もっと詳しく」で fetchFixExplanation を呼び、解説を自分の state に保持する */
function FixItem({ fix }: { fix: { original: string; better: string } }) {
  const { state, request } = useExplain(() => fetchFixExplanation(fix.original, fix.better));
  return (
    <li>
      <s>{fix.original}</s> → <strong>{fix.better}</strong>
      {state.status === "idle" && <Button variant="ghost" onClick={request}>💡 もっと詳しく</Button>}
      {state.status === "loading" && <p className="text-sm text-muted">解説を書いています…</p>}
      {state.status === "error" && (
        <p className="text-sm text-muted">解説を取得できませんでした。<Button variant="ghost" onClick={request}>再試行</Button></p>
      )}
      {state.status === "done" && <p className="sentence-explain text-sm">{state.text}</p>}
    </li>
  );
}
```

（不要になった `useEffect`/`useRef`/`useState` の import は、ReflectionScreen 本体で他に使っていなければ整理する。`fetchReflection`/`fetchFixExplanation` は残す。）

- [ ] **Step 3: FourThreeTwoScreen の `AeItemView` を useExplain で書き換え（JA のまま）**

`FourThreeTwoScreen.tsx` の import に `import { useExplain } from "../useExplain";` を足し、`AeItemView`（387-427行）を置換:

```tsx
/** AE指摘1件。「もっと詳しく」で fetchFixExplanation を呼び、解説を自分の state に保持する */
function AeItemView({ item }: { item: { quote: string; issue: string; better: string; why_ja: string } }) {
  const { state, request } = useExplain(() => fetchFixExplanation(item.quote, item.better, item.issue));
  return (
    <li className="ae-item">
      {item.quote && (
        <div>
          <s>{item.quote}</s> → <strong>{item.better}</strong> <em>({item.issue})</em>
        </div>
      )}
      <div className="ae-why">{item.why_ja}</div>
      {item.quote && item.better && state.status === "idle" && (
        <Button variant="ghost" onClick={request}>💡 もっと詳しく</Button>
      )}
      {state.status === "loading" && <p className="text-sm text-muted">解説を書いています…</p>}
      {state.status === "error" && (
        <p className="text-sm text-muted">解説を取得できませんでした。<Button variant="ghost" onClick={request}>再試行</Button></p>
      )}
      {state.status === "done" && <p className="sentence-explain text-sm">{state.text}</p>}
    </li>
  );
}
```

- [ ] **Step 4: ShadowingScreen の explain を旧センチネルから useExplain へ統一（唯一の許容挙動変更: エラー時に再試行ボタンを出す）**

`ShadowingScreen.tsx`:
- import 追加: `import { useExplain } from "../useExplain";`
- 21行 `const [explain, setExplain] = useState<string | null>(null);` を削除し、`const explainer = useExplain(() => fetchTalkExplanation(text));` を（`text` state 宣言より後に）置く。
- 88-111行の explain 表示ブロックを置換:

```tsx
          {showScript && (
            <>
              <Card className="reading-text">{text}</Card>
              {explainer.state.status === "idle" && (
                <Button variant="ghost" onClick={explainer.request}>💡 日本語訳と解説</Button>
              )}
              {explainer.state.status === "loading" && <p className="text-sm text-muted">日本語訳と解説を書いています…</p>}
              {explainer.state.status === "error" && (
                <p className="text-sm text-muted">解説を取得できませんでした。もう一度お試しください。<Button variant="ghost" onClick={explainer.request}>再試行</Button></p>
              )}
              {explainer.state.status === "done" && <p className="sentence-explain text-sm">{explainer.state.text}</p>}
            </>
          )}
```

  挙動変更（許容）: 旧実装は取得失敗時にエラー文を解説本文へ入れて「💡 日本語訳と解説」ボタンが二度と出ず再試行不能だった。統一後はエラー表示＋「再試行」ボタンになる（spec §5 R4 の許容改善）。JA 文言「解説を取得できませんでした。もう一度お試しください。」は保持。

- [ ] **Step 5: 検証ゲート**

Run: `cd app/client && bun run build` → Expected: 型エラー0・ビルド成功
Run: `cd app/client && bun test` → Expected: PASS（15 tests）
Run: `cd app && bun test` → Expected: 347 pass

- [ ] **Step 6: コミット**

```bash
git add app/client/src/useExplain.ts app/client/src/screens/ReflectionScreen.tsx app/client/src/screens/FourThreeTwoScreen.tsx app/client/src/screens/ShadowingScreen.tsx
git commit -m "refactor: useExplain フックへ FixItem/AeItemView/Shadowing の解説状態を統一（Shadowing に再試行を追加）"
```

---

## Task 5: useSupport を useSyncExternalStore 化し loadSupport 検証テストと showJaFromPrep を追加

**Files:**
- Modify: `app/client/src/support.ts:7,74-89`（`useSupport` の実装と `showJaFromPrep` 追加）
- Modify: `app/client/src/support.test.ts`（`loadSupport` の検証テストを追加）
- Modify: `app/client/src/screens/WarmupReadingScreen.tsx:45`（`showJaFromPrep` へ）
- Modify: `app/client/src/screens/FourThreeTwoScreen.tsx:278`（`showJaFromPrep` へ）

**Interfaces:**
- Produces: `export function showJaFromPrep(support: SupportSettings, prep: { hintDefault: "ja" | "en" }): boolean`。`useSupport` は `useSyncExternalStore` ベースへ（戻り型 `SupportSettings` は不変）。
- Consumes: 既存 `getSupport` / `onSupportChange` / `resolveSupport`。

- [ ] **Step 1: `loadSupport` の検証テストを先に追加（現状の loadSupport でも通る＝回帰ガード）**

`support.test.ts` の先頭 import を差し替え、describe を追加:

```ts
import { afterEach, describe, expect, test } from "bun:test";
import { DEFAULT_SUPPORT, loadSupport, resolveSupport } from "./support";

function stubStorage(value: string | null): void {
  (globalThis as unknown as { localStorage: Storage }).localStorage = {
    getItem: () => value, setItem: () => {}, removeItem: () => {}, clear: () => {}, key: () => null, length: 0,
  } as Storage;
}
afterEach(() => { delete (globalThis as unknown as { localStorage?: Storage }).localStorage; });

describe("loadSupport", () => {
  test("保存値なしは全既定", () => {
    stubStorage(null);
    expect(loadSupport()).toEqual(DEFAULT_SUPPORT);
  });
  test("不正JSONは既定へフォールバックする", () => {
    stubStorage("{ not json");
    expect(loadSupport()).toEqual(DEFAULT_SUPPORT);
  });
  test("未知の preset / 不正 toggle は既定に丸め、妥当値は保持する", () => {
    stubStorage(JSON.stringify({ preset: "bogus", jaHint: "yes", modelTalk: true, cloze: false }));
    expect(loadSupport()).toEqual({ preset: "auto", jaHint: null, modelTalk: true, cloze: false });
  });
});
```

（既存の `describe("resolveSupport", …)` はそのまま残す。`DEFAULT_SUPPORT` は support.ts で既に export 済み。）

- [ ] **Step 2: テストが緑（現状 loadSupport の挙動を確認）**

Run: `cd app/client && bun test src/support.test.ts`
Expected: PASS（resolveSupport 3 + loadSupport 3）

- [ ] **Step 3: `useSupport` を `useSyncExternalStore` 化し `showJaFromPrep` を追加**

`support.ts` の import を `import { useSyncExternalStore } from "react";` に変更（`useEffect`/`useState` は他で未使用なら削除）。`useSupport`（74-78行）を置換し、末尾に `showJaFromPrep` を追加:

```ts
/** サイドバー・各画面で購読して最新設定に追従する React フック */
export function useSupport(): SupportSettings {
  // onSupportChange(subscribe) は () => void 引数のコールバックとしても呼べる。getSupport は
  // 変更まで同一参照（current）を返すため getSnapshot として安全（再レンダーループを起こさない）。
  return useSyncExternalStore(onSupportChange, getSupport, getSupport);
}

/**
 * 準備パックの日本語表示可否。個別トグル → preset → サーバの stage 既定（hintDefault）で解決する。
 * hintDefault の "ja"/"en" 反転ミスを1箇所に封じるための薄いヘルパ。
 */
export function showJaFromPrep(support: SupportSettings, prep: { hintDefault: "ja" | "en" }): boolean {
  return resolveSupport(support.jaHint, support.preset, prep.hintDefault === "ja");
}
```

- [ ] **Step 4: 2つの呼び出しサイトを `showJaFromPrep` へ**

- `WarmupReadingScreen.tsx:9` の import を `import { showJaFromPrep, useSupport } from "../support";` にし、45行を:
  ```tsx
  const showJa = prep ? showJaFromPrep(support, prep) : true;
  ```
- `FourThreeTwoScreen.tsx:13` の import を `import { getSupport, resolveSupport, showJaFromPrep, useSupport } from "../support";` にし、278行を:
  ```tsx
  const showJa = showJaFromPrep(support, prep);
  ```
  （432 は `resolveSupport`/`getSupport` を autoPlay 算出でまだ使うため import に残す。）

- [ ] **Step 5: 検証ゲート**

Run: `cd app/client && bun test` → Expected: PASS（18 tests）
Run: `cd app/client && bun run build` → Expected: 型エラー0・ビルド成功
Run: `cd app && bun test` → Expected: 347 pass

- [ ] **Step 6: コミット**

```bash
git add app/client/src/support.ts app/client/src/support.test.ts app/client/src/screens/WarmupReadingScreen.tsx app/client/src/screens/FourThreeTwoScreen.tsx
git commit -m "refactor: useSupport を useSyncExternalStore 化し loadSupport 検証テストと showJaFromPrep を追加"
```

---

## Task 6: サーバのメニュータイトルを titleKey+topicTitle で返し SessionRunner で組み立て

**Files:**
- Modify: `app/server/menu.ts:17,25-28,87-102,147,151-159`（`MenuBlock` 型・`MenuTitleKey`・各ブロックへ `titleKey`/`topicTitle` 付与）
- Modify: `app/client/src/i18n.ts`（`MenuTitleKey` 型・`menuTitle`/`session` サブ辞書を EN/JA 追加）
- Modify: `app/client/src/api/menu.ts`（`MenuBlock` に `titleKey?`/`topicTitle?`）
- Modify: `app/client/src/screens/SessionRunner.tsx`（`lang` prop・タイトル組み立て・自画面文言の辞書化）
- Modify: `app/client/src/App.tsx:118-120`（`SessionRunner` へ `lang` を渡す）
- Modify: `app/server/__tests__/menu.test.ts`（titleKey/topicTitle の assertion を追加）

**Interfaces:**
- Produces（サーバ→クライアント契約）: `MenuBlock` に `titleKey: MenuTitleKey`（サーバ必須）／クライアントは `titleKey?`（旧キャッシュ後方互換のため任意）、`topicTitle?: string`。`title`(JA) は据え置き。
- Produces（i18n）: `export type MenuTitleKey = "warmup" | "ftt" | "ftt-mini" | "roleplay-daily" | "roleplay-business" | "roleplay-it" | "shadowing" | "reflection";` と `menuTitle: Record<MenuTitleKey, (topicTitle: string) => string>`、`session` サブ辞書。
- Consumes: Task 2 の合成 `Strings`（新サブ辞書を `& MenuTitleStrings & SessionStrings` で合成）。

**タイトルの内訳（JA は現行サーバ出力と一字一句一致させること）:**

| titleKey | topicTitle | JA | EN |
|---|---|---|---|
| warmup | — | 音読ウォームアップ | Read-Aloud Warm-up |
| ftt | topic.title | `4/3/2: ${t}` | `4/3/2: ${t}` |
| ftt-mini | topic.title | `4/3/2ミニ: ${t}` | `4/3/2 Mini: ${t}` |
| roleplay-daily | scenario.title | `日常ロールプレイ: ${t}` | `Daily Role-play: ${t}` |
| roleplay-business | scenario.title | `ビジネスロールプレイ: ${t}` | `Business Role-play: ${t}` |
| roleplay-it | scenario.title | `ITロールプレイ: ${t}` | `IT Role-play: ${t}` |
| shadowing | topic.title | `シャドーイング: ${t}` | `Shadowing: ${t}` |
| reflection | — | 振り返り | Reflection |

- [ ] **Step 1: `menu.test.ts` に titleKey/topicTitle の assertion を先に追加（RED）**

`app/server/__tests__/menu.test.ts` の `describe("buildTodayMenu", …)` 内に追加:

```ts
  test("各ブロックが titleKey を持ち、topic 系は topicTitle を返す（title は従来の日本語のまま）", () => {
    const dirs = makeContentDirs();
    const menu = buildTodayMenu(60, { ...dirs, today: JULY5 });
    // makeContentDirs の s1 は domain 省略＝既定 "it" なので roleplay-it になる
    expect(menu.blocks.map((b) => b.titleKey)).toEqual([
      "warmup", "ftt", "roleplay-it", "shadowing", "reflection",
    ]);
    const ftt = menu.blocks[1];
    expect(ftt.topicTitle).toBe("Topic One");
    expect(ftt.title).toBe("4/3/2: Topic One"); // title(JA) は据え置き
    expect(menu.blocks[0].topicTitle).toBeUndefined(); // warmup は topicTitle なし
    expect(menu.blocks[4].topicTitle).toBeUndefined(); // reflection も無し
  });
```

`describe("buildQuickMenu", …)` 内に追加:

```ts
  test("quick メニューも titleKey/topicTitle を返す", () => {
    const dirs = makeContentDirs();
    const deps: MenuDeps = { ...dirs, today: JULY5 };
    expect(buildQuickMenu("warmup", deps).blocks[0].titleKey).toBe("warmup");
    expect(buildQuickMenu("ftt-mini", deps).blocks[0].titleKey).toBe("ftt-mini");
    expect(buildQuickMenu("ftt-mini", deps).blocks[0].topicTitle).toBe("Topic One");
    expect(buildQuickMenu("shadowing", deps).blocks[0].titleKey).toBe("shadowing");
    // s1 は domain 省略＝既定 "it" のため roleplay-it（domain 明示時のロールプレイは既存テスト 319/334 行が担保）
    expect(buildQuickMenu("roleplay", deps).blocks[0].titleKey).toBe("roleplay-it");
  });
```

- [ ] **Step 2: RED 確認**

Run: `cd app && bun test server/__tests__/menu.test.ts`
Expected: FAIL（`titleKey` が存在しない）

- [ ] **Step 3: `menu.ts` に `MenuTitleKey`・型拡張・各ブロックの titleKey/topicTitle を実装**

`menu.ts`:
- 17行の `MenuBlock` 型を拡張:
  ```ts
  export type MenuTitleKey =
    | "warmup" | "ftt" | "ftt-mini"
    | "roleplay-daily" | "roleplay-business" | "roleplay-it"
    | "shadowing" | "reflection";
  export type MenuBlock = {
    id: string; kind: BlockKind; title: string; titleKey: MenuTitleKey; topicTitle?: string;
    minutes: number; params: Record<string, unknown>;
  };
  ```
- 25-28行 `roleplayTitle` の下に domain→titleKey ヘルパを追加:
  ```ts
  export function roleplayTitleKey(scenario: ContentItem): MenuTitleKey {
    return scenario.domain === "daily" ? "roleplay-daily" : scenario.domain === "business" ? "roleplay-business" : "roleplay-it";
  }
  ```
- 60分/30分の blocks（87-102行）へ付与（`title` は現行のまま。以下は 60分版の例。30分版・quick も同様に付ける）:
  ```ts
  { id: "b1", kind: "warmup-reading", title: warmupTitle, titleKey: "warmup", minutes: 8, params: { topic: mainTopic } },
  { id: "b2", kind: "four-three-two", title: `4/3/2: ${mainTopic.title}`, titleKey: "ftt", topicTitle: mainTopic.title, minutes: 16, params: { topic: mainTopic, roundsSec: fttRoundsSec(level), modelTalkMode: prepParams(stage).modelTalk } },
  { id: "b3", kind: "roleplay", title: roleplayTitle(scenario), titleKey: roleplayTitleKey(scenario), topicTitle: scenario.title, minutes: 20, params: { scenario } },
  { id: "b4", kind: "shadowing", title: `シャドーイング: ${shadowTopic.title}`, titleKey: "shadowing", topicTitle: shadowTopic.title, minutes: 8, params: { topic: shadowTopic } },
  { id: "b5", kind: "reflection", title: "振り返り", titleKey: "reflection", minutes: 5, params: {} },
  ```
- 30分版 blocks（97-102行）: warmup→`titleKey: "warmup"`、ftt→`titleKey: "ftt", topicTitle: mainTopic.title`、roleplay→`titleKey: roleplayTitleKey(scenario), topicTitle: scenario.title`、reflection→`titleKey: "reflection"`。
- `buildQuickMenu`（147,151-159行）: roleplay→`{ …, title: roleplayTitle(scenario), titleKey: roleplayTitleKey(scenario), topicTitle: scenario.title, … }`、warmup→`titleKey: "warmup"`、ftt-mini→`{ …, title: \`4/3/2ミニ: ${topic.title}\`, titleKey: "ftt-mini", topicTitle: topic.title, … }`、shadowing→`{ …, title: \`シャドーイング: ${topic.title}\`, titleKey: "shadowing", topicTitle: topic.title, … }`。

- [ ] **Step 4: GREEN 確認**

Run: `cd app && bun test server/__tests__/menu.test.ts`
Expected: PASS（既存 + 追加テスト）。既存の roleplay `title` assertion（`日常ロールプレイ: Daily One` 等, 319/334行）は `title` を据え置いたため無改修で通る。

- [ ] **Step 5: クライアント `api/menu.ts` の `MenuBlock` 型を additive 拡張**

`api/menu.ts`（Task 1 で分割済み）の `MenuBlock` を:

```ts
import type { MenuTitleKey } from "../i18n";
export type MenuBlock = {
  id: string; kind: string; title: string;
  titleKey?: MenuTitleKey; topicTitle?: string;
  minutes: number;
  params: { topic?: ContentItem; scenario?: ContentItem; roundsSec?: number[]; modelTalkMode?: "auto" | "button" };
};
```

（`titleKey?` を任意にするのは、当日デプロイ直後にデプロイ前生成のキャッシュ（titleKey 無し）が同日中は配信されうるため。i18n.ts は api を import しないので循環しない。）

- [ ] **Step 6: `i18n.ts` に `MenuTitleKey`・`menuTitle`・`session` を追加**

型定義部に追加（Task 2 の合成に `& MenuTitleStrings & SessionStrings` を足す）:

```ts
export type MenuTitleKey =
  | "warmup" | "ftt" | "ftt-mini"
  | "roleplay-daily" | "roleplay-business" | "roleplay-it"
  | "shadowing" | "reflection";
type MenuTitleStrings = { menuTitle: Record<MenuTitleKey, (topicTitle: string) => string> };
type SessionStrings = {
  session: {
    building: string; retry: string; timerNote: string;
    finish: string; next: string;
    noTopic: string; noScenario: string; unknownBlock: (kind: string) => string;
  };
};
```

`STR.en` に追加:

```ts
    menuTitle: {
      warmup: () => "Read-Aloud Warm-up",
      ftt: (t) => `4/3/2: ${t}`,
      "ftt-mini": (t) => `4/3/2 Mini: ${t}`,
      "roleplay-daily": (t) => `Daily Role-play: ${t}`,
      "roleplay-business": (t) => `Business Role-play: ${t}`,
      "roleplay-it": (t) => `IT Role-play: ${t}`,
      shadowing: (t) => `Shadowing: ${t}`,
      reflection: () => "Reflection",
    },
    session: {
      building: "Building today's menu…", retry: "Retry", timerNote: "Move on at a natural stopping point",
      finish: "✅ Finish session", next: "Next block →",
      noTopic: "No topic available", noScenario: "No scenario available",
      unknownBlock: (kind) => `Unknown block: ${kind}`,
    },
```

`STR.ja` に追加:

```ts
    menuTitle: {
      warmup: () => "音読ウォームアップ",
      ftt: (t) => `4/3/2: ${t}`,
      "ftt-mini": (t) => `4/3/2ミニ: ${t}`,
      "roleplay-daily": (t) => `日常ロールプレイ: ${t}`,
      "roleplay-business": (t) => `ビジネスロールプレイ: ${t}`,
      "roleplay-it": (t) => `ITロールプレイ: ${t}`,
      shadowing: (t) => `シャドーイング: ${t}`,
      reflection: () => "振り返り",
    },
    session: {
      building: "今日のメニューを組んでいます…", retry: "再試行", timerNote: "キリのいいところで次へ",
      finish: "✅ セッションを終える", next: "次のブロックへ →",
      noTopic: "トピックがありません", noScenario: "シナリオがありません",
      unknownBlock: (kind) => `未知のブロック: ${kind}`,
    },
```

- [ ] **Step 7: `SessionRunner.tsx` に lang を通し、タイトル組み立て・自画面文言を辞書化**

`SessionRunner.tsx`:
- import 追加: `import { STR, type Lang } from "../i18n";`
- `SessionRunner(props: { source: MenuSource; sessionId: string; onExit: () => void })` → `{ …; lang: Lang }`。`const t = STR[props.lang].session;`
- タイトル組み立てヘルパを追加（コンポーネント外）:
  ```tsx
  function blockTitle(block: MenuBlock, lang: Lang): string {
    // titleKey の無い旧キャッシュ（デプロイ当日など）は従来の JA title をそのまま使う
    if (!block.titleKey) return block.title;
    return STR[lang].menuTitle[block.titleKey](block.topicTitle ?? "");
  }
  ```
- 文言置換: 84行 `再試行`→`{t.retry}`、88行 `今日のメニューを組んでいます…`→`{t.building}`、116行 `title={block.title}`→`title={blockTitle(block, props.lang)}`、120行 note `"キリのいいところで次へ"`→`{t.timerNote}`（TimerChip の note prop）、129行 `isLast ? "✅ セッションを終える" : "次のブロックへ →"`→`isLast ? t.finish : t.next`。
- `BlockBody` に `lang` を渡す＆受ける: `<BlockBody block={block} sessionId={props.sessionId} lang={props.lang} />`。`function BlockBody({ block, sessionId, lang }: { block: MenuBlock; sessionId: string; lang: Lang })`。フォールバック文言を辞書化: `<p>トピックがありません</p>`(×3)→`<p>{STR[lang].session.noTopic}</p>`、`<p>シナリオがありません</p>`→`<p>{STR[lang].session.noScenario}</p>`、`<p>未知のブロック: {block.kind}</p>`→`<p>{STR[lang].session.unknownBlock(block.kind)}</p>`。
  - 注: BlockBody から各ブロック画面（Warmup/432/Roleplay/Shadowing/Reflection）へは Task 7/8 で `lang` を渡す。本タスクでは BlockBody が `lang` を受け取り自身のフォールバック文言に使うのみ。

- [ ] **Step 8: `App.tsx` から SessionRunner へ lang を渡す**

`App.tsx:118-120` を:

```tsx
      {mode.kind === "session" && (
        <SessionRunner source={mode.source} sessionId={sessionId} lang={lang} onExit={() => setMode({ kind: "start" })} />
      )}
```

- [ ] **Step 9: 検証ゲート**

Run: `cd app && bun test` → Expected: 347+3 相当（menu.test 追加分）pass
Run: `cd app && bun run typecheck` → Expected: 型エラー0
Run: `cd app/client && bun run build` → Expected: 型エラー0・ビルド成功
Run: `cd app/client && bun test` → Expected: PASS（18 tests）

- [ ] **Step 10: コミット**

```bash
git add app/server/menu.ts app/server/__tests__/menu.test.ts app/client/src/i18n.ts app/client/src/api/menu.ts app/client/src/screens/SessionRunner.tsx app/client/src/App.tsx
git commit -m "feat: メニュータイトルを titleKey+topicTitle で返しクライアントで言語別に組み立てる"
```

---

## Task 7: 練習系画面の辞書化 バッチ1（Warmup / 4-3-2 / Reflection / ChunkList）

**Files:**
- Modify: `app/client/src/i18n.ts`（`warmup` / `ftt432` / `reflection` / `chunkList` サブ辞書を EN/JA 追加、合成へ）
- Modify: `app/client/src/ui/ChunkList.tsx`（`playAria` prop 化）
- Modify: `app/client/src/screens/WarmupReadingScreen.tsx`（`lang` prop・文言辞書化）
- Modify: `app/client/src/screens/FourThreeTwoScreen.tsx`（`lang` prop・文言辞書化）
- Modify: `app/client/src/screens/ReflectionScreen.tsx`（`lang` prop・文言辞書化）
- Modify: `app/client/src/screens/SessionRunner.tsx:BlockBody`（対象3画面へ `lang` を渡す）

**Interfaces:**
- Produces: `warmup` / `ftt432` / `reflection` / `chunkList` サブ辞書。`WarmupReadingScreen`/`FourThreeTwoScreen`/`ReflectionScreen` が `{ lang: Lang; … }` を受ける。`ChunkList` は `playAria?: (en: string) => string` を受ける。
- Consumes: Task 6 の `lang` 配線（BlockBody→各画面）。

- [ ] **Step 1: `i18n.ts` にバッチ1のサブ辞書を追加（EN/JA・合成へ）**

型に `& WarmupStrings & Ftt432Strings & ReflectionStrings & ChunkListStrings` を追加し、named 型を定義:

```ts
type WarmupStrings = { warmup: {
  intro: string; loading: string; retry: string; fallbackTitle: string;
  clozeStepButton: string; clozeStepTitle: string; clozeStepBody: string; outlineTitle: string;
} };
type Ftt432Strings = { ftt432: {
  min: (v: string) => string;
  prepTitle: (topic: string) => string;
  prepIntro: (rounds: string, count: number, prep: string) => string;
  prepTimerNote: string; loading: string; retry: string; outlineTitle: string;
  modelIdle: string; modelScript: string; modelAudio: string; modelPlaying: string; modelRetry: string;
  startRound1: (min: string) => string; modelTranscript: string;
  aeTitle: string; aeLoading: string; aeNoRecording: string; startRound2: (min: string) => string;
  doneBody: (count: number) => string;
  roundHeading: (n: number, min: string, topic: string) => string;
  timeUp: string; recStop: string; recTranscribing: string; recStart: string; roundFinish: string;
  micError: (detail: string) => string;
  explainMore: string; explainLoading: string; explainError: string;
} };
type ReflectionStrings = { reflection: {
  loading: string; retry: string; goodPhrases: string; fixes: string; tomorrow: string;
  explainMore: string; explainLoading: string; explainError: string;
} };
type ChunkListStrings = { chunkList: { playAria: (en: string) => string } };
```

`STR.en` に追加:

```ts
    warmup: {
      intro: "Read these out loud (twice each). Tap 🔊 to hear a model. You'll use them in the 4/3/2 that follows.",
      loading: "Your coach is preparing phrases…", retry: "Retry",
      fallbackTitle: "Read these out loud instead",
      clozeStepButton: "🔡 Read with gaps (round 2 · optional)",
      clozeStepTitle: "Read with gaps (optional)",
      clozeStepBody: "This time fill the blanks yourself as you read aloud. The answers are in the list above.",
      outlineTitle: "Today's story outline",
    },
    ftt432: {
      min: (v) => `${v} min`,
      prepTitle: (topic) => `Prep — ${topic}`,
      prepIntro: (rounds, count, prep) => `You'll tell the same story ${count} times: ${rounds}. First, look over some phrases and an outline (about ${prep}).`,
      prepTimerNote: "Time to get started", loading: "Your coach is preparing phrases…", retry: "Retry",
      outlineTitle: "Story outline",
      modelIdle: "🎧 Hear a model talk (optional)", modelScript: "✍ Writing the script…",
      modelAudio: "🎙 Generating audio…", modelPlaying: "🔊 Playing…", modelRetry: "🎧 Model talk (retry)",
      startRound1: (min) => `Start Round 1 (${min}) →`, modelTranscript: "Model talk transcript",
      aeTitle: "Feedback (read it, then Round 2)", aeLoading: "Your coach is writing feedback…",
      aeNoRecording: "No recording, so there's no feedback", startRound2: (min) => `Start Round 2 (${min})`,
      doneBody: (count) => `4/3/2 done! You told the same story ${count} times, a little faster each round.`,
      roundHeading: (n, min, topic) => `Round ${n} (${min}) — ${topic}`,
      timeUp: "— Time's up!", recStop: "⏹ Stop recording", recTranscribing: "📝 Transcribing…",
      recStart: "🎙 Start speaking", roundFinish: "End this round →",
      micError: (detail) => `Can't access the microphone: ${detail}`,
      explainMore: "💡 Explain more", explainLoading: "Writing an explanation…", explainError: "Couldn't load the explanation.",
    },
    reflection: {
      loading: "Your coach is reviewing today's session…", retry: "Retry",
      goodPhrases: "👏 What went well", fixes: "✏️ Worth polishing", tomorrow: "📝 For tomorrow",
      explainMore: "💡 Explain more", explainLoading: "Writing an explanation…", explainError: "Couldn't load the explanation.",
    },
    chunkList: { playAria: (en) => `Play "${en}"` },
```

`STR.ja` に追加（**JA は現行ハードコードと一字一句一致**）:

```ts
    warmup: {
      intro: "声に出して読みましょう（各フレーズ2回ずつ）。🔊でお手本を聞けます。このあとの 4/3/2 で実際に使います。",
      loading: "コーチが表現チャンクを用意しています…", retry: "再試行",
      fallbackTitle: "代わりにこちらを声に出して読みましょう",
      clozeStepButton: "🔡 歯抜けで音読（2周目・任意）",
      clozeStepTitle: "歯抜けで音読（任意）",
      clozeStepBody: "今度は空欄を自分で埋めながら声に出しましょう。答えは上の一覧で確認できます。",
      outlineTitle: "今日の話の骨組み",
    },
    ftt432: {
      min: (v) => `${v}分`,
      prepTitle: (topic) => `準備 — ${topic}`,
      prepIntro: (rounds, count, prep) => `これから同じ話を ${rounds} で${count}回話します。まず使えそうな表現と骨組みを確認してください（目安 ${prep}）。`,
      prepTimerNote: "そろそろ始めましょう", loading: "コーチが表現チャンクを用意しています…", retry: "再試行",
      outlineTitle: "話の骨組み",
      modelIdle: "🎧 モデルトークを聞く（任意）", modelScript: "✍ 原稿を作成中…",
      modelAudio: "🎙 音声を生成中…", modelPlaying: "🔊 再生中…", modelRetry: "🎧 モデルトーク（再試行）",
      startRound1: (min) => `Round 1 を始める（${min}）→`, modelTranscript: "モデルトーク本文",
      aeTitle: "フィードバック（読んだら Round 2 へ）", aeLoading: "コーチがフィードバックを書いています…",
      aeNoRecording: "録音がなかったのでフィードバックはありません", startRound2: (min) => `Round 2 を始める（${min}）`,
      doneBody: (count) => `4/3/2 完了！同じ話を${count}回、少しずつ速く話せました。`,
      roundHeading: (n, min, topic) => `Round ${n}（${min}） — ${topic}`,
      timeUp: "— 時間切れ！", recStop: "⏹ 録音を止める", recTranscribing: "📝 文字起こし中…",
      recStart: "🎙 話し始める", roundFinish: "このラウンドを終える →",
      micError: (detail) => `マイクにアクセスできません: ${detail}`,
      explainMore: "💡 もっと詳しく", explainLoading: "解説を書いています…", explainError: "解説を取得できませんでした。",
    },
    reflection: {
      loading: "コーチが今日のセッションを振り返っています…", retry: "再試行",
      goodPhrases: "👏 良かった表現", fixes: "✏️ 直したい表現", tomorrow: "📝 明日へ",
      explainMore: "💡 もっと詳しく", explainLoading: "解説を書いています…", explainError: "解説を取得できませんでした。",
    },
    chunkList: { playAria: (en) => `「${en}」を再生` },
```

（注: 現行 432 の完了文言は「時間切れ！」の前に半角スペース＋全角？ 実コードは `{formatMmSs(timer.remaining)} {timer.expired && <span className="text-sm">— 時間切れ！</span>}` なので `timeUp` は `"— 時間切れ！"`。JA 一致を確認すること。roundHeading の全角括弧・中黒・ダッシュも実コード `Round {n}（{min}） — {topic}` に一致。）

- [ ] **Step 2: `ChunkList.tsx` を `playAria` prop 化（JA ハードコード aria を除去）**

```tsx
import { Button } from "./Button";
type Chunk = { en: string; ja?: string };

export function ChunkList(
  { chunks, playingIdx, onPlay, showJa = true, playAria }:
  { chunks: Chunk[]; playingIdx: number | null; onPlay?: (i: number, en: string) => void; showJa?: boolean; playAria?: (en: string) => string },
) {
  return (
    <ul className={`chunk-list${onPlay ? "" : " no-audio"}`}>
      {chunks.map((c, i) => (
        <li key={i}>
          {onPlay && (
            <Button variant="ghost" onClick={() => onPlay(i, c.en)} disabled={playingIdx !== null} ariaLabel={playAria ? playAria(c.en) : c.en}>
              {playingIdx === i ? "…" : "🔊"}
            </Button>
          )}
          <span className="chunk-en">{c.en}</span>
          {showJa && c.ja && <span className="chunk-ja">{c.ja}</span>}
        </li>
      ))}
    </ul>
  );
}
```

- [ ] **Step 3: `WarmupReadingScreen.tsx` を辞書化（`lang` prop 追加）**

- props: `export function WarmupReadingScreen(props: { topic: ContentItem; lang: Lang })`。import に `import { STR, type Lang } from "../i18n";`。本体先頭 `const t = STR[props.lang].warmup;`。
- 文言置換: 50行 intro→`{t.intro}`、52行→`{t.loading}`、55行 `再試行`→`{t.retry}`、60行→`{t.fallbackTitle}`、71行→`{t.clozeStepButton}`、74行 header→`{t.clozeStepTitle}`、75行→`{t.clozeStepBody}`、84行 header→`{t.outlineTitle}`。
- `ChunkList` 呼び出しに `playAria={(en) => STR[props.lang].chunkList.playAria(en)}` を追加（onPlay ありの本体側）。フォールバックの `<ChunkList chunks={props.topic.hints.map(...)} playingIdx={null} />`（onPlay 無し）は aria 不要。

- [ ] **Step 4: `FourThreeTwoScreen.tsx` を辞書化（`lang` prop 追加）**

- props に `lang: Lang` を追加。import に `import { STR, type Lang } from "../i18n";`。本体先頭 `const t = STR[props.lang].ftt432;`。
- ローカル `minLabel(seconds)`（26-29行）を、数値文字列を返す `minNum` に置換し単位は辞書へ:
  ```ts
  function minNum(seconds: number): string {
    const m = seconds / 60;
    return `${Number.isInteger(m) ? m : m.toFixed(1)}`;
  }
  ```
  以降 `minLabel(x)` は `t.min(minNum(x))` に、`roundsSec.map(minLabel).join("→")` は `roundsSec.map((s) => t.min(minNum(s))).join("→")` に置換。
- 文言置換（現行行 → 置換）:
  - 258 `準備 — ${props.topic.title}`→`{t.prepTitle(props.topic.title)}`
  - 260-261 の説明文→`{t.prepIntro(roundsSec.map((s) => t.min(minNum(s))).join("→"), roundsSec.length, t.min(minNum(PREP_SECONDS)))}`
  - 263 TimerChip `note="そろそろ始めましょう"`→`note={t.prepTimerNote}`
  - 270 `コーチが表現チャンクを用意しています…`→`{t.loading}`
  - 272 `再試行`→`{t.retry}`
  - 282 `<ChunkList … />` に `playAria={(en) => STR[props.lang].chunkList.playAria(en)}` 追加
  - 285 header `話の骨組み`→`{t.outlineTitle}`
  - 298-303 モデルボタン各状態→`t.modelIdle`/`t.modelScript`/`t.modelAudio`/`t.modelIdle`(ready も同文言)/`t.modelPlaying`/`t.modelRetry`
  - 306 `Round 1 を始める（${minLabel(roundsSec[0])}）→`→`{t.startRound1(t.min(minNum(roundsSec[0])))}`
  - 311 summary `モデルトーク本文`→`{t.modelTranscript}`
  - 322 header→`{t.aeTitle}`、323→`{t.aeLoading}`、324→`{t.aeNoRecording}`、337 `Round 2 を始める（…）`→`{t.startRound2(t.min(minNum(roundsSec[1])))}`
  - 346 完了文→`{t.doneBody(roundsSec.length)}`
  - 353-355 round heading→`{t.roundHeading(roundIndex + 1, t.min(minNum(roundsSec[roundIndex])), props.topic.title)}`
  - 363 `— 時間切れ！`→`{t.timeUp}`（`{timer.expired && <span className="text-sm">{t.timeUp}</span>}`）
  - 371 録音ボタン3状態→`t.recStop`/`t.recTranscribing`/`t.recStart`
  - 375 `このラウンドを終える →`→`{t.roundFinish}`
  - 185 `マイクにアクセスできません: …`→`t.micError(err instanceof Error ? err.message : String(err))`
  - AeItemView（Task 4 で useExplain 済み）の explain 文言を辞書化: このコンポーネントは `lang` を受けないため、`AeItemView` に `lang: Lang` を渡すよう `<AeItemView key={i} item={item} lang={props.lang} />`（330行）に変更し、`function AeItemView({ item, lang }: { … lang: Lang })` で `const t = STR[lang].ftt432;` を使い、`💡 もっと詳しく`→`{t.explainMore}`、`解説を書いています…`→`{t.explainLoading}`、`解説を取得できませんでした。`→`{t.explainError}`、`再試行`→`{STR[lang].ftt432.retry}` に置換。
  - `LISTENERS`（19-23行・英語）と `You:`（380行）は据え置き（Global Constraints のスコープ外方針どおり）。

- [ ] **Step 5: `ReflectionScreen.tsx` を辞書化（`lang` prop 追加）**

- props: `export function ReflectionScreen({ lang }: { lang: Lang })`。import に `import { STR, type Lang } from "../i18n";`。`const t = STR[lang].reflection;`。
- 置換: 51行 `再試行`→`{t.retry}`、55行 loading→`{t.loading}`、61行 `👏 良かった表現`→`{t.goodPhrases}`、66行 `✏️ 直したい表現`→`{t.fixes}`、74行 `📝 明日へ`→`{t.tomorrow}`。
- `FixItem`（Task 4 で useExplain 済み）に `lang` を渡す: `<FixItem key={i} fix={f} lang={lang} />`。`function FixItem({ fix, lang }: { fix: …; lang: Lang })` で `const t = STR[lang].reflection;` を使い `💡 もっと詳しく`→`{t.explainMore}`、`解説を書いています…`→`{t.explainLoading}`、`解説を取得できませんでした。`→`{t.explainError}`、`再試行`→`{t.retry}` に置換。

- [ ] **Step 6: `SessionRunner.tsx` の BlockBody から対象3画面へ `lang` を渡す**

`BlockBody`（Task 6 で `lang` 受領済み）の該当 return を変更:
- `<WarmupReadingScreen topic={block.params.topic} lang={lang} />`
- `<FourThreeTwoScreen topic={...} sessionId={...} blockId={...} roundsSec={...} modelTalkMode={...} lang={lang} />`
- `<ReflectionScreen lang={lang} />`

- [ ] **Step 7: 検証ゲート**

Run: `cd app/client && bun run build` → Expected: 型エラー0・ビルド成功（辞書の EN/JA 網羅は typecheck が保証）
Run: `cd app/client && bun test` → Expected: PASS（18 tests）
Run: `cd app && bun test` → Expected: pass（サーバ無影響）

- [ ] **Step 8: JA 回帰の目視確認（任意・推奨）**

`cd app && bun run dev` で JA トグルにして Warmup / 4-3-2 / Reflection を開き、文言が従来どおりであることを確認（自動テストは無いため）。

- [ ] **Step 9: コミット**

```bash
git add app/client/src/i18n.ts app/client/src/ui/ChunkList.tsx app/client/src/screens/WarmupReadingScreen.tsx app/client/src/screens/FourThreeTwoScreen.tsx app/client/src/screens/ReflectionScreen.tsx app/client/src/screens/SessionRunner.tsx
git commit -m "feat: Warmup/4-3-2/Reflection/ChunkList を i18n 辞書化（バッチ1）"
```

---

## Task 8: 練習系画面の辞書化 バッチ2（Shadowing / Library / Roleplay / FreeTalk）

**Files:**
- Modify: `app/client/src/i18n.ts`（`shadowing` / `library` / `roleplay` / `freeTalkScreen` サブ辞書を EN/JA 追加、合成へ）
- Modify: `app/client/src/screens/ShadowingScreen.tsx`（`lang` prop・文言辞書化）
- Modify: `app/client/src/screens/LibraryScreen.tsx`（`lang` prop・文言辞書化）
- Modify: `app/client/src/screens/RoleplayScreen.tsx`（`lang` prop・文言辞書化・EN 時はシナリオ英語タイトル）
- Modify: `app/client/src/screens/FreeTalkScreen.tsx`（`lang` prop・LABELS/エラー/ヒント/aria の辞書化）
- Modify: `app/client/src/screens/SessionRunner.tsx:BlockBody`（Shadowing/Roleplay へ `lang` を渡す）
- Modify: `app/client/src/App.tsx`（Library と 自由会話の FreeTalkScreen へ `lang` を渡す）

**Interfaces:**
- Produces: `shadowing` / `library` / `roleplay` / `freeTalkScreen` サブ辞書。`ShadowingScreen`/`LibraryScreen`/`RoleplayScreen`/`FreeTalkScreen` が `lang: Lang` を受ける。
- Consumes: Task 6 の lang 配線と `usePlayRow`（Library, Task 3 で適用済み）。

- [ ] **Step 1: `i18n.ts` にバッチ2のサブ辞書を追加（EN/JA・合成へ）**

型に `& ShadowingStrings & LibraryStrings & RoleplayStrings & FreeTalkScreenStrings` を追加:

```ts
type ShadowingStrings = { shadowing: {
  intro: string; writingScript: string; generatingAudio: string; retry: string;
  playing: string; play: string; showScript: string;
  explainMore: string; explainLoading: string; explainError: string;
} };
type LibraryStrings = { library: {
  title: string; loading: string; retry: string; empty: string;
  playAria: (title: string) => string; playing: string; transcript: string;
} };
type RoleplayStrings = { roleplay: { starters: string } };
type FreeTalkScreenStrings = { freeTalkScreen: {
  idle: string; recording: string; transcribing: string; thinking: string; speaking: string; errorLabel: string;
  micError: (detail: string) => string; notHeard: string;
  hintLabel: string; hintPlaceholder: string; hintButton: string; hintThinking: string; hintError: string; retry: string;
  you: string; ai: string; translate: string; translating: string; translateError: string;
} };
```

`STR.en` に追加:

```ts
    shadowing: {
      intro: "First, without looking at the script, repeat the audio slightly behind it, layering your voice over it (shadowing). Even one listen is fine. Stuck? Tap 'Show script' to check.",
      writingScript: "✍ Your coach is writing the model talk…", generatingAudio: "🎙 Generating audio…", retry: "Retry",
      playing: "🔊 Playing…", play: "▶ Play (as many times as you like)", showScript: "📄 Show script",
      explainMore: "💡 Translation & notes", explainLoading: "Writing the translation and notes…",
      explainError: "Couldn't load the explanation. Please try again.",
    },
    library: {
      title: "📚 Model Talk Library", loading: "Loading…", retry: "Retry",
      empty: "Nothing yet. Model talks you generate in 4/3/2 prep or Shadowing will be saved here.",
      playAria: (title) => `Play "${title}"`, playing: "🔊 Playing…", transcript: "Transcript",
    },
    roleplay: { starters: "You could open with:" },
    freeTalkScreen: {
      idle: "🎙 Speak (click to start recording)", recording: "⏹ Recording… (click to send)",
      transcribing: "📝 Transcribing…", thinking: "🤔 Thinking…", speaking: "🔊 Playing…", errorLabel: "🎙 Speak again",
      micError: (detail) => `Can't access the microphone: ${detail}`, notHeard: "Couldn't catch that. Please try again.",
      hintLabel: "Stuck? Type what you want to say in Japanese and I'll suggest ways to say it in English",
      hintPlaceholder: "e.g. その機能はまだ試していません", hintButton: "💡 Phrasing hint",
      hintThinking: "Thinking of ways to say it…", hintError: "Couldn't get hints. Please try again.", retry: "Retry",
      you: "You", ai: "AI", translate: "Translate", translating: "Translating…", translateError: "Couldn't load the translation.",
    },
```

`STR.ja` に追加（**JA 一字一句一致**）:

```ts
    shadowing: {
      intro: "まずはスクリプトを見ずに、音声に少し遅れてかぶせるように声に出して繰り返します（シャドーイング）。1回聞くだけでもOK。行き詰まったら「スクリプトを表示」で確認できます。",
      writingScript: "✍ コーチがモデルトークを書いています…", generatingAudio: "🎙 音声を生成しています…", retry: "再試行",
      playing: "🔊 再生中…", play: "▶ 再生（何度でも）", showScript: "📄 スクリプトを表示",
      explainMore: "💡 日本語訳と解説", explainLoading: "日本語訳と解説を書いています…",
      explainError: "解説を取得できませんでした。もう一度お試しください。",
    },
    library: {
      title: "📚 モデルトークライブラリ", loading: "読み込み中…", retry: "再試行",
      empty: "まだありません。4/3/2 の準備やシャドーイングでモデルトークを生成すると、ここに残ります。",
      playAria: (title) => `「${title}」を再生`, playing: "🔊 再生中…", transcript: "本文",
    },
    roleplay: { starters: "こう切り出せます:" },
    freeTalkScreen: {
      idle: "🎙 話す（クリックで録音開始）", recording: "⏹ 録音中…（クリックで送信）",
      transcribing: "📝 文字起こし中…", thinking: "🤔 考え中…", speaking: "🔊 再生中…", errorLabel: "🎙 もう一度話す",
      micError: (detail) => `マイクにアクセスできません: ${detail}`, notHeard: "音声を聞き取れませんでした。もう一度話してください。",
      hintLabel: "うまく言えないときは、言いたいことを日本語で入力すると英語の言い方を提案します",
      hintPlaceholder: "例: その機能はまだ試していません", hintButton: "💡 言い方のヒント",
      hintThinking: "言い方を考えています…", hintError: "ヒントを取得できませんでした。もう一度お試しください。", retry: "再試行",
      you: "あなた", ai: "AI", translate: "訳", translating: "訳しています…", translateError: "訳を取得できませんでした。",
    },
```

- [ ] **Step 2: `ShadowingScreen.tsx` を辞書化（`lang` prop 追加）**

- props: `export function ShadowingScreen(props: { topic: ContentItem; lang: Lang })`。import に `import { STR, type Lang } from "../i18n";`。本体先頭 `const t = STR[props.lang].shadowing;`。
- 置換: 71行 intro→`{t.intro}`、73行→`{t.writingScript}`、74行→`{t.generatingAudio}`、76行 `再試行`→`{t.retry}`、83行 `🔊 再生中…`/`▶ 再生（何度でも）`→`t.playing`/`t.play`、86行 `📄 スクリプトを表示`→`{t.showScript}`。
- Task 4 の useExplain ブロック（Step 4 で導入済み）の文言を辞書化: `💡 日本語訳と解説`→`{t.explainMore}`、`日本語訳と解説を書いています…`→`{t.explainLoading}`、error 文言→`{t.explainError}`、`再試行`→`{t.retry}`。

- [ ] **Step 3: `LibraryScreen.tsx` を辞書化（`lang` prop 追加）**

- props: `export function LibraryScreen({ lang }: { lang: Lang })`。import に `import { STR, type Lang } from "../i18n";`。`const t = STR[lang].library;`。（`usePlayRow` は Task 3 で適用済み。）
- 置換: 36行 `📚 モデルトークライブラリ`→`{t.title}`、37行 `読み込み中…`→`{t.loading}`、39行 `再試行`→`{t.retry}`、42-44行 空状態→`{t.empty}`、56行 aria `「${e.topicTitle || e.topicId}」を再生`→`t.playAria(e.topicTitle || e.topicId)`、58行 `🔊 再生中…`→`{t.playing}`（`▶` はそのまま）、66行 summary `本文`→`{t.transcript}`。

- [ ] **Step 4: `RoleplayScreen.tsx` を辞書化（`lang` prop 追加・EN 時シナリオ英語タイトル）**

```tsx
import { type ContentItem } from "../api";
import { STR, type Lang } from "../i18n";
import { Card } from "../ui/Card";
import { FreeTalkScreen } from "./FreeTalkScreen";

export function RoleplayScreen(props: { scenario: ContentItem; lang: Lang }) {
  const t = STR[props.lang].roleplay;
  const starters = props.scenario.starters ?? [];
  // EN 表示ではシナリオの英語タイトル、JA では従来どおり titleJa
  const heading = props.lang === "ja" ? props.scenario.titleJa : props.scenario.title;
  return (
    <div className="stack">
      <Card>
        <p className="text-muted">{heading}</p>
        <ul>
          {props.scenario.hints.map((h, i) => (<li key={i}>{h}</li>))}
        </ul>
        {starters.length > 0 && (
          <div className="stack">
            <p className="text-sm text-muted">{t.starters}</p>
            <ul>
              {starters.map((s, i) => (<li key={i}>{s}</li>))}
            </ul>
          </div>
        )}
      </Card>
      <FreeTalkScreen scenarioId={props.scenario.id} lang={props.lang} />
    </div>
  );
}
```

（挙動注: JA では `titleJa` 表示のまま＝一字一句同一。EN のみ `title`（英語）に切替＝新規挙動で監査の非対称を解消。）

- [ ] **Step 5: `FreeTalkScreen.tsx` を辞書化（`lang` prop 追加）**

- props: `export function FreeTalkScreen(props: { scenarioId?: string; onSessionId?: (id: string) => void; lang: Lang })`。import に `import { STR, type Lang } from "../i18n";`。本体先頭 `const t = STR[props.lang].freeTalkScreen;`。
- モジュール定数 `LABELS`（10-17行）は lang 依存にできないため、本体内の派生に変更:
  ```tsx
  const LABELS: Record<Status, string> = {
    idle: t.idle, recording: t.recording, transcribing: t.transcribing,
    thinking: t.thinking, speaking: t.speaking, error: t.errorLabel,
  };
  ```
  （`type Status` 定義は残す。モジュールトップの `const LABELS` は削除。）
- 置換: 46行 `マイクにアクセスできません: …`→`t.micError(err instanceof Error ? err.message : String(err))`、59行→`{t.notHeard}`（`setErrorMsg(t.notHeard)`）、125行 hint ラベル→`{t.hintLabel}`、133行 placeholder→`placeholder={t.hintPlaceholder}`、135行 `💡 言い方のヒント`→`{t.hintButton}`、137行→`{t.hintThinking}`、107行 `setHintError("ヒントを…")`→`setHintError(t.hintError)`、141行 `再試行`→`{t.retry}`、158行 aria `t.role === "you" ? "あなた" : "AI"`→`t.role === "you" ? t.you : t.ai`、162行 `訳`→`{t.translate}`、164行→`{t.translating}`、167行 `訳を取得できませんでした。`→`{t.translateError}`、168行 `再試行`→`{t.retry}`。

- [ ] **Step 6: 呼び出しサイトへ `lang` を渡す**

- `SessionRunner.tsx` BlockBody: `<ShadowingScreen topic={block.params.topic} lang={lang} />`、`<RoleplayScreen scenario={block.params.scenario} lang={lang} />`。
- `App.tsx`: 127行 `<FreeTalkScreen />`→`<FreeTalkScreen lang={lang} />`、130行 `<LibraryScreen />`→`<LibraryScreen lang={lang} />`。

- [ ] **Step 7: 検証ゲート**

Run: `cd app/client && bun run build` → Expected: 型エラー0・ビルド成功（EN/JA 網羅は typecheck が保証）
Run: `cd app/client && bun test` → Expected: PASS（18 tests）
Run: `cd app && bun test` → Expected: pass

- [ ] **Step 8: JA 回帰の目視確認（任意・推奨）**

`cd app && bun run dev` で JA トグルにして Shadowing / Library / Roleplay / 自由会話を開き、文言が従来どおりであることを確認。EN トグルで新規英語表示を確認。

- [ ] **Step 9: コミット**

```bash
git add app/client/src/i18n.ts app/client/src/screens/ShadowingScreen.tsx app/client/src/screens/LibraryScreen.tsx app/client/src/screens/RoleplayScreen.tsx app/client/src/screens/FreeTalkScreen.tsx app/client/src/screens/SessionRunner.tsx app/client/src/App.tsx
git commit -m "feat: Shadowing/Library/Roleplay/FreeTalk を i18n 辞書化（バッチ2）"
```

---

## 実装順序と依存

1. **Task 1（api 分割）** → 2. **Task 2（i18n 合成＋drills union）** → 3. **Task 3（SentencesScreen 分割＋usePlayRow）** → 4. **Task 4（useExplain）** → 5. **Task 5（useSupport/showJaFromPrep）** → 6. **Task 6（メニュータイトル＋lang 配線）** → 7. **Task 7（辞書バッチ1）** → 8. **Task 8（辞書バッチ2）**

R4（1〜5）の構造改革を先に済ませ、その i18n 基盤（Task 2 の合成）と lang 配線（Task 6）の上に P4 の8画面辞書（7〜8）を載せる。Task 4 の useExplain は Task 7（Reflection/432）・Task 8（Shadowing）の explain 文言辞書化の前提。Task 6 は Task 7/8 の lang 配線の前提。
