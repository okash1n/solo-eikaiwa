node_modules/
models/
.env
.env.*
!.env.example
dist/
# verify.sh の検証専用ビルド出力（配信用 dist とは分離・#196）
dist-verify/
/app/client/test-results/
*.log
.superpowers/

# Claude Code 個人設定（hook等）
.claude/settings.local.json

# bun build --compile が中断時に残す一時ファイル（63MB級・PUBLIC履歴に入れない）
*.bun-build

# 鍵・リリースsecretsの誤コミット防止（最終防衛線・#243）。追跡済みの該当ファイルが
# 無いことを git ls-files で確認済み（2026-07-17）
*.p8
*.p12
*.pem
*.key
release.env
